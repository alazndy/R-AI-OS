# Project Memory: R-AI-OS

## Context
- **Status**: In Development — v3.2.0 (workspace split + daemon auth hardening)
- **Stack**: Rust 2021 Edition + Ratatui TUI + Tokio async + SQLite (rusqlite) + Axum HTTP + VS Code Extension
- **Last Milestone**: Full-day security/quality audit pass (2026-07-07) — brutal security audit of the daemon auth chain (7 findings, all fixed), test-suite coverage audit (4 gaps closed), adversarial re-review of that same day's work (12 findings, all resolved), and a repo-hygiene pass (version drift, dangling gitlink, stale docs). 583 tests green, 0 clippy warnings. See 2026-07-07 Change Log entries below for the full trail; detailed findings live in `docs/adversarial-review-2026-07-07.md`.
- **Repo**: https://github.com/alazndy/R-AI-OS

## Active Objectives
- [x] Phase 11: Tool Pinning — implemented & wired into MCP dispatch (`security/tool_pin.rs`)
- [x] Phase 12: Secret Leasing — `raios secret grant/list/revoke` fully implemented
- [x] Phase 2B: memory.md Write-Back — sidebar checkboxes now interactive; `raios task-update` CLI added
- [x] Rate Limiting — `security/rate_limiter.rs`, configurable via raios-policy.toml
- [x] Quarantine Mode — `security/quarantine.rs`, MCP dispatch integration complete
- [x] Lifecycle Worker — auto beklemede/archived/active transitions based on git activity (`daemon/lifecycle.rs`)
- [x] discover status-preserve fix — beklemede/archived no longer reset by discover
- [x] Phase 17: 4-Agent Matrix & Atomic Handoff — `raios handoff`, TUI Inbox panel, `agy` spawn fix
- [x] aiosd systemd service — enabled, RestartPreventExitStatus=SIGTERM + PATH env added
- [x] Clean up the disconnected legacy `tool_handover` MCP tool — rewired to create_handoff_workflow cp_*
- [x] UMAI — Universal Multi-agent Access & Isolation-guard (security/umai.rs), wired to WS daemon + MCP
- [x] Delta events — AgentStarted/AgentStopped/HealthDelta broadcast; TUI handlers added
- [x] Plan A: Cortex Real Embeddings & hf-hub Redirect Fix
- [x] Plan B: Autonomous Scheduler (`raios cron` CLI + background daemon worker)
- [x] Audit ledger write path — `record_tool_decision` wired into MCP + WS dispatch (was defined but never called)
- [x] `raios policy suggest`/`show`/`caps` — learns rule suggestions from audit history, shows resolved capabilities
- [x] Capability-declaration sandbox — `ToolCapabilities` on `ToolRule` + `security/capabilities.rs`, enforced pre-dispatch
- [x] Egress enforcement chokepoint — `EgressFilter` now actually checked (via declared network capabilities)
- [x] A2A protocol surface — `GET /.well-known/agent.json` + `POST /a2a` (`message/send`, `tasks/get`, `tasks/list`)
- [x] Security-kernel hardening pass — filesystem jail no-op, blocklist `..` bypass, egress/SSRF hardening, live WS shell-injection RCE, `cargo audit` clean (see 2026-07-02 entries)

## Technical Decisions
- **Architecture**: Modular kernel — `src/security/`, `src/kernel/`, `src/control_plane.rs`, `src/swarm/`, `src/cortex/`
- **Database**: SQLite via rusqlite (bundled). `cp_*` schema is the sole source of truth. Legacy tables are read-only cache.
- **Control Plane**: `cp_daemon_snapshot()` = single-call operational view. `cp_scheduler_list_ready()` = canonical task scheduler.
- **Security Kernel**: 4 layers — Filesystem Jail → Policy Manager → Verify Chain (hash-chained audit ledger) → Egress Filter
- **Policy**: `raios-policy.toml` is the single security config source. `confirm` = fail-closed (deny) in daemon/stdio mode.
- **Egress**: Filtered at MCP tool hook level (not OS network layer — scoped to what raios controls).
- **Hash Chain**: Tamper *detection*, not prevention — correctly classified as forensic tool.
- **IPC**: VS Code extension proxies requests to daemon (port 42071) via Bearer token — token never exposed to WebView.
- **Critical Notes**: New work must read/write through `cp_*` functions in `src/db.rs`. Never write directly to legacy tables.

## Important Links & Paths
- **Main Entry (CLI)**: `src/bin/raios.rs`
- **Daemon Entry**: `src/bin/aiosd.rs`
- **DB Layer**: `src/db.rs` — all `cp_*` functions here
- **Security**: `src/security/` — sandbox.rs, policy.rs, verify_chain.rs, egress.rs, capabilities.rs, policy_suggest.rs, secret_scan.rs
- **A2A Protocol**: `src/server/http/a2a.rs` — `/.well-known/agent.json` + `/a2a`
- **Control Plane**: `src/control_plane.rs`
- **TUI**: `src/ui/panels/` (13 modules)
- **VS Code Extension**: `vscode-extension/`
- **Policy Config**: `raios-policy.toml`
- **Repo**: `gitrepo.md`

## Current Focus
- Health worker RAM resolved (5.8GB → 1.1GB peak). Project in hardening/expansion mode.
- Next candidates: VS Code extension package bump, tray macOS/Windows validasyonu.

## Change Log & Agent Trail
- 2026-06-03 [Antigravity Kaira]: Hash-Chained Audit Ledger, Redaction Engine, Sentry SDK integration — v2.0.0-alpha foundation
- 2026-06-04 [Antigravity Kaira]: Security Kernel 4 phases complete (Sandbox+Policy+Chain+Egress), 239 tests. Hybrid UI Faz 2A+2C (VS Code Sidebar WebView + TokenBridge). raios-0.4.0.vsix packaged.
- 2026-06-04 [Antigravity Kaira]: Refactor — events.rs→events/, dashboard.rs→13 panels, build.rs→10 submodules, deps.rs→10 submodules, keyboard.rs→6 submodules. False-positive risk pattern fix in refactor_scan.rs.
- 2026-06-10 [Claude Kaira]: Control Plane Migration phases 1-8 complete. cp_* is sole source of truth. Legacy tables cache-only. cp_daemon_snapshot() added. 376→378 tests green.
- 2026-06-13 [Claude Kaira]: memory.md migrated to AGENT_CONSTITUTION v5.0 template. PATH fixed (~/.cargo/bin added to .zshrc). Claude Kaira skills installed.
- 2026-06-13 [Claude Kaira]: K-AI-RA identity integrated into setup wizard. Project dedup fix (145→52). DB vacuum (88MB→308KB). Zombie aiosd fix. raios-tray built (AppIndicator3, daemon toggle, all-projects popup, agent launcher). Phase 2B write-back implemented (sidebar checkboxes + raios task-update CLI). All backlog phases confirmed complete. 378 tests green.
- 2026-06-16 [Claude Kaira]: Lifecycle worker added (auto beklemede/archived/active via git activity). DaemonConfig: lifecycle_standby_days/archive_days/interval_secs. upsert_project CASE fix (beklemede/archived preserved on discover). ZRAM 8GB + swappiness=10 system optimization. aiosd config intervals increased (CPU relief). Active project set defined in DB.
- 2026-06-18 [Codex Kaira]: Cross-platform `raios-tray` project copy added under `tools/raios-tray/` in this repo, including PySide6 tray app, startup assets, and in-tray `aiosd` settings editor.
- 2026-06-22 [Claude Kaira]: Gemini CLI removed from entire codebase (~165 lines across 28+ source files) — Google shut down Gemini CLI. Removed WizardStep::Gemini, scan_gemini_usage, agent_runner gemini case, filebrowser Gemini CLI section, cli/new.rs config block, all gemini keybindings/actions from events/, tasks.rs agent parser patterns, discovery gemini paths, db.rs gemini provider, GEMINI.md hardlinks, ui panels references. Replaced gemini fallback with claude in cli/new.rs. Updated ~20 test files. 0 warnings, clean cargo check.
- 2026-06-25 [Claude Kaira]: `raios handoff` added — atomic 4-agent handoff (claude_kaira/codex_kaira/opencode_kaira/antigravity_kaira) built on the existing control plane, not a new STATE.json. `create_handoff_workflow`/`cp_take_pending_handoff`/`cp_consume_handoff` in db.rs reuse `cp_tasks`/`cp_agent_runs`/`cp_artifacts`/`cp_approvals` (`ArtifactKind::HandoverNote`, `ApprovalType::Handover` were already defined, now wired up). `agent_runner::run_agent` delivers pending handovers to `raios run`/`raios task` spawns via `RAIOS_HANDOVER_CONTEXT` env, exactly once. Also fixed a stale, never-committed `lock_manager.rs` test (`blocks_equal_priority_second_owner`) left broken by the 2026-06-22 gemini→claude rename above — it had started asserting against itself. 379 tests green, 0 warnings. AGENT_CONSTITUTION.md Section 1 + new Section 10 updated to match (OpenCode/Antigravity as separate identities, Gemini fully dropped).
- 2026-06-25 [Claude Kaira]: Live-tested the handoff above end-to-end against the real `~/.config/raios/workspace.db` (not a scratch DB): `raios handoff --to codex-kaira` then `raios run codex` delivered `[HANDOVER CONTEXT]` and flipped approval pending→approved, task awaiting_approval→completed. Surfaced a pre-existing gap while doing so: `agent_runner::run_agent`'s spawn match had no `"codex"` arm (`raios run codex` always errored "Unsupported agent" despite Codex Kaira being a canonical identity) — added it. 379 tests still green, 0 warnings.
- 2026-06-25 [Claude Kaira]: Found that `RAIOS_HANDOVER_CONTEXT` (and the pre-existing `RAIOS_INSTINCTS`) are env vars no CLI actually reads — `claude`/`codex`/`opencode` are real upstream binaries, not raios wrapper scripts. Rewired the handoff delivery in `agent_runner.rs` to use each tool's real prompt-injection surface: `claude --append-system-prompt <block>`, `codex <block>` (positional PROMPT), `opencode --prompt <block>`; env var kept only as a best-effort fallback for `antigravity` (not installed on this box, flag unverified). Confirmed via `strace -f -e trace=execve` that the handover text lands in `codex`'s actual argv. 379 tests green, 0 warnings.
- 2026-06-25 [Codex Kaira]: Reviewed the handoff feature via a real handoff delivered to codex_kaira, fixed 2 real defects: `create_handoff_workflow` wasn't atomic (now wrapped in `conn.unchecked_transaction()`), and `run_agent` consumed a handoff even if `cmd.spawn()` failed (now consumes only after spawn succeeds). Committed everything as `bebff1b` — note this commit also swept in the long-uncommitted 2026-06-22 Gemini-removal diff (36 files) since it was sitting dirty in the tree.
- 2026-06-25 [Claude Kaira]: Researched A2A protocol / handoff best-practice articles, added 3 improvements on top of Codex's fixes: (1) `cli/handoff.rs` auto-attaches `git diff --stat HEAD` as `HandoffContext.diff_stat`, rendered to the receiving agent as a `[Changed files since handoff]` block. (2) `looks_like_secret()` heuristic-scans `--msg` for API keys/tokens/private-key blocks (AWS, Anthropic, OpenAI-style, GitHub, PEM headers, generic key=value) and refuses the handoff before it ever touches the DB or process argv. (3) `create_handoff_workflow` now supersedes any still-pending handover for the same `(to_agent, project_id)` inside the same transaction — old approval→`expired`, artifact→`superseded`, task→`cancelled` — so only one active handoff per assignee/project survives. 380 tests green (1 new), 0 warnings. Live-verified all three against the real DB, then cleaned up the test rows.
- 2026-06-25 [Claude Kaira]: `raios run agy`/`raios run antigravity` were both broken — the real Antigravity CLI binary is named `agy` (confirmed via `agy --help`), but `agent_runner.rs` only recognized the literal string `"antigravity"` as input and then tried to spawn a binary called `antigravity` (doesn't exist). Fixed: `"antigravity" | "agy"` both accepted as input, both spawn `Command::new("agy")`, both resolve to `antigravity_kaira` in `canonical_agent_identity`. Wired the real handover flag too: `agy --prompt-interactive <block>` (verified via `strace`: diff_stat + context delivered correctly). Antigravity now has full parity with claude/codex/opencode instead of only the env-var fallback. 380 tests green, 0 warnings.
- 2026-06-25 [Claude Kaira]: Audited the ratatui TUI for control-plane visibility — found none of the 13 `ui/panels/` screens render `cp_tasks`/`cp_approvals`/`cp_agent_runs` at all (only the MCP tool `get_inbox` aggregated them, invisible to a human at the terminal). Added a 14th menu item "Inbox" (`src/ui/panels/inbox.rs`, wired via `MENU_ITEMS`/`content.rs` index 13) rendering pending approvals (handovers tagged distinctly), active agent runs, and blocked tasks straight from the existing `cp_query_*` functions — no new DB plumbing needed. Also added an `agent_handoff` case to `ORIGIN_EXPR` in db.rs so handoff tasks are labeled correctly wherever task origin is shown. **Found and fixed 2 real bugs while visually verifying with `screen` (no TUI test harness existed, so launched it for real in a detached `screen` session and `hardcopy`'d the frames):** (1) `cp_list_personal_tasks`'s WHERE clause only checked `plan_id`/`parent_task_id IS NULL`, which handoff (and likely file-change-approval) tasks also satisfy — they were leaking into the personal-task checklist sidebar with checkbox UI. Fixed by excluding any task with a `cp_approvals` row attached. (2) `create_handoff_workflow`'s supersede step updated approval/artifact/task status but forgot `cp_agent_runs` — superseded handoffs left orphaned `awaiting_approval` runs that lingered forever in the Inbox's "active runs" section. Both fixed, both regression-tested (381 tests green, 0 warnings). Found a separate, disconnected legacy MCP tool `tool_handover` (`mcp/tools_workspace.rs`) that writes to `_session_notes.md` and pings a likely-dead TCP port (42069) — predates the `cp_*`-based handoff system entirely, untouched so far, flagged for later cleanup.
- 2026-06-27 [Claude Kaira]: Health worker RAM spike fixed — `scan_project_fast` added to `security/scanner.rs` skipping `cargo audit`/semgrep in background scans. Peak dropped 5.8GB → 1.1GB. 386 tests green.
- 2026-06-27 [Claude Kaira]: UMAI (Universal Multi-agent Access & Isolation-guard) added — `src/security/umai.rs`, wired as middleware in WS daemon dispatch and MCP tool dispatch. AgentStarted/AgentStopped/HealthDelta delta events added with proxy broadcast + TUI handler chain. Telemetry channel separated (telem_tx cap 64, lossy) from control channel. tool_handover legacy rewritten to use create_handoff_workflow cp_*. aiosd.service hardened (RestartPreventExitStatus=SIGTERM, PATH env). raios-policy.toml: explicit WS command rules added. 386 tests green.
- 2026-06-25 [Claude Kaira]: Version bump `2.0.0-alpha` → `3.0.0` (`raios version-bump major . --changelog --tag`) for the 4-agent handoff milestone. Replaced the auto-generated CHANGELOG entry (it dumped the *entire* git history since no prior tag existed) with a curated v3.0.0 section. README updated: version badges, ASCII banner, dropped remaining Gemini CLI mentions (`/api/usage` table, vision blurb), added a "📨 Agent Handoff" module section, `raios handoff` CLI reference row, Phase 17 roadmap entry, panel count 13→14. Git tag `v3.0.0` deliberately deleted and not yet recreated — it was created by the bump command against the pre-existing HEAD (Codex's `bebff1b`), before this round's commit existed; needs to be re-tagged against the actual 3.0.0 commit.
- 2026-06-28 [Antigravity]: Implemented background scheduler worker and `raios cron` subcommands (add, list, remove, pause, resume, run). Created `cp_scheduled_jobs` database schema with atomic claiming, crash recovery, and cron job activation controls. Added 4 unit tests. Fixed negative time interval parsing in sqlite. 390 green tests (with 1 unrelated failure). Manual integration validated. Implemented adaptive CPU throttling in Cortex search indexing (`embed_batch`), chunking requests dynamically based on available cores and `/proc/loadavg` load to prevent CPU spikes and maintain system stability.
- 2026-06-28 [Claude Kaira]: Agent Wrapper feature added — WizardStep::AgentWrapper (step 7/9) installs shell functions (claude/codex/opencode/agy → raios run <agent>) into ~/.zshrc/~/.bashrc. Zero recursion risk: Command::new() bypasses shell functions via execve. CLI: raios agent-wrapper install|remove|status. Config: agent_wrapper_enabled persisted. 393 tests green. commit 5f6a974.
- 2026-06-28 [Claude Kaira]: TUI Scheduler panel added (src/ui/panels/scheduler.rs, menu index 14) — live view of cp_scheduled_jobs with active/paused sections, interval formatting, next_run_at. VS Code extension bumped v0.8.0: cronList, cronAdd (4-step QuickPick flow), handoff (target+status QuickPick), inboxStatus commands wired. .gitignore updated (.fastembed_cache/, .context/, .tmp/). ~/.claude/settings.json expanded with comprehensive bash permissions (cargo *, git *, raios *, gh *, systemctl, find, grep, rg, etc.) to eliminate per-command auth prompts. SwarmStore confirmed already on SQLite (no migration needed). 3 commits: 3429312, c2bcd53.


- 2026-06-30 [Claude Kaira]: (1) Fixed `waiting` project status resetting hourly — 3-point protection: normalizasyon NOT IN, upsert CASE, lifecycle skip+reload filter (src/db.rs, src/entities.rs, src/daemon/lifecycle.rs). Root cause: eski aiosd binary (PID 25178) çalışırken lifecycle her saatte open_db() ile override ediyordu. (2) Fixed `raios run` agent flag forwarding — trailing_var_arg+allow_hyphen_values (src/cli/mod.rs) + extra_args param + CWD fallback (src/agent_runner.rs). Artık `claude --model opus --resume` çalışıyor. (3) Fixed Turkish auto-memory — heuristic_extract'e TR keyword'ler eklendi (src/session_memory.rs). (4) atc-report.md + atc-analysis/ global gitignore'a eklendi. 38 waiting proje korunuyor, 4 yeni GitHub repo oluşturuldu. Commits: 572a7a5, bde1475.
- 2026-06-30 [Codex Kaira]: Refactor split batch complete for `src/core/process.rs`, `src/system_scan.rs`, and `src/server/http.rs` into directory modules (`process/{command,ports,processes,kill}`, `system_scan/{tools,usage}`, `server/http/{auth,plans,routes,websocket}`) while preserving public API paths. Original flat files removed. Full `cargo test` green: 393 passed.
- 2026-07-01 [Antigravity Kaira]: Resolved all 34 compiler warnings. Cleaned up unused imports, unused mut variables, and unused private helper functions/tests in `src/daemon/server.rs`. Added `#[allow(dead_code)]` to deserialization-only fields in `src/app/events/ext_actions.rs` and `src/cli/ext/mod.rs`. Regenerated `SIGMAP.md` and `.github/copilot-instructions.md`. All 390 tests green.
- 2026-07-01 [Antigravity Kaira]: Repaletted TUI theme to orange, cyan, blue, turquoise. Updated BANNER to use strictly orange (AMBER) for the top 3 lines and blue (CYAN) for the bottom 3 lines, keeping the rest of the UI in the specified theme colors. 390 tests green.
- 2026-07-01 [Claude Kaira]: Fixed 17 clippy warnings to 0 (dead identical if/else in `app/events/ext_actions.rs`, manual `strip_prefix` in `security/umai.rs`, `mem_upsert`'s 8-arg signature grouped into a `MemUpsert` struct in `db/mem.rs`, 14 more auto-fixed via `cargo clippy --fix`). 390 tests green.
- 2026-07-01 [Claude Kaira]: Competitive-landscape research (GitHub + web + Exa search) surfaced 3 gaps vs. adjacent projects (`snulbug`, `dullfig/AgentOS`, Google A2A) and 2 dormant subsystems (`record_audit_event` never called in production; `EgressFilter` never enforced at a call site). Implemented all three as a 4-phase plan:
  - **Phase 0** (prereq): wired `record_tool_decision` (new shared helper in `security/verify_chain.rs`) into both `mcp/tools.rs::handle_tools_call` and `daemon/handlers.rs` WS dispatch — every UMAI allow/deny/confirm decision now lands in `audit_log` as `{tool, args_hash, matched_rule}` (raw args never persisted, only a SHA-256 hash). `EgressFilter` moved into the `McpServer` struct (`mcp/mod.rs`), constructed but not yet enforced — Phase 2's chokepoint.
  - **Phase 1**: `raios policy suggest [--min-count N] [--apply]` / `raios policy show` — new `security/policy_suggest.rs` (`suggest_policy_rules`, pure/testable) reads the now-populated audit ledger and proposes `[[tools.rules]]` entries. Never suggests escalating a tool's privilege — only pins down whatever `default_action` already produced; confirm-heavy tools surface as a review note, not an allow suggestion. `--apply` appends idempotently to `raios-policy.toml` (new `PolicyConfig::default_path()`).
  - **Phase 2**: Capability-declaration sandbox ("no ambient authority") — new `ToolCapabilities` field on `ToolRule` (`security/policy.rs`) + `security/capabilities.rs` static default map for all 32 built-in tools + `resolve`/`check_fs_capability`/`check_network_capability`. Enforced in `handle_tools_call` before dispatch: the 12 tools that resolve a real path via `resolve_git_path` (`PATH_RESOLVING_TOOLS`) are checked against `SandboxGuard`; any tool declaring `network` capability is checked against the (now-wired) `EgressFilter`. `exec` is declarative-only (surfaced via new `raios policy caps [tool]`) — no chokepoint exists for arbitrary `Command::new` spawns without a much larger refactor, stated honestly in code comments rather than pretending to enforce it. New `capability:` error prefix → JSON-RPC code `-32024`.
  - **Phase 3**: A2A (Agent2Agent) protocol surface — `GET /.well-known/agent.json` (public Agent Card, exempted from Bearer auth in `server/http/auth.rs`) + `POST /a2a` (new `server/http/a2a.rs`, JSON-RPC 2.0: `message/send` → reuses `create_handoff_workflow` verbatim, `tasks/get`/`tasks/list` → reuses the same `cp_query_active_tasks` read model as `GET /api/inbox`). `looks_like_secret` extracted from `cli/handoff.rs` into a shared `security/secret_scan.rs` so both the CLI and A2A delivery paths get the same secret-paste deterrent.
  - Live-verified end-to-end against the real `~/.config/raios/workspace.db` after a full `install.sh` rebuild+restart: MCP `project_info` call passed UMAI→capability→dispatch; `/a2a` `message/send` created a real handoff (cleaned up test rows after), secret-laden text was correctly refused with `-32010`, missing Bearer token correctly 401'd, `raios verify-chain` confirmed the hash chain stayed intact through all of it. 424 tests green (34 new), 0 clippy warnings.
- 2026-07-01 [Codex Kaira]: Aligned the 5-control-feature work with the approved implementation plan. Added `raios` post-run review storage/output via new `git_utils` + `session_review`, moved handoff diff-stat to the shared helper, split preflight into `CommitGate` vs `AgentRunGate`, added opt-in `[preflight].enforce_before_run` policy support with `RAIOS_SKIP_PREFLIGHT=1` escape hatch, and kept wrapper session summaries in `cp_agent_runs.summary` without overwriting old values on `None`. Regenerated SigMap output. Full verification clean: `cargo test --lib` 447 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Started the modularization/TUI refactor track with a new `docs/superpowers/plans/2026-07-01-raios-modularization-tui-plan.md` and `src/app/services.rs` service boundary. Moved `Inbox`, `Scheduler`, `Policies`, and `Projects` list rendering onto typed service snapshots, extracted extension shell/config helpers out of `app/events/ext_actions.rs`, and removed render-path `systemctl` calls by precomputing extension service status during discovery. Full verification clean: `cargo test --lib` 450 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Continued the modularization pass by moving `ProjectDetail` loading off controller inline logic into `app/services.rs`: `memory.md`, `git log`, `git diff`, and `GRAPH_REPORT.md` now load through service helpers, and `BgMsg::ProjectOpened` carries a typed `ProjectDetailData` payload instead of ad-hoc vectors. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Continued trimming controller fat in `app/events/commands.rs`: remote daemon command JSON construction (`Search`, `GetLogs`, remote `raios` job submit) and vault-note creation moved behind shared `app/services.rs` helpers, and the duplicate local `append_memo()` implementation was deleted in favor of `app/events/helpers.rs`. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Continued the TUI/runtime boundary cleanup by extracting IPC bootstrap and protocol helpers out of `app/ipc.rs` into new `app/ipc_support.rs` (`resolve_daemon_addr`, local daemon auto-spawn, auth handshake, initial state request, standardized IPC log entries). `ipc.rs` now focuses on connection lifecycle and event dispatch. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Finished the next IPC split layer by moving the large daemon event decode/forward `match` out of `app/ipc.rs` into dedicated `app/ipc_events.rs`. The connection loop is now isolated from event payload translation, which makes a later runtime/surface crate split much less tangled. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Continued collapsing the remaining TUI monolith by splitting `app/events/bg_messages.rs` internally into focused lifecycle/sync/file-change/extension handler methods. `handle_bg_msg()` is now mostly a router, and the heavier side effects sit behind named methods instead of one 500-line match block. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Antigravity Kaira]: Pulled out keyboard orchestration logic (launch_agent, daemon JSON messages, git commit/push threads, health refresh triggers) from keyboard event handlers into dedicated App controller methods in actions.rs. Keyboard modules are now thin routers. Cleaned up unused imports and verified all tests pass successfully.
- 2026-07-01 [Antigravity Kaira]: Resolved the dependency leak in src/workers.rs by defining a generic RuntimeEvent enum and wiring up event forwarding inside App::new. src/workers.rs is now 100% decoupled from TUI surface (BgMsg). All tests passed.
- 2026-07-01 [Antigravity Kaira]: Physically partitioned monolithic codebase into a Cargo workspace with 5 crates: `raios-core` (db, config, task_graph, mempalace), `raios-runtime` (daemon, workers, swarm, search, server, filebrowser, system_scan), `raios-surface-tui` (app, ui, setup_wizard), `raios-surface-mcp` (stdio mcp server), and `raios-surface-cli` (cli implementation + `raios`/`aiosd` binaries). Configured self-alias crate packaging to allow smooth refactored imports compilation. staged checks compile clean and all 452 unit tests pass.
- 2026-07-02 [Claude Kaira]: Read-only security audit of the security kernel (`security/sandbox.rs`, `capabilities.rs`, `egress.rs`) surfaced two real bypasses, then fixed both: (1) `check_fs_capability` called `SandboxGuard::new(resolved_target)` — workspace root == the target being checked — making the filesystem-jail boundary trivially true for any path; now takes a distinct `workspace_root` param (`McpServer`'s `config.dev_ops_path`, verified via `entities::load_entities(dev_ops)` that registered projects already live there). (2) `SandboxGuard::check`'s blocklist matched the raw, unresolved target string before canonicalization, so `sub/../.secrets/key` never string-prefix-matched a blocked `.secrets` dir; blocklist now runs against the resolved/canonicalized target via component-based `Path::starts_with` (case-insensitive on Windows only). 6 regression tests added.
- 2026-07-02 [Claude Kaira]: Egress/SSRF hardening — `extract_domain` rewritten on the `url` crate (RFC 3986) instead of ad-hoc `split('/')`/`split(':')`, which truncated IPv6 literals at their first colon and never stripped `user@host` userinfo. Added `is_blocked_metadata_target`: cloud metadata IPs (`169.254.169.254`, AWS IPv6 equivalent, GCP/Azure metadata hostnames) and the full `169.254.0.0/16` link-local block are now denied unconditionally — even with `[egress] enabled = false` or an explicit allowlist entry. Deliberately excludes `127.0.0.1`/`localhost`: that's this codebase's own legitimate target (`capabilities::default_for`'s daemon-tool network capability, port 42069), not an SSRF target — blocking it would have broken `semantic_search`/`get_inbox`/etc. 8 regression tests added.
- 2026-07-02 [Claude Kaira]: Found and fixed a live, auth-gated shell-injection RCE: `proxy_store.rs`'s `Backend::Shell` did `command.replace("{input}", input)` then `sh -c`, and the WS command that reaches it (`RouteCapability`) defaults to `action = "allow"` in `raios-policy.toml` — no confirm-gate, unlike `ExecuteCapability`. AgentShield's 9-pattern denylist (Layer 1) never blocks the classic escapes (`; touch x`, `$(...)`). Fixed by tokenizing the command template once via `shlex` and substituting `{input}` into an already-tokenized argv slot, then spawning directly with `Command::new(program).args(args)` — no shell involved, so no substitution result is ever re-interpreted. Also fixed `daemon/proxy.rs::spawn_agent` (WS `Handover` → `sh -lc <client-supplied target>`, same bug class but currently latent since `Handover` defaults to `action = "confirm"` with no confirm→execute path wired up) to use an allowlist match mirroring `agent_runner::canonical_agent_identity` instead of a shell. And closed a related AgentShield blind spot: `CreateTaskGraph`'s nested `nodes[].shell_cmd` array was never fed into the Layer-1 payload scan (`daemon/handlers.rs` only checked top-level `input`/`query`/`shell_cmd`); extracted a testable `collect_scan_payload` that now includes nested node commands too. 13 regression tests added across the three fixes.
- 2026-07-02 [Claude Kaira]: `cargo audit` was never installed/run on this repo. Installed it, found 2 direct/near-direct fixes (`anyhow` 1.0.102→1.0.103 patches an unsound `downcast_mut`; `notify-rust` 4.17.0→4.18.0 bumped as a side effect) and 2 unfixable-today high-severity CVEs (`quick-xml` <0.41, DoS-only, reachable only via `notify-rust → tauri-winrt-notification` on Windows builds, which pins `quick-xml = "^0.37"` — verified no compatible version exists upstream via `cargo update --precise`). Added `.cargo/audit.toml` ignoring just those 2 advisory IDs with a dated justification (not a blanket suppression — left the 3 non-blocking "unmaintained" warnings, `number_prefix`/`paste`/`lru`, visible on purpose). `cargo audit` now exits 0. Also moved `secret_scan.rs`'s 7 regex patterns behind a `LazyLock` so they compile once instead of once per `looks_like_secret` call (was on the A2A `message/send` hot path).
- 2026-07-02 [Codex Kaira]: Added Sprint 1 Raios Trace Memory: central `tool_traces` schema + CRUD/search DB functions, `raios trace record/search/forget` CLI, exact dedup via content hash, and secret-like input refusal via existing `looks_like_secret()` with redacted refusal audit rows. Verified with `cargo test -p raios-core` and CLI smoke tests against a temp DB.
- 2026-07-02 [Codex Kaira]: Continued Raios Trace Memory with runtime recall integration: handoff creation now attaches relevant successful trace memory, `raios run` handoff delivery augments `[HANDOVER CONTEXT]` with trace recall, and post-run session reviews auto-record trace rows only when there is a failure/risk/learned decision. Added token fallback recall tests and session-review trace tests. Verified with `cargo test -p raios-runtime trace_recall`, `cargo test -p raios-core tool_trace`, `cargo test -p raios-surface-cli trace`, plus temp-DB CLI smoke showing `"trace_memory_attached": true`.
- 2026-07-02 [Codex Kaira]: Added Trace → Evolution bridge: `raios evolve from-traces [--project] [-n]` imports successful, non-redacted trace fixes as pending instinct candidates instead of auto-promoting them. Fixed an existing schema split where runtime expected `instinct_candidates.rule/promoted/expires_at` but core migration creates `suggestion/status`; `CandidateStore` now supports both schemas. Verified with `cargo test -p raios-runtime evolution`, `cargo test -p raios-surface-cli evolve`, and temp-DB CLI smoke producing one candidate from a trace row.
- 2026-07-02 [Codex Kaira]: Added Trace → MemPalace bridge export: `raios trace kg-export [query] [--project] [-n]` emits KG triple JSON (`subject`, `predicate`, `object`, `valid_from`, source trace id) for safe ingestion by MemPalace MCP `mempalace_kg_add`. Kept it export-only because R-AI-OS Rust CLI is not an MCP client and should not silently write to an external semantic store. Verified with `cargo test -p raios-surface-cli trace` and temp-DB CLI smoke producing project/agent/command/success/error/fix triples.
- 2026-07-02 [Codex Kaira]: Applied Claude review fixes for Trace Memory: split `ToolTraceQuery.project` filtering from `preferred_project` ranking so cross-project recall can prioritize the current project, removed a `needless_borrow` in session-review secret refusal, and added regression tests for redacted-row search exclusion plus preferred-project ranking. Verified with `cargo test -p raios-core tool_trace`, `cargo test -p raios-runtime trace_recall`, `cargo test -p raios-surface-cli trace`, and `cargo clippy -p raios-runtime` (only pre-existing `unexpected cfg(cortex)` warning remains).
- 2026-07-07 [Claude Kaira]: Brutal security audit of the daemon auth/token chain surfaced 7 findings, all fixed same day: (1) **CRITICAL** — `.ipc_token` carried the same secret as `.session_token` but was written with default (world-readable) permissions instead of matching its 0600 hardening, and was the file MCP/TUI/tray/VS Code actually authenticated with; removed entirely, all clients now read `.session_token` only. (2) Daemon TCP AUTH handshake used a plain `!=` compare; switched to the existing `constant_time_compare`. (3) Session tokens and the hub API key used a hand-rolled `SHA256(uuid‖pid‖time)` instead of a real CSPRNG draw; replaced with a direct `getrandom` call. (4) `hub api-key show` printed the full bearer token to stdout unconditionally; now masked by default (`--reveal` for full). (5) No `trusted_proxy` concept existed, so a same-host reverse proxy silently downgraded remote requests onto the (never-meant-to-leave-the-host) session-token auth path; added `[server.hub] trusted_proxy` + `X-Forwarded-For` handling, gated on the direct peer being loopback. (6) Windows had zero permission enforcement on token files, at write *or* read time; added `harden_file_perms` (icacls) on write and `verify_windows_only_owner_can_read` on read — the latter had to learn from real CI output that SYSTEM/Administrators legitimately remain in the ACL and must be trusted (they're already root-equivalent on Windows), only an *other* account is a real finding. (7) A stale `#[cfg]`-only `cortex` feature reference (never declared in this crate's own Cargo.toml) meant `DaemonConfig::startup_cortex_indexing` was hardcoded `false` regardless of whether real embeddings were compiled in — wired the feature through `raios-core`/`raios-runtime`/`raios-surface-cli`'s Cargo.toml properly. 5 commits, all CI-green across Linux/macOS/Windows.
- 2026-07-07 [Claude Kaira]: Test-suite coverage audit (prompted by "what's the actual test coverage") added `cargo-llvm-cov` to CI (workspace was at ~33% line coverage, previously unmeasured) and closed 4 concrete gaps found via the coverage report: a fixed-sleep flaky test in `lock_manager.rs` (same class already hit twice this session in `factory.rs`) converted to poll-with-deadline; a zero-coverage daemon AUTH handshake given a pure `check_auth_line` extraction + real-socket regression test; and baseline unit tests added for `semver.rs`/`control_plane.rs` (0% → tested) — the latter surfaced a real schema bug: `cp_detect_graph_cache_drift` queried a `task_graph_nodes.cp_task_id` column that only ever existed via `GraphStore`'s own bolted-on `ALTER TABLE`, never in the central `schema.rs` migration (dead code today, would have hit "no such column" the moment anything called it without a `GraphStore` having touched that DB file first). 546 tests green.
- 2026-07-07 [Claude Kaira]: Ran an adversarial ("prosecutor") re-review of that same day's own security fixes plus a fresh architecture/purpose pass, written up in `docs/adversarial-review-2026-07-07.md`. Investigated and resolved all 12 findings: wired the `cortex` feature properly (see above) rather than just suppressing the warning; found `daemon/server.rs`'s token-bootstrap block had 0% coverage despite being the exact file three security commits touched — extracted `bootstrap_session_token()` with a regression test asserting `.ipc_token` is never written again; built a real `axum::Router` + `tower::oneshot` harness (with a process-global HOME/XDG_CONFIG_HOME/cwd override guard, since `SessionTokenManager`/`PolicyConfig::try_load_default()` have no DI seam) covering the full HTTP auth matrix and a live-socket UMAI-deny-over-the-wire test; added pure-logic tests for `agent_runner.rs` (`canonical_agent_identity`, `strip_session_block`, `get_dir_size` — all previously 0%); consolidated `task_graph_nodes`/`swarm_tasks` schema into `schema.rs` (found `swarm_tasks` had the identical drift `task_graph_nodes` did, just not yet surfaced by a test) with a `PRAGMA table_info`-based regression test; resolved/documented all 11 `#[allow(dead_code)]` sites — found 2 more real bugs this way: `factory.rs`'s `write_lock` was constructed but never actually locked anywhere (wired around the 4 SQLite write methods; also had to switch it from `tokio::sync::Mutex` to `std::sync::Mutex` since the critical section is pure sync I/O), and `apply_antigravity_auth_metadata` was written and tested but `scan_antigravity_usage()` never called it — found the real auth file (`~/.gemini/antigravity-cli/antigravity-oauth-token`, content shape confirmed) and wired it in; and fixed README's Policy Manager example, which didn't just have one wrong field name but described a `path_glob`-based deny rule that never existed in the schema (added a doc-sync test that parses the actual README fence against `PolicyConfig`). 583 tests green, 0 clippy warnings, all CI-green.
- 2026-07-07 [Claude Kaira]: Repo-hygiene pass from a "fresh auditor" read-through: version number disagreed across three files (README/CHANGELOG said v3.0.0, `gitrepo.md` said v2.0.0-alpha, real version is v3.2.0 — set once at the crate-split refactor, never touched since, no v3.1.0 ever tagged); added a CHANGELOG v3.2.0 entry rather than reconstructing ~90 untagged commits in detail. `wiki_temp` was a dangling gitlink (mode 160000, no `.gitmodules` entry) that had been printing a harmless-but-uninvestigated git warning on every single CI checkout all session — untracked and gitignored. `graphify-out/` (74 files, 3MB of generated AST cache) was tracked in git with no `.gitignore` entry — untracked, files kept on disk, gitignored going forward. `status_report_v080.md` described `.ipc_token` as a current security feature (the exact mechanism removed above) and listed already-completed work as "Sıradaki Hedefler" — marked as a historical snapshot rather than rewritten. `atc-report.md` (untracked, recommending a Uniswap v4 DeFi hook integration — unrelated to project scope, never acted on) deleted.
- 2026-07-08 [Claude Kaira]: Documentation version bump for the VS Code extension: README still advertised v0.6.0 in three places (badge, section header, install command) while `package.json` was already at 0.8.0 and the only committed vsix was the stale `raios-0.7.0.vsix` — so the README's copy-paste install command pointed at a file that didn't exist. Compiled the extension (tsc clean), packaged `raios-0.8.0.vsix` (vsce 3.9.2, includes the post-0.8.0-bump `.session_token` consolidation from 9182c30), swapped it for the 0.7.0 vsix in git, and aligned all README references to v0.8.0. Known-but-deferred: `raios-policy.toml` `blocked_paths` still lists only Windows paths (no Linux equivalents like `~/.ssh`) — flagged during today's project review, not part of this docs pass.
- 2026-07-08 [Claude Kaira]: TUI usability pass. Fixed 4 real bugs: (1) `?`/`/help` set `AppState::HelpView` but the render arm just drew the dashboard again — the keypress looked like a no-op and the *next* keystroke was silently swallowed by the "any key closes help" handler; HelpView now renders a real full-screen help (header with version + two-column keys/commands reference + footer). (2) Tab was documented ("Tab shows full palette") to open the command palette but the render condition required a non-empty buffer, so Tab showed nothing until you typed — palette now renders whenever command mode is active. (3) The command palette popup capped at 11 visible rows with no windowing, so the cursor could walk below the visible area (already broken at the pre-existing 14 items); the list now windows around the cursor. (4) Antigravity was launch-broken end-to-end: the launcher modal offered no [A] option at all, and both `dispatch_to_agent` (tasks.rs) and `launch_agent` (helpers.rs) spawned the literal command `antigravity`, a binary that doesn't exist — the installed CLI is `agy`; added the modal entry + `a`/`A` key and mapped identity name → `agy` at both spawn sites. Also: help panel content was stale (documented only ~60% of actual bindings, listed none of the panel-specific keys) — rewritten to match the real keymap; palette gained 7 missing commands (/task /timeline /logs /audit /mempalace /ext /help); bottom bar now hints `[?] help`. Verified live in tmux (dashboard header shows v3.2.0, help view, palette windowing, launcher modal all exercised against the rebuilt binary). 159 tests green, clippy clean on touched crates (one pre-existing `items_after_test_module` warning in `raios-runtime/src/cli/preflight.rs`, untouched by this pass, left for a follow-up). Rebuilt release binary reinstalled to `~/.local/bin/raios` + `~/.cargo/bin/raios`.
- 2026-07-08 [Claude Kaira]: Fixed a real navigation trap in the Extensions panel (menu_cursor==15): Up/Down/Tab/Enter/e/Left/Right were all wired unconditionally on `menu_cursor == 15` alone, with no `right_panel_focus` gate unlike every other panel (Tasks/Search/Projects/file panels). Landing on Extensions from the menu and pressing Up/Down — the normal way to move the menu cursor — was instead silently redirected into the extension's internal cmd_cursor/cfg_cursor, so the user could never navigate away once they arrived at Extensions. Fixed by gating all Extensions-specific key arms on `right_panel_focus` (matching the established focus model), adding Extensions to the `can_focus` set so `→`/`l` actually enters it, and adding a universal `Esc` = "unfocus back to menu" arm usable from any panel (previously Esc did nothing on the Dashboard outside Extensions' own edit-cancel case). Also fixed a general discoverability gap: the footer hint bar only ever showed `[→] files` for file-backed panels (Rules/Agents/Policies/MemPalace) and a single generic "[e] edit [o] VS Code" hint for every focused panel regardless of relevance — Tasks/Search/Projects/Extensions had no on-screen indication that `→` did anything, and the focused-hint text was actively wrong for them (e.g. "VS Code" hint on the Tasks panel). Footer now shows panel-aware hints for both the unfocused (`[→] tasks`/`results`/`projects`/`extensions`) and focused states. Extended the help screen (`?`) with the missing EXTENSIONS keybinding section. Verified live in tmux against the rebuilt binary: Up from unfocused Extensions correctly moves to Scheduler; →/Esc and →/Tab/Left(at boundary) correctly focus/unfocus. 159 tests green, clippy clean.
- [2026-07-09] [Claude Kaira]: Ported TencentDB-Agent-Memory concepts: mem_nodes/mem_lineage (L0 evidence + revision archive, fixes append-only body bloat), L1 atomic facts w/ hash slugs, L2 daily scenes, L3 deterministic persona, layer-aware export, Mermaid session canvas (raios sessions --canvas).
- [2026-07-09] [Codex Kaira]: Fixed sigmap config drift — `customOutput` was an unrecognized key (silently ignored), sigmap actually defaults to writing `.github/copilot-instructions.md`. Corrected to `{"output": "SIGMAP.md", "modelContextLimit": 200000}` to match AGENT_CONSTITUTION's SigMap workflow assumptions and improve coverage grade. `SIGNATURES.md` at repo root is now confirmed orphaned (not written by any current sigmap invocation) — left in place pending a decision on whether to delete it.
- [2026-07-09] [Codex Kaira]: Completed caution-area fixes in isolated worktree: CLI security/refactor scans now disclose pattern/heuristic limitations, `session_memory` split into `mod.rs` + `transcript_io.rs` + `heuristics.rs` + `distillation.rs` with behavior preserved, and `raios usage` now reads a fresh Claude statusLine cache for remaining quota without touching plan/auth metadata.
- [2026-07-10] [Antigravity Kaira]: Executed search timing diagnostic plan (docs/superpowers/plans/2026-07-10-search-timing-diagnostic.md). Confirmed Branch A: Cortex::init() -> VectorEngine::load() HNSW rebuild dominates runtime (6.03s in release mode, 205.7s in debug mode). Pre-scoping vs post-scoping difference was due to running unoptimized debug binaries under high load. Documented findings and handed off back to Claude Kaira.
- [2026-07-10] [Antigravity Kaira]: Implemented BM25 cache wiring plan (docs/superpowers/plans/2026-07-10-bm25-cache-wiring.md). Exposed default_db_path(), added scope-aware retain logic and canonicalization to load_or_build to prevent cross-scope eviction, transactionalized writes, optimized index_file to return postings for O(1) cold-write lookup, added force parameter, and wired load_or_build into cmd_search (CLI) and tool_semantic_search (MCP). All 619 workspace tests green.
- [2026-07-10] [Codex Kaira]: Implemented `raios grep <pattern>` trigram search in isolated worktree `feat/trigram-grep`: runtime trigram schema/cache/candidate intersection/regex verification, CLI command, and MCP `grep_search`. Verification: `cargo test --workspace` 629 passed, release forced reindex 32.31s, warm grep 0.04s, set-normalized parity with `grep -rn --include="*.rs"` over `crates/` was clean.
- [2026-07-10] [Antigravity Kaira]: Implemented Cortex Daemon Residency plan (docs/superpowers/plans/2026-07-10-cortex-daemon.md). Derived Deserialize on VectorResult, created CortexWorker/CortexRequest loop with DirtyTracker, threaded cortex_tx through DaemonState, updated VectorSearch handler to serve queries via resident Cortex and return raw vector_hits, added CortexReindex handler, and wired client delegation/silent fallback into cmd_search. All 624 workspace tests green.
- [2026-07-11] [Antigravity Kaira]: Verified MCP search tools and implemented Dart/Flutter ecosystem support (docs/superpowers/plans/2026-07-11-search-tools-mcp-and-ecosystem-verification.md). Allowed grep_search/semantic_search and loopback domains (127.0.0.1/localhost) in raios-policy.toml. Added dart to INDEXED_EXTS and .fastembed_cache to SKIP_DIRS in indexer.rs. Verified MCP tool list schemas, grep_search exact/case-insensitive matching on GT-Launcher, and semantic_search on R-AI-OS. Verified Dart indexing and search on NEXUS and esp32flutter projects. All 634 tests green.
- [2026-07-11] [Claude Kaira]: Independently reverified Antigravity's 2026-07-11 handoff live (test count, diff content, policy.toml scope all matched claims) before merging feat/dart-support → master. Then found a real, previously-undocumented perf bug while live-testing myself: `tool_semantic_search` (MCP) never got the daemon-delegation treatment `raios search` (CLI) received in yesterday's cortex-daemon-residency work — every MCP semantic_search call paid a full in-process `Cortex::init()` + `index_project()` + HNSW rebuild; measured >60s per call on R-AI-OS itself, never completing within a 60s timeout. Fixed by mirroring cli/search.rs's `daemon_vector_search_opt` (TCP 127.0.0.1:42069, AUTH via `.session_token`, `VectorSearch` command, 300ms connect timeout, silent fallback) into tools_workspace.rs. Verified live post-install: first call after an aiosd restart pays a one-time ~30-60s warmup, every call after that ~1s (matches CLI's daemon-warm numbers). 635 tests green (634+1 new fallback test), binaries rebuilt/reinstalled/daemon restarted.
- [2026-07-11] [Claude Kaira]: Renamed `raios grep`/MCP `grep_search` → `raios locate`/`locate_search` end-to-end (CLI, MCP, core `trigram::grep`→`locate`, `GrepMatch`→`LocateMatch`, raios-policy.toml rule) at user request, to stop the command name colliding with "grep" as a concept. While re-verifying parity post-rename, found and fixed a real duplicate-match bug: `SKIP_DIRS` never listed "worktrees", so any repo with a Claude Code isolated-worktree checkout under `.claude/worktrees/<id>/` (GT-Launcher carries a 56MB one) got every match reported twice. Added to the shared `SKIP_DIRS` constant (all three search engines pick it up). Verified live: `raios locate` and MCP `locate_search` both return the same 25 matches with zero worktree duplicates, exact line-for-line parity with `grep -rn` across every `INDEXED_EXTS` extension. 635 tests green, clippy clean, binaries rebuilt/reinstalled. Note: first commit attempt silently under-staged (a bad pathspec in a combined `git add` aborted before staging 7 of 8 files) — caught immediately via `git show --stat` before reporting completion, fixed with a second commit; worth remembering to verify `git diff --cached --stat` before committing multi-file changes, not just check `git status` for red/green.

## Instincts
- When using rustfmt in a Rust module tree, avoid running it on mod.rs unless skip_children is set; otherwise rustfmt can traverse child modules and create unrelated format diffs, including forbidden conflict files.
- When a generated context command is part of final verification, run git status afterward because some tools update timestamp-only files and can make a clean worktree dirty again.
- When troubleshooting timing/performance regressions, always run a benchmark with a release/optimized profile, as unoptimized debug profile math operations (like building HNSW graph indices) can be orders of magnitude slower and present false-positive regressions.
