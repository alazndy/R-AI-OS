# Project Memory: R-AI-OS

## Context
- **Status**: Released — v3.7.0 tagged and published as a formal GitHub Release; repo carries a real OSS license (AGPL-3.0) for the first time.
- **Stack**: Rust 2021 Edition + Ratatui TUI + Tokio async + SQLite (rusqlite) + Axum HTTP + VS Code Extension
- **Last Milestone**: Productization pass (2026-07-21) prompted by "ürün olmaktan ne kadar uzağız" — added `LICENSE` (AGPL-3.0, explicit owner decision), `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, 3 GitHub issue templates, tagged every crate's `Cargo.toml` with the license, and cut a real `v3.7.0` GitHub Release (previously only a git tag existed, latest formal Release was stuck at v3.6.0). Preceded same-day by a full paranoid health audit (build/test/clippy/audit/security/verify-chain all green) that found and fixed a real live bug: the "JSON Backup" scheduled job had been retrying every ~60s for 3 days instead of respecting its 86400s interval (daemon `PATH` missing `~/.local/bin` + `cp_scheduled_job_revert_firing` never backed off `next_run_at` on failure) — fixed both root causes with a TDD red/green test, live-verified the job now fires successfully.
- **Repo**: https://github.com/alazndy/R-AI-OS — public, AGPL-3.0, latest Release v3.7.0

## Active Objectives
- [x] Phase 11: Tool Pinning — implemented & wired into MCP dispatch (`security/tool_pin.rs`)
- [x] Phase 12: Secret Leasing — `raios secret grant/list/revoke` fully implemented
- [x] Phase 2B: memory.md Write-Back — sidebar checkboxes now interactive; `raios task-update` CLI added
- [x] Rate Limiting — `security/rate_limiter.rs`, configurable via raios-policy.toml
- [x] Quarantine Mode — `security/quarantine.rs`, MCP dispatch integration complete
- [x] Lifecycle Worker — auto beklemede/archived/active transitions based on git activity (`daemon/lifecycle.rs`)
- [x] discover status-preserve fix — beklemede/archived no longer reset by discover
- [x] Phase 17: 4-Agent Matrix & Atomic Handoff — `raios handoff`, TUI Inbox panel, `agy` spawn fix
- [x] aiosd systemd service — enabled, RestartPreventExitStatus=SIGTERM + PATH env added
- [x] Clean up the disconnected legacy `tool_handover` MCP tool — rewired to create_handoff_workflow cp_*
- [x] UMAI — Universal Multi-agent Access & Isolation-guard (security/umai.rs), wired to WS daemon + MCP
- [x] Delta events — AgentStarted/AgentStopped/HealthDelta broadcast; TUI handlers added
- [x] Plan A: Cortex Real Embeddings & hf-hub Redirect Fix
- [x] Plan B: Autonomous Scheduler (`raios cron` CLI + background daemon worker)
- [x] Audit ledger write path — `record_tool_decision` wired into MCP + WS dispatch (was defined but never called)
- [x] `raios policy suggest`/`show`/`caps` — learns rule suggestions from audit history, shows resolved capabilities
- [x] Capability-declaration sandbox — `ToolCapabilities` on `ToolRule` + `security/capabilities.rs`, enforced pre-dispatch
- [x] Egress enforcement chokepoint — `EgressFilter` now actually checked (via declared network capabilities)
- [x] A2A protocol surface — `GET /.well-known/agent.json` + `POST /a2a` (`message/send`, `tasks/get`, `tasks/list`)
- [x] Security-kernel hardening pass — filesystem jail no-op, blocklist `..` bypass, egress/SSRF hardening, live WS shell-injection RCE, `cargo audit` clean (see 2026-07-02 entries)
- [x] wrapper-note: E2E test through the real Unix socket IPC path added (`agent_runner.rs::tests::wrapper_note_ipc`) — two projects run in parallel over real sockets, mutation-checked (fails when the run-ID ownership guard is removed). Required adding a `RAIOS_DB_PATH` env override to `raios-core::db::db_path()` so the test never touches the live `workspace.db`.
- [x] wrapper-note: version bump + CHANGELOG entry — v3.7.0, covers wrapper-note plus the other 8 previously-unlogged commits merged since v3.6.0.
- [x] **wrapper-note now works under Codex's true default invocation** (2026-07-21, see Change Log): per explicit user decision, `codex_command()` in `agent_runner.rs` now always passes `-c sandbox_workspace_write.network_access=true` to every `codex` child raios spawns (both `run_agent` and `spawn_agent_detached`), namespaced so it's inert whenever Codex's effective sandbox is `read-only` or `danger-full-access`. Live-verified with zero explicit sandbox/config flags — just the ordinary `codex "<prompt>"` invocation a real user would type — wrote `cp_wrapper_events` row `e30753a3`. Deliberate trade-off, not an oversight: this widens network egress for every shell command Codex's own agentic loop runs while wrapped by `raios run codex`, not just wrapper-note.
- [x] Scheduler retry-storm fix (2026-07-21) — `cp_scheduled_job_revert_firing` now advances `next_run_at` by `interval_secs` on spawn failure instead of leaving it due forever; `aiosd.service` PATH now includes `~/.local/bin`. TDD red/green test `scheduled_job_revert_firing_backs_off_next_run`. 733 tests green.
- [x] OSS productization pass (2026-07-21) — `LICENSE` (AGPL-3.0), `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, issue templates, per-crate `license` field, formal `v3.7.0` GitHub Release cut.
- [x] `install.sh` ExecStart drift self-heal (2026-07-21) — script now reads `aiosd.service`'s current `ExecStart`, compares to the `BIN_DIR` it's about to install into, and rewrites+`daemon-reload`s the unit if they've diverged, before restarting. Root cause was two independent PATH-order-dependent resolutions (`raios hub install`'s `which::which("aiosd")` at unit-creation time vs. `install.sh`'s `command -v raios` at every reinstall) with nothing keeping them in sync. Live-verified: ran `install.sh` again, watched it detect `~/.cargo/bin` vs `~/.local/bin` drift and correct it automatically.
- [x] Fixed a real red-CI bug (2026-07-21) found while re-checking product-readiness status, not by intentional testing: `gh run list` showed the productization-commit CI run had failed — `Test (macos-latest)` job, `security::hooks::tests::hook_exit_code_zero_is_allowed` panicked with `left: Skipped, right: Allowed`. Root cause: `hook_recursion_disabled_returns_skipped` mutates the process-global `RAIOS_HOOKS_DISABLED` env var with no synchronization; Rust's default test runner executes `#[test]`s on parallel threads sharing one process's env, so a concurrently-running test could read that var mid-mutation and get `Skipped` instead of its real expected outcome — a true race, more likely to manifest under macOS CI's thread scheduling but not actually macOS-specific. Fixed using the codebase's existing `static ENV_LOCK: Mutex<()>` convention (already used in `db_path.rs`/`agent_runner.rs`/`handlers.rs`/`auth.rs`) rather than inventing a new pattern; also fixed the recursion test to restore the original env value instead of unconditionally removing it. Verified with 8x repeated local runs at `--test-threads=16`, then live-verified the actual GitHub Actions run for this fix went fully green across all 8 jobs including `Test (macos-latest)`.
- [x] Coverage re-measured (2026-07-21) via local `cargo-llvm-cov` (installed fresh, wasn't present locally before): **39.10% workspace line coverage**, up from the 33% baseline measured 2026-07-07. Added one real regression-guard test (`product_factory/policy.rs`) asserting `FactoryPolicy::default()` and its `#[serde(default)]` deserialization both land fail-closed (all three approval gates `true`, `MaintenanceMode::Observe`) — this is the kind of default a future PR could silently flip while adding a capability. **Not attempted**: closing the full 0%-coverage backlog — audited it and found it's dominated by two very different categories that don't belong in the same fix: (1) `raios-surface-tui`'s `ui/panels/*.rs` and `app/events/*.rs` (bulk of the gap) is TUI rendering/keyboard-dispatch code, low-risk and better suited to golden-render snapshot tests (a few already exist under `ui/routes/tests::golden_render_*`) than line-coverage unit tests; (2) `raios-runtime`'s `daemon/*.rs` (scheduler.rs, lifecycle.rs, sentinel.rs, git.rs, health.rs, kernel.rs, workers.rs — all 0%) is higher-risk async daemon-loop code that needs a real test harness (sockets/tokio, like the 2026-07-07 audit built for the HTTP auth matrix), not quick unit tests — worth a dedicated follow-up session, not a rushed pass tonight. `raios-core/src/safe_io.rs` specifically was checked and rejected as an easy win: it unconditionally dials `127.0.0.1:42069` (the live daemon's real port) with no way to override that in tests, so a naive test would talk to whatever `aiosd` happens to be running on the test machine — same "no DI seam" class of problem noted in the 2026-07-07 audit for `SessionTokenManager`/`PolicyConfig::try_load_default()`.

## Technical Decisions
- **Architecture**: Modular kernel — `src/security/`, `src/kernel/`, `src/control_plane.rs`, `src/swarm/`, `src/cortex/`
- **Database**: SQLite via rusqlite (bundled). `cp_*` schema is the sole source of truth. Legacy tables are read-only cache.
- **Control Plane**: `cp_daemon_snapshot()` = single-call operational view. `cp_scheduler_list_ready()` = canonical task scheduler.
- **Security Kernel**: 4 layers — Filesystem Jail → Policy Manager → Verify Chain (hash-chained audit ledger) → Egress Filter
- **Policy**: `raios-policy.toml` is the single security config source. `confirm` = fail-closed (deny) in daemon/stdio mode.
- **Egress**: Filtered at MCP tool hook level (not OS network layer — scoped to what raios controls).
- **Hash Chain**: Tamper *detection*, not prevention — correctly classified as forensic tool.
- **IPC**: VS Code extension proxies requests to daemon (port 42071) via Bearer token — token never exposed to WebView.
- **Critical Notes**: New work must read/write through `cp_*` functions in `src/db.rs`. Never write directly to legacy tables.

## Important Links & Paths
- **Main Entry (CLI)**: `src/bin/raios.rs`
- **Daemon Entry**: `src/bin/aiosd.rs`
- **DB Layer**: `src/db.rs` — all `cp_*` functions here
- **Security**: `src/security/` — sandbox.rs, policy.rs, verify_chain.rs, egress.rs, capabilities.rs, policy_suggest.rs, secret_scan.rs
- **A2A Protocol**: `src/server/http/a2a.rs` — `/.well-known/agent.json` + `/a2a`
- **Control Plane**: `src/control_plane.rs`
- **TUI**: `src/ui/panels/` (13 modules)
- **VS Code Extension**: `vscode-extension/`
- **Policy Config**: `raios-policy.toml`
- **Repo**: `gitrepo.md`

## Current Focus
- Health worker RAM resolved (5.8GB → 1.1GB peak). Project in hardening/expansion mode.
- v3.6.0 released 2026-07-17: typed TUI control plane, Windows installer/service support, PowerShell hooks/wrappers, and cross-platform CI. GitHub `master` and the public release are published.
- v3.7.0 (2026-07-21): Product Factory phases 0-10, React Native Factory support, ANKA transcript recall, wrapper-note IPC (with a real socket E2E test and `RAIOS_DB_PATH` test isolation), wrapper launch-prompt capture/isolation, TUI numbered route tabs, and a workspace/extension security hardening pass. See CHANGELOG.md for the full breakdown. Not yet tagged/pushed — pending explicit go-ahead.
- TUI control-plane migration is active; next delivery target is a reproducible Windows 10/11 install with native CLI/TUI, daemon Scheduled Task, PowerShell hooks/wrappers, and platform config paths.

## Change Log & Agent Trail
- [2026-08-18] [Codex Kaira]: Follow-up live journal review after the notification deployment found repeatable `database is locked` errors in health-worker security activity synchronization (not just first-boot noise). First attempt kept expensive scans at concurrency three and serialized only their DB phase; live deployment proved that insufficient because a different worker can still hold SQLite's writer slot. Root cause: `sync_security_activity_findings` began a deferred transaction, read prior state, then attempted a lock upgrade; SQLite can reject that deadlock immediately without honoring busy timeout. Added a two-connection TDD regression test (red with `DatabaseBusy` while another connection held `BEGIN IMMEDIATE`, green after the fix), changed the sync transaction itself to `BEGIN IMMEDIATE`, and retained the health-only semaphore to prevent self-contention. Runtime/core tests, clippy, pre-flight, and live post-install scan verification cover the final change.
- [2026-08-18] [Codex Kaira]: Deployed merged background-activity notifications from `master` (`caab465`) to the live `raios`/`aiosd` 3.8.0 install, removed a stale orphan daemon that had forced `aiosd.service` into 1870 address-in-use restarts, and verified one systemd-owned daemon serves ports 42069/42070/42071 with authenticated health and notification routes. Installed Kaira Launcher `origin/master` (`c21af54`) into the GNOME extension directory with source-identical `extension.js`/`notifications.js`; tray restarted from canonical source and created its mode-0600 persistent notification client ID. Final dogfood found `raios hub status` falsely called the healthy systemd process orphaned because it ignored the existing port-owner resolver when no legacy PID file existed; wired start/status to that resolver and added a Linux socket-owner regression test (62 CLI tests green).
- [2026-08-11] [Claude Kaira]: PR #33 — raised the CI line-coverage floor from 47% to 50% after measuring real workspace coverage post-PR#18-32 (the full test-coverage pass across `raios-surface-cli`, `raios-surface-mcp`, and OpenCode Kaira's handoff work on `raios-core`/`raios-runtime`/`raios-surface-tui`): `cargo llvm-cov --workspace` reports 50.27% lines overall, up from 47.48% at the last raise (PR #17). Set the floor at 50 (not 50.27) for a small buffer. Updated README's coverage-floor mention to match. Remaining large 0%-covered files identified but not pursued this pass: `raios-surface-tui`'s `ui/setup/steps/action.rs` (599 lines) and `info.rs` (505 lines) — an interactive setup-wizard UI, likely low ROI same as other render-heavy TUI surfaces.
- [2026-08-11] [OpenCode Kaira]: PR #32 — raios-surface-tui reducer.rs (~4% → 99.39%): 11 tests covering the previously-untouched reduce_intent arms (SwitchRoute full navigation-state reset, PrevRoute wraparound, cursor up-clamping, panel focus left/right/toggle, command palette open/close, help toggle, refresh/quit) and reduce_event arms (agent-run status, approval requested/resolved, LogAppended category prefix, CommandFailed last_error, CommandSucceeded idempotency log, unrelated-event no-op).
- [2026-08-11] [OpenCode Kaira]: PR #30 — raios-runtime discovery.rs (0% → 94.81%): 5 tempdir tests for discover_skills/scan_dir_for_skills — directory vs SKILL.md-bearing-directory description classification, markdown suffix trimming, non-skill extension filtering, nested-subdir handling, missing-path no-op, plus a structural invariant on the 3-entry discover_agents list (read-only PATH/home lookups, no production state touched).
- [2026-08-11] [OpenCode Kaira]: PR #29 — raios-runtime kernel.rs (0% → 66.02%): 12 tests for McpTcpSession::handle, the MCP-over-TCP (port 42070) JSON-RPC 2.0 dispatcher — initialize serverInfo, initialized/ping, resources/list, tools/list, -32601 unknown-method errors, id-less-notification semantics, and the pre-fs error paths of resources/read and tools/call. FS-touching branches (raios://memory/tasks/master reads, list_projects) left untested. Claude Kaira note on merge: CI's `Test (macos-latest)` job segfaulted (SIGSEGV) on the first run with no clear culprit test in the log — passed clean on an immediate re-run with zero code changes, confirming a transient native-library crash (likely fastembed/onnxruntime, already a known source of first-run flakiness per PR #25's entry) rather than a regression from this PR's own tests.
- [2026-08-11] [OpenCode Kaira]: PR #28 — raios-runtime tasks.rs (0% → 70.55%): 16 tests for the pure personal-task markdown parser/serializer and dispatch prompt builder — checkbox prefix variants, @agent/#project tag extraction incl. the cx→codex alias and unknown-tag passthrough, empty-text rejection, serialize⇄parse round-trip, agent_label badge map, is_persisted/display/from_personal_row, build_prompt project+sentinel context, short_path. load_tasks/save_tasks left untested (route through the real shared workspace.db via open_db(), same call as SwarmStore/CandidateStore); dispatch_to_agent left untested (process/clipboard I/O).
- [2026-08-11] [OpenCode Kaira]: PR #27 — first test-coverage PR of this pass on raios-core (cli/mcp already at diminishing returns). Covered security/audit.rs (2.23% → 93.72%), the pure npm/cargo/pip audit-JSON parsers that feed raios security scans: 8 tests for severity mapping across all npm levels, the via[].title fallback, 60-char snippet truncation, cargo CVSS→severity tiers, pip fix-version extraction, invalid-JSON no-op, and unsupported-project-type passthrough. Deliberately left run_dependency_audit untested (shells out to pnpm/cargo-audit/pip-audit).
- [2026-08-11] [OpenCode Kaira]: PR #31 — raios-surface-tui app/mod.rs (5.05% → 41.05%): 11 tests via the existing App::test_instance() constructor (no threads/daemon/fs) for filtered_palette (slash-command fuzzy matching, case-insensitive cmd+description search) and App::update_search's input-validation branches (empty/overlong/secret-like query rejection, valid-query no-op when daemon disconnected), plus add_activity's 100-entry timeline cap and the case-insensitive substring project-path lookup.
- [2026-08-11] [Claude Kaira]: Handed off the coverage-scan pass to OpenCode Kaira (`raios handoff` → `raios run opencode`) after closing out `raios-surface-cli`/`raios-surface-mcp` at diminishing returns (PRs #18-26). OpenCode found real opportunities across `raios-core`/`raios-runtime`/`raios-surface-tui` (PRs #27-32) following the same discipline (pure-logic-only, no real shared-state I/O, RAIOS_DB_PATH+tempfile isolation where safe). Merging its PRs sequentially hit a mechanical snag worth noting: every PR was branched from the same pre-#27 master commit and all touch SIGMAP.md + memory.md's Change Log, so each merge immediately conflicted the next PR in the queue. Fixed per-PR via `git rebase origin/master` — SIGMAP.md resolved by just re-running `sigmap` (fully regenerated, no manual merge needed), memory.md resolved by keeping both changelog entries in date order — then force-pushed and re-verified CI before merging. Straightforward but repetitive; a future multi-PR handoff batch should either land PRs in one master sync point or expect this same rebase cycle per PR. Also: PR #31's own conflict resolution left a stray literal `<<<<<<< HEAD` marker line in memory.md that slipped through review and got squash-merged into master — caught and removed while resolving PR #32's conflict. Lesson: after resolving a conflict by hand, grep the file for `<<<<<<<`/`=======`/`>>>>>>>` before staging, don't just spot-check the visible diff region.
- [2026-08-11] [Claude Kaira]: PR #26 — moved to `tools_workspace.rs` (647 lines, 9.82% covered, `raios-surface-mcp`'s largest and lowest-covered file). Found two untested branches in already-partially-tested `extract_validation_errors_from_state_sync` (the non-`StateSync`-event rejection, and the no-filter/return-everything path) and one wholly untested private helper, `resolve_search_scope` — structurally identical to `tools.rs`'s `resolve_git_path` (already tested in PR #24) but defaults to the current working directory instead of erroring when no `path` arg is given. 5 new tests total; reused the `RAIOS_DB_PATH` + tempfile isolation pattern for the DB-fallback case. The file's remaining ~20 tools (`ask_architect`, `get_inbox`, `get_health`, `list_projects`, `semantic_search`, `portfolio_status`, etc.) are thin wiring over real shared `workspace.db`/project-registry/daemon-socket state — not independently testable without either touching production state or disproportionate scaffolding, so left as-is. This closes the `raios-surface-mcp` coverage pass at diminishing returns: `mod.rs`/`tools_dev.rs`/`tools_workspace.rs`'s remaining gaps are all real I/O/DB/network wiring, not missed pure logic. CI caught a real cross-module race from this: `tools.rs`'s `resolve_git_path_tests` and this PR's `resolve_search_scope_tests` each defined their own separate, module-local `DB_ENV_LOCK` — two independent `Mutex`es don't synchronize the single process-global `RAIOS_DB_PATH` against each other, so parallel test threads from both modules could still both set it at once; reproduced on Ubuntu and Windows CI as one test's temp DB leaking into another's `open_db()` call ("Project not found: ai-os" on a query that should have found it). Fixed by moving `DB_ENV_LOCK` to a single shared static in `mcp/mod.rs`, referenced by both modules — verified with 5 consecutive full-crate test runs. Lesson for any future `RAIOS_DB_PATH`-touching test in this crate: reuse `crate::mcp::DB_ENV_LOCK`, never declare a new module-local one.
- [2026-08-11] [Claude Kaira]: PR #25 — closed out the `raios-surface-mcp` 0%-coverage list (`resources.rs` PR #23, `tools_git.rs` PR #22, this PR's `tools_swarm.rs`; `tools_dev.rs` had no independently-testable logic per PR #24). Of `tools_swarm.rs`'s 5 tools, 4 (`create/list/approve_swarm_task`, `list/promote_evolution_candidate`) route through `SwarmStore`/`CandidateStore`'s `default_path()`, the real shared `workspace.db` with no env-var override — left untested, same call made for `resources.rs`'s session-store branches. Tested `tool_route_capability` instead: 3 tests, missing-query rejection plus exact-name routing for the full 15-entry capability list (regression guard against a renamed capability breaking silently). Deliberately only asserted exact-name matches — `EdgeRouter::route()` short-circuits to those before falling back to embedding similarity, and pinning a query to a capability via semantic score would be liable to drift depending on which embedder is active (real fastembed vs TF-IDF fallback per `AGENT_CONSTITUTION`'s embedding-mode-blindness note). Hit a real first-run flake in this sandbox: `EdgeRouter::new()` embeds all 15 descriptions via fastembed's `AllMiniLML6V2` on construction (even for exact-name-only routing), which downloads the ONNX model on first use and panicked here with "Failed to retrieve model.onnx" on a cold cache; passed reliably once cached (confirmed by running `raios-runtime`'s own pre-existing `edge.rs` tests first, which depend on the identical mechanism and already ship in this codebase's CI). The same flake then reproduced for real on Windows CI (not just locally): two of the three tests raced to construct `EdgeRouter` concurrently on separate threads within the same test binary and collided downloading the same cache file. Fixed by serializing the two EdgeRouter-touching tests behind a `Mutex` (same pattern as `DB_ENV_LOCK`/`ENV_LOCK` elsewhere in this crate) — cross-binary races aren't a concern since `cargo test` runs each crate's test binary sequentially by default, only within-binary thread parallelism was the actual hazard.
- [2026-08-11] [Claude Kaira]: PR #24 — moving to `tools_dev.rs` (288 lines, 0% covered) found it was entirely thin formatting wrappers around `raios-core` build/test/deps/env/disk checks, with no independently testable logic of its own (same shape `raios-surface-cli`'s `dev.rs` had before PR #21's status-icon/duration extraction). Tested `resolve_git_path` instead — the shared helper (defined in `tools.rs`) that every one of those tools calls first to turn a `"project"` argument into a real path, previously untested despite underpinning most MCP tool calls. 4 tests: missing-arg rejection, direct-existing-path acceptance, case-insensitive substring lookup against registered projects, not-found error path. Discovered and reused an existing `McpServer::new_for_test()` helper already used by `steer_tool_tests` (would have saved duplicating a similar constructor in PR #23's `resources.rs` tests, had it been spotted sooner — note for future coverage passes on this crate: check `mod.rs` for `new_for_test()` before writing a new one, though `resources.rs`'s custom `test_server()` was still necessary there since it needed a `Config` pointed at a temp `dev_ops_path`, which `new_for_test()` doesn't support).
- [2026-08-11] [Claude Kaira]: PR #23 — moved the coverage effort to `raios-surface-mcp` (was 15.11%, four files at 0%: `resources.rs`, `tools_dev.rs`, `tools_git.rs` [now fixed in PR #22], `tools_swarm.rs`). Started with `resources.rs` (smallest, most self-contained). Added a `test_server()` helper that constructs `McpServer` directly (bypassing `McpServer::new()`, which has global side effects — opens the shared workspace DB, loads real policy config — unsafe under parallel `cargo test`) using each security subsystem's `disabled()`/`new(None)` constructor and a `Config` pointed at an isolated temp dir; works because the struct's fields are private-to-module but `resources` is a descendant of `mcp`, so they're visible there. 9 tests cover the full 5-URI resource list, the unknown-URI/missing-uri-param error paths, and both the missing-file fallback message and real-content read for `raios://memory`/`tasks`/`master`. Deliberately left `raios://session/current`/`raios://session/recent` untested here: both route through `SessionStore::default_path()`, the real shared `workspace.db` with no env-var override hook (unlike `RAIOS_DB_PATH` used elsewhere) — testing them would mean touching production state.
- [2026-08-11] [Claude Kaira]: PR #22 — while scoping the next `raios-surface-mcp` coverage target (0%-covered files: `resources.rs`, `tools_dev.rs`, `tools_git.rs`, `tools_swarm.rs`; `mcp/tools_git.rs` chosen first, smallest and most self-contained), found a real crash bug: `tool_session_note` (the MCP tool an agent calls to leave a session note) truncated caller-supplied text with `&note[..note.len().min(500)]` — plain byte-slicing that panics with "byte index 500 is not a char boundary" whenever a multi-byte UTF-8 character straddles the 500-byte cut. `note` is arbitrary MCP tool-call input from any connected agent, so this was live and externally triggerable, not hypothetical — the exact same bug class already found and fixed in `cron.rs`'s `truncate_title` (PR history, see `SIGMAP.md`/git log). Fixed identically: extracted `truncate_note_bytes()`, which snaps the cut point down to the nearest `is_char_boundary()` instead of slicing at a fixed offset. Added 6 tests including a reproduction of the exact multi-byte collision. `cargo test --workspace --lib`: 959 passed, 0 failed (ran the full workspace, not just the touched crate, since this changes real truncation behavior).
- [2026-08-11] [Claude Kaira]: PR #21 — continued `raios-surface-cli` coverage (`dev.rs`, the crate's largest file at 694 lines, previously 0% covered). Extracted `run_status_icon`/`job_status_icon`/`format_job_duration` out of `print_ci_report` (no behavior change) — `raios ci`'s status-glyph mapping and duration formatting, previously inline and untestable without capturing stdout. Added 8 tests. `job_status_icon` is kept distinct from `run_status_icon` (not shared) because individual jobs can be "skipped", an outcome a workflow run's overall conclusion never reports. This closes out the `raios-surface-cli` pure-logic pass started in PR #18: the remaining surface across `hub.rs`/`cron.rs`/`dev.rs` and the rest of the crate is CLI wiring/process-spawning/formatting over `raios-core` reporting types, with no further independently-testable logic worth extracting at this ROI.
- [2026-08-11] [Claude Kaira]: PR #20 — continued `raios-surface-cli` coverage (`hub.rs`). Extracted the `raios-policy.toml` `api_key_hash` insert/replace logic out of `cmd_api_key_generate` into a pure `apply_api_key_hash(content, key_hash)` function (no behavior change) so its 4 branches (replace existing / insert into `[server.hub]` / create `[server.hub]` under `[server]` / write both sections fresh) could be tested without touching the filesystem. Added 5 tests, including a regression guard against double-inserting `[server.hub]` on a malformed file with a duplicate `[server]` header. `systemd_unit_content`/`mask_secret` were already covered from an earlier pass. `hub.rs`'s remaining surface (`cmd_start`/`cmd_stop`/`cmd_status`/`find_pid_by_port`/process spawning) is inherently I/O- and OS-process-heavy — left untested for now as lower ROI than pure logic extraction.
- [2026-08-11] [Claude Kaira]: PR #19 — continued closing `raios-surface-cli`'s test gap (see 2026-08-11 entry below for PR #18/`reflect.rs`). Added 11 unit tests for `cron.rs`'s `parse_interval`, the pure function converting human-readable interval strings (`30s`/`5m`/`6h`/`1d`) into seconds for `raios cron add`. Previously completely untested despite several real edge cases: per-unit conversion, whitespace trimming, unknown units, zero/negative values, non-numeric prefixes, and strings too short to contain a unit. `cargo clippy --workspace --all-targets -- -D warnings` clean.
- [2026-08-10] [Claude Kaira]: Opened PR #15 (`feature/tui-cleanup-specs` → `master`, first time this branch and the earlier tmux-steer work ever ran through the protected-branch CI gate) and fixed everything CI found. All 5 red checks were pre-existing gaps, not regressions from this session's own diff: (1) `system_scan::doctor::tests::doctor_check_notes_tmux_presence` routed through `run_doctor_check("claude", ..)`, which early-returns before the tmux check if the `claude` binary/config-dir isn't present — true on a clean CI runner, false on a dev box with Claude Code installed. Extracted `tmux_presence_note()` as a standalone function and pointed the test at it directly. (2) `daemon::proxy`'s real-tmux-spawn tests (`spawn_agent_via_tmux_*`, `steer_agent_sends_keys_into_live_session`, and all of `integration_tests`) failed on Linux/macOS CI with `tmux new-session failed ... exit: Some(1)` — a concurrent-server-bootstrap race under `cargo test`'s parallel execution (first tmux client has to spin up the server; two doing that at once can collide). Added a `tmux start-server` step to the CI `test` (non-Windows) and `coverage` jobs, which is idempotent and removes the race by ensuring a server already exists before any test touches tmux. (3) Same tests failed harder on Windows (8 failures) because tmux has no Windows implementation at all — `spawn_via_tmux` unconditionally shells out to the `tmux` binary. Gated the 3-test `integration_tests` module and the 4 other tmux-spawning tests behind `cfg(not(windows))`/`cfg_attr(windows, ignore)`, being explicit that tmux-based agent spawning is a real, currently Unix-only gap in the steer feature (not something CI should paper over) rather than trying to fake tmux support on a Windows runner. (4) `Security and extension package` failed on `pnpm audit --audit-level=high`: `@vscode/vsce` (already latest, 3.9.2) transitively pulls vulnerable `undici` (GHSA-4cwx-7wf7-3272), `fast-uri` (GHSA-7p8r-x3mc-p8w7), and `js-yaml` (GHSA-5p4m-2wfm-xmqj) via its bundled `secretlint`, plus a `brace-expansion` CVE (GHSA-rgw5-rvv9-x895) that bypasses the already-pinned mitigation for an earlier one. Learned mid-fix that pnpm v10+ silently ignores a `pnpm.overrides` block in `package.json` (only warns) — the project already had `vscode-extension/pnpm-workspace.yaml` for exactly this reason; added the 3 new overrides there alongside the existing `brace-expansion` pin (bumped 5.0.8 → 5.0.9). Verified `pnpm audit --audit-level=high` clean and that `pnpm run compile`/`pnpm run package` still produce a working `.vsix` with the overridden versions. `cargo test --workspace`/`fmt --check`/`clippy -D warnings` all clean after every fix.
- [2026-08-10] [Claude Kaira]: Filled the test gap left open by Codex Kaira's TUI cleanup migration (Task 5 of the implementation plan): added `App::test_instance()` (`app/mod.rs`) — a test-only constructor bypassing `App::new()`'s background daemon-connect thread, which is unsafe to trigger inside `cargo test`. Used it to add render-dispatch coverage (`ui/mod.rs`) for all 6 new `AppState` views via `TestBackend`, plus handler tests (Esc/q close, cursor bounds, active-agent selection clamping, modal-precedence) across `active_agents_view.rs`, `timeline_view.rs`, `search_view.rs`, `tasks_view.rs`, `constitution_view.rs`, `extensions_view.rs`. Deliberately did not test `tasks_view.rs`'s completion-toggle key: it calls `raios_runtime::tasks::save_tasks`, which opens the real shared `workspace.db` (only overridable via the process-global `RAIOS_DB_PATH` env var — unsafe under parallel `cargo test`). `cargo test -p raios-surface-tui`: 66 → 87 passed. `cargo test --workspace`: all crates green, 0 failed. `cargo fmt --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` both clean. Regenerated `SIGMAP.md`.
- [2026-08-09] [Codex Kaira]: Completed the approved TUI cleanup migration: removed the dormant 16-item `menu_cursor` dashboard surface and its legacy file/project dispatch helpers; moved Constitution, Extensions, local markdown Tasks, local Search, active-agent Logs, and Timeline into dedicated `AppState` views with isolated keyboard handlers; aliased `/memory` to the established MemPalace flow. WORK now supports visible client-side project sorting and a three-target Projects/Ocak/Tasks focus model. Ocak’s six aggregate rows prefill a reviewed `/ocak` command prefix on Enter without dispatching it. Targeted TUI tests and strict Clippy pass.
- 2026-07-21 [Claude Kaira]: Ran a full paranoid health audit end-to-end: `cargo build --workspace --all-targets` clean, `cargo test --workspace` (not just `--lib`) 732 passed/0 failed, `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean, `cargo audit` 0 CVEs (3 known unmaintained/unsound advisories, pre-existing), `raios verify-chain` 3449 entries no tampering, `raios health`/DB budget clean. Found a real live bug via `journalctl`: the "JSON Backup" scheduled job (`raios cron`) had been retrying every ~60s for 3 days — `NEXT RUN` stuck at 2026-07-18. Root-caused to two layered issues: (1) `aiosd.service`'s `PATH` was missing `~/.local/bin`, where the real `claude` binary lives, so every spawn attempt hit ENOENT; (2) `cp_scheduled_job_revert_firing` (`raios-core/src/db/scheduler.rs`) never advanced `next_run_at` on failure, so the scheduler re-claimed and re-fired the same overdue job on every tick forever regardless of its configured interval. Fixed via TDD (red test `scheduled_job_revert_firing_backs_off_next_run` first, confirmed failing for the right reason, then implementation): `revert_firing` now takes `interval_secs` and pushes `next_run_at` forward on failure, same as a successful fire. Updated both call sites in `daemon/scheduler.rs`. Full suite re-verified: 733 tests green, clippy clean. Fixed the PATH in the systemd unit, ran the project's own `install.sh`, then discovered and fixed a second real issue live: `install.sh` installed the new binaries to `~/.local/bin` (via `command -v raios` PATH resolution) while `aiosd.service`'s `ExecStart` stays hardcoded to `~/.cargo/bin/aiosd` — the two diverged (different SHA-256), so the daemon was still running a stale binary after "reinstall." Manually synced via `cp`+`mv` (avoiding the known `ETXTBSY` issue) and restarted; live-verified the JSON Backup job actually fired successfully afterward (`run_count` 5→6, `next_run_at` correctly advanced to +86400s). Committed (`47b8f52`) and pushed on explicit user approval.
- 2026-07-21 [Claude Kaira]: User asked "ürün olmaktan ne kadar uzağız" (how far from being a product) — audited productization gaps: no `LICENSE` (`licenseInfo: null` on GitHub — legally un-forkable despite being public), no `CONTRIBUTING.md`/`SECURITY.md`/`CODE_OF_CONDUCT.md`/issue templates, latest formal GitHub Release stuck at v3.6.0 despite v3.7.0 being tagged locally, README Roadmap showing Phase 27 (Product Factory) as incomplete despite it having shipped, test coverage last measured ~33% on 2026-07-07 (stale), and zero external traction (2 stars, 0 forks, repo 2.5 months old). User chose **AGPL-3.0** (explicit decision — copyleft, so anyone offering R-AI-OS as a hosted service must open-source their changes) over MIT/Apache-2.0. Fetched the canonical license text via `gh api licenses/agpl-3.0` (not hand-transcribed). Added `LICENSE`, `CONTRIBUTING.md`, `SECURITY.md` (private disclosure via GitHub Security Advisories or email, explicit in/out-of-scope list), `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), 3 issue templates (bug/feature/security-redirect), and `license = "AGPL-3.0-only"` on all 6 crates' `Cargo.toml` (verified via `cargo metadata`). Fixed the README roadmap to mark Phase 27 done and logged Phase 28 (ANKA) and Phase 29 (this productization pass) as shipped. Committed (`b954c1f`), pushed, then cut the first real `v3.7.0` GitHub Release via `gh release create` (previously only a git tag existed) with notes sourced from `CHANGELOG.md`. GitHub now correctly reports `licenseInfo.key: "agpl-3.0"`. Regenerated `SIGMAP.md` per the mandatory pre-commit rule (was stale after this session's code changes).
- 2026-07-21 [Claude Kaira]: Audited the uncommitted wrapper-note handoff (Codex Kaira's `raios wrapper-note` work — DB handoff metadata mislabeled it `from: claude_kaira`, but memory.md attribution and content confirm Codex). `cargo build`/`cargo test --workspace --lib` (271 passed, 0 failed)/`cargo clippy --workspace --all-targets` all clean. `SIGMAP.md` was stale (45 lines behind source) — regenerated via `sigmap --diff`. Gaps found and logged above as Active Objectives: no E2E test through the real socket IPC path, no version bump/CHANGELOG entry, no live Codex validation of this specific feature. No code changed, nothing committed.
- 2026-07-17 [Codex Kaira]: Control-surface TUI now renders the pending handoff/approval queue with agent direction and risk, adds project lifecycle/Git/memory status in WORK, and keeps a selected project while navigating tasks. The typed work snapshot includes a bounded read-only `memory.md` preview for the selected project; it is never written or logged by the control plane.
- 2026-06-03 [Antigravity Kaira]: Hash-Chained Audit Ledger, Redaction Engine, Sentry SDK integration — v2.0.0-alpha foundation
- 2026-06-04 [Antigravity Kaira]: Security Kernel 4 phases complete (Sandbox+Policy+Chain+Egress), 239 tests. Hybrid UI Faz 2A+2C (VS Code Sidebar WebView + TokenBridge). raios-0.4.0.vsix packaged.
- 2026-06-04 [Antigravity Kaira]: Refactor — events.rs→events/, dashboard.rs→13 panels, build.rs→10 submodules, deps.rs→10 submodules, keyboard.rs→6 submodules. False-positive risk pattern fix in refactor_scan.rs.
- 2026-06-10 [Claude Kaira]: Control Plane Migration phases 1-8 complete. cp_* is sole source of truth. Legacy tables cache-only. cp_daemon_snapshot() added. 376→378 tests green.
- 2026-06-13 [Claude Kaira]: memory.md migrated to AGENT_CONSTITUTION v5.0 template. PATH fixed (~/.cargo/bin added to .zshrc). Claude Kaira skills installed.
- 2026-06-13 [Claude Kaira]: K-AI-RA identity integrated into setup wizard. Project dedup fix (145→52). DB vacuum (88MB→308KB). Zombie aiosd fix. raios-tray built (AppIndicator3, daemon toggle, all-projects popup, agent launcher). Phase 2B write-back implemented (sidebar checkboxes + raios task-update CLI). All backlog phases confirmed complete. 378 tests green.
- 2026-06-16 [Claude Kaira]: Lifecycle worker added (auto beklemede/archived/active via git activity). DaemonConfig: lifecycle_standby_days/archive_days/interval_secs. upsert_project CASE fix (beklemede/archived preserved on discover). ZRAM 8GB + swappiness=10 system optimization. aiosd config intervals increased (CPU relief). Active project set defined in DB.
- 2026-06-18 [Codex Kaira]: Cross-platform `raios-tray` project copy added under `tools/raios-tray/` in this repo, including PySide6 tray app, startup assets, and in-tray `aiosd` settings editor.
- 2026-06-22 [Claude Kaira]: Gemini CLI removed from entire codebase (~165 lines across 28+ source files) — Google shut down Gemini CLI. Removed WizardStep::Gemini, scan_gemini_usage, agent_runner gemini case, filebrowser Gemini CLI section, cli/new.rs config block, all gemini keybindings/actions from events/, tasks.rs agent parser patterns, discovery gemini paths, db.rs gemini provider, GEMINI.md hardlinks, ui panels references. Replaced gemini fallback with claude in cli/new.rs. Updated ~20 test files. 0 warnings, clean cargo check.
- 2026-06-25 [Claude Kaira]: `raios handoff` added — atomic 4-agent handoff (claude_kaira/codex_kaira/opencode_kaira/antigravity_kaira) built on the existing control plane, not a new STATE.json. `create_handoff_workflow`/`cp_take_pending_handoff`/`cp_consume_handoff` in db.rs reuse `cp_tasks`/`cp_agent_runs`/`cp_artifacts`/`cp_approvals` (`ArtifactKind::HandoverNote`, `ApprovalType::Handover` were already defined, now wired up). `agent_runner::run_agent` delivers pending handovers to `raios run`/`raios task` spawns via `RAIOS_HANDOVER_CONTEXT` env, exactly once. Also fixed a stale, never-committed `lock_manager.rs` test (`blocks_equal_priority_second_owner`) left broken by the 2026-06-22 gemini→claude rename above — it had started asserting against itself. 379 tests green, 0 warnings. AGENT_CONSTITUTION.md Section 1 + new Section 10 updated to match (OpenCode/Antigravity as separate identities, Gemini fully dropped).
- 2026-06-25 [Claude Kaira]: Live-tested the handoff above end-to-end against the real `~/.config/raios/workspace.db` (not a scratch DB): `raios handoff --to codex-kaira` then `raios run codex` delivered `[HANDOVER CONTEXT]` and flipped approval pending→approved, task awaiting_approval→completed. Surfaced a pre-existing gap while doing so: `agent_runner::run_agent`'s spawn match had no `"codex"` arm (`raios run codex` always errored "Unsupported agent" despite Codex Kaira being a canonical identity) — added it. 379 tests still green, 0 warnings.
- 2026-06-25 [Claude Kaira]: Found that `RAIOS_HANDOVER_CONTEXT` (and the pre-existing `RAIOS_INSTINCTS`) are env vars no CLI actually reads — `claude`/`codex`/`opencode` are real upstream binaries, not raios wrapper scripts. Rewired the handoff delivery in `agent_runner.rs` to use each tool's real prompt-injection surface: `claude --append-system-prompt <block>`, `codex <block>` (positional PROMPT), `opencode --prompt <block>`; env var kept only as a best-effort fallback for `antigravity` (not installed on this box, flag unverified). Confirmed via `strace -f -e trace=execve` that the handover text lands in `codex`'s actual argv. 379 tests green, 0 warnings.
- 2026-06-25 [Codex Kaira]: Reviewed the handoff feature via a real handoff delivered to codex_kaira, fixed 2 real defects: `create_handoff_workflow` wasn't atomic (now wrapped in `conn.unchecked_transaction()`), and `run_agent` consumed a handoff even if `cmd.spawn()` failed (now consumes only after spawn succeeds). Committed everything as `bebff1b` — note this commit also swept in the long-uncommitted 2026-06-22 Gemini-removal diff (36 files) since it was sitting dirty in the tree.
- 2026-06-25 [Claude Kaira]: Researched A2A protocol / handoff best-practice articles, added 3 improvements on top of Codex's fixes: (1) `cli/handoff.rs` auto-attaches `git diff --stat HEAD` as `HandoffContext.diff_stat`, rendered to the receiving agent as a `[Changed files since handoff]` block. (2) `looks_like_secret()` heuristic-scans `--msg` for API keys/tokens/private-key blocks (AWS, Anthropic, OpenAI-style, GitHub, PEM headers, generic key=value) and refuses the handoff before it ever touches the DB or process argv. (3) `create_handoff_workflow` now supersedes any still-pending handover for the same `(to_agent, project_id)` inside the same transaction — old approval→`expired`, artifact→`superseded`, task→`cancelled` — so only one active handoff per assignee/project survives. 380 tests green (1 new), 0 warnings. Live-verified all three against the real DB, then cleaned up the test rows.
- 2026-06-25 [Claude Kaira]: `raios run agy`/`raios run antigravity` were both broken — the real Antigravity CLI binary is named `agy` (confirmed via `agy --help`), but `agent_runner.rs` only recognized the literal string `"antigravity"` as input and then tried to spawn a binary called `antigravity` (doesn't exist). Fixed: `"antigravity" | "agy"` both accepted as input, both spawn `Command::new("agy")`, both resolve to `antigravity_kaira` in `canonical_agent_identity`. Wired the real handover flag too: `agy --prompt-interactive <block>` (verified via `strace`: diff_stat + context delivered correctly). Antigravity now has full parity with claude/codex/opencode instead of only the env-var fallback. 380 tests green, 0 warnings.
- 2026-06-25 [Claude Kaira]: Audited the ratatui TUI for control-plane visibility — found none of the 13 `ui/panels/` screens render `cp_tasks`/`cp_approvals`/`cp_agent_runs` at all (only the MCP tool `get_inbox` aggregated them, invisible to a human at the terminal). Added a 14th menu item "Inbox" (`src/ui/panels/inbox.rs`, wired via `MENU_ITEMS`/`content.rs` index 13) rendering pending approvals (handovers tagged distinctly), active agent runs, and blocked tasks straight from the existing `cp_query_*` functions — no new DB plumbing needed. Also added an `agent_handoff` case to `ORIGIN_EXPR` in db.rs so handoff tasks are labeled correctly wherever task origin is shown. **Found and fixed 2 real bugs while visually verifying with `screen` (no TUI test harness existed, so launched it for real in a detached `screen` session and `hardcopy`'d the frames):** (1) `cp_list_personal_tasks`'s WHERE clause only checked `plan_id`/`parent_task_id IS NULL`, which handoff (and likely file-change-approval) tasks also satisfy — they were leaking into the personal-task checklist sidebar with checkbox UI. Fixed by excluding any task with a `cp_approvals` row attached. (2) `create_handoff_workflow`'s supersede step updated approval/artifact/task status but forgot `cp_agent_runs` — superseded handoffs left orphaned `awaiting_approval` runs that lingered forever in the Inbox's "active runs" section. Both fixed, both regression-tested (381 tests green, 0 warnings). Found a separate, disconnected legacy MCP tool `tool_handover` (`mcp/tools_workspace.rs`) that writes to `_session_notes.md` and pings a likely-dead TCP port (42069) — predates the `cp_*`-based handoff system entirely, untouched so far, flagged for later cleanup.
- 2026-06-27 [Claude Kaira]: Health worker RAM spike fixed — `scan_project_fast` added to `security/scanner.rs` skipping `cargo audit`/semgrep in background scans. Peak dropped 5.8GB → 1.1GB. 386 tests green.
- 2026-06-27 [Claude Kaira]: UMAI (Universal Multi-agent Access & Isolation-guard) added — `src/security/umai.rs`, wired as middleware in WS daemon dispatch and MCP tool dispatch. AgentStarted/AgentStopped/HealthDelta delta events added with proxy broadcast + TUI handler chain. Telemetry channel separated (telem_tx cap 64, lossy) from control channel. tool_handover legacy rewritten to use create_handoff_workflow cp_*. aiosd.service hardened (RestartPreventExitStatus=SIGTERM, PATH env). raios-policy.toml: explicit WS command rules added. 386 tests green.
- 2026-06-25 [Claude Kaira]: Version bump `2.0.0-alpha` → `3.0.0` (`raios version-bump major . --changelog --tag`) for the 4-agent handoff milestone. Replaced the auto-generated CHANGELOG entry (it dumped the *entire* git history since no prior tag existed) with a curated v3.0.0 section. README updated: version badges, ASCII banner, dropped remaining Gemini CLI mentions (`/api/usage` table, vision blurb), added a "📨 Agent Handoff" module section, `raios handoff` CLI reference row, Phase 17 roadmap entry, panel count 13→14. Git tag `v3.0.0` deliberately deleted and not yet recreated — it was created by the bump command against the pre-existing HEAD (Codex's `bebff1b`), before this round's commit existed; needs to be re-tagged against the actual 3.0.0 commit.
- 2026-06-28 [Antigravity]: Implemented background scheduler worker and `raios cron` subcommands (add, list, remove, pause, resume, run). Created `cp_scheduled_jobs` database schema with atomic claiming, crash recovery, and cron job activation controls. Added 4 unit tests. Fixed negative time interval parsing in sqlite. 390 green tests (with 1 unrelated failure). Manual integration validated. Implemented adaptive CPU throttling in Cortex search indexing (`embed_batch`), chunking requests dynamically based on available cores and `/proc/loadavg` load to prevent CPU spikes and maintain system stability.
- 2026-06-28 [Claude Kaira]: Agent Wrapper feature added — WizardStep::AgentWrapper (step 7/9) installs shell functions (claude/codex/opencode/agy → raios run <agent>) into ~/.zshrc/~/.bashrc. Zero recursion risk: Command::new() bypasses shell functions via execve. CLI: raios agent-wrapper install|remove|status. Config: agent_wrapper_enabled persisted. 393 tests green. commit 5f6a974.
- 2026-06-28 [Claude Kaira]: TUI Scheduler panel added (src/ui/panels/scheduler.rs, menu index 14) — live view of cp_scheduled_jobs with active/paused sections, interval formatting, next_run_at. VS Code extension bumped v0.8.0: cronList, cronAdd (4-step QuickPick flow), handoff (target+status QuickPick), inboxStatus commands wired. .gitignore updated (.fastembed_cache/, .context/, .tmp/). ~/.claude/settings.json expanded with comprehensive bash permissions (cargo *, git *, raios *, gh *, systemctl, find, grep, rg, etc.) to eliminate per-command auth prompts. SwarmStore confirmed already on SQLite (no migration needed). 3 commits: 3429312, c2bcd53.


- 2026-06-30 [Claude Kaira]: (1) Fixed `waiting` project status resetting hourly — 3-point protection: normalizasyon NOT IN, upsert CASE, lifecycle skip+reload filter (src/db.rs, src/entities.rs, src/daemon/lifecycle.rs). Root cause: eski aiosd binary (PID 25178) çalışırken lifecycle her saatte open_db() ile override ediyordu. (2) Fixed `raios run` agent flag forwarding — trailing_var_arg+allow_hyphen_values (src/cli/mod.rs) + extra_args param + CWD fallback (src/agent_runner.rs). Artık `claude --model opus --resume` çalışıyor. (3) Fixed Turkish auto-memory — heuristic_extract'e TR keyword'ler eklendi (src/session_memory.rs). (4) atc-report.md + atc-analysis/ global gitignore'a eklendi. 38 waiting proje korunuyor, 4 yeni GitHub repo oluşturuldu. Commits: 572a7a5, bde1475.
- 2026-06-30 [Codex Kaira]: Refactor split batch complete for `src/core/process.rs`, `src/system_scan.rs`, and `src/server/http.rs` into directory modules (`process/{command,ports,processes,kill}`, `system_scan/{tools,usage}`, `server/http/{auth,plans,routes,websocket}`) while preserving public API paths. Original flat files removed. Full `cargo test` green: 393 passed.
- 2026-07-01 [Antigravity Kaira]: Resolved all 34 compiler warnings. Cleaned up unused imports, unused mut variables, and unused private helper functions/tests in `src/daemon/server.rs`. Added `#[allow(dead_code)]` to deserialization-only fields in `src/app/events/ext_actions.rs` and `src/cli/ext/mod.rs`. Regenerated `SIGMAP.md` and `.github/copilot-instructions.md`. All 390 tests green.
- 2026-07-01 [Antigravity Kaira]: Repaletted TUI theme to orange, cyan, blue, turquoise. Updated BANNER to use strictly orange (AMBER) for the top 3 lines and blue (CYAN) for the bottom 3 lines, keeping the rest of the UI in the specified theme colors. 390 tests green.
- 2026-07-01 [Claude Kaira]: Fixed 17 clippy warnings to 0 (dead identical if/else in `app/events/ext_actions.rs`, manual `strip_prefix` in `security/umai.rs`, `mem_upsert`'s 8-arg signature grouped into a `MemUpsert` struct in `db/mem.rs`, 14 more auto-fixed via `cargo clippy --fix`). 390 tests green.
- 2026-07-01 [Claude Kaira]: Competitive-landscape research (GitHub + web + Exa search) surfaced 3 gaps vs. adjacent projects (`snulbug`, `dullfig/AgentOS`, Google A2A) and 2 dormant subsystems (`record_audit_event` never called in production; `EgressFilter` never enforced at a call site). Implemented all three as a 4-phase plan:
  - **Phase 0** (prereq): wired `record_tool_decision` (new shared helper in `security/verify_chain.rs`) into both `mcp/tools.rs::handle_tools_call` and `daemon/handlers.rs` WS dispatch — every UMAI allow/deny/confirm decision now lands in `audit_log` as `{tool, args_hash, matched_rule}` (raw args never persisted, only a SHA-256 hash). `EgressFilter` moved into the `McpServer` struct (`mcp/mod.rs`), constructed but not yet enforced — Phase 2's chokepoint.
  - **Phase 1**: `raios policy suggest [--min-count N] [--apply]` / `raios policy show` — new `security/policy_suggest.rs` (`suggest_policy_rules`, pure/testable) reads the now-populated audit ledger and proposes `[[tools.rules]]` entries. Never suggests escalating a tool's privilege — only pins down whatever `default_action` already produced; confirm-heavy tools surface as a review note, not an allow suggestion. `--apply` appends idempotently to `raios-policy.toml` (new `PolicyConfig::default_path()`).
  - **Phase 2**: Capability-declaration sandbox ("no ambient authority") — new `ToolCapabilities` field on `ToolRule` (`security/policy.rs`) + `security/capabilities.rs` static default map for all 32 built-in tools + `resolve`/`check_fs_capability`/`check_network_capability`. Enforced in `handle_tools_call` before dispatch: the 12 tools that resolve a real path via `resolve_git_path` (`PATH_RESOLVING_TOOLS`) are checked against `SandboxGuard`; any tool declaring `network` capability is checked against the (now-wired) `EgressFilter`. `exec` is declarative-only (surfaced via new `raios policy caps [tool]`) — no chokepoint exists for arbitrary `Command::new` spawns without a much larger refactor, stated honestly in code comments rather than pretending to enforce it. New `capability:` error prefix → JSON-RPC code `-32024`.
  - **Phase 3**: A2A (Agent2Agent) protocol surface — `GET /.well-known/agent.json` (public Agent Card, exempted from Bearer auth in `server/http/auth.rs`) + `POST /a2a` (new `server/http/a2a.rs`, JSON-RPC 2.0: `message/send` → reuses `create_handoff_workflow` verbatim, `tasks/get`/`tasks/list` → reuses the same `cp_query_active_tasks` read model as `GET /api/inbox`). `looks_like_secret` extracted from `cli/handoff.rs` into a shared `security/secret_scan.rs` so both the CLI and A2A delivery paths get the same secret-paste deterrent.
  - Live-verified end-to-end against the real `~/.config/raios/workspace.db` after a full `install.sh` rebuild+restart: MCP `project_info` call passed UMAI→capability→dispatch; `/a2a` `message/send` created a real handoff (cleaned up test rows after), secret-laden text was correctly refused with `-32010`, missing Bearer token correctly 401'd, `raios verify-chain` confirmed the hash chain stayed intact through all of it. 424 tests green (34 new), 0 clippy warnings.
- 2026-07-01 [Codex Kaira]: Aligned the 5-control-feature work with the approved implementation plan. Added `raios` post-run review storage/output via new `git_utils` + `session_review`, moved handoff diff-stat to the shared helper, split preflight into `CommitGate` vs `AgentRunGate`, added opt-in `[preflight].enforce_before_run` policy support with `RAIOS_SKIP_PREFLIGHT=1` escape hatch, and kept wrapper session summaries in `cp_agent_runs.summary` without overwriting old values on `None`. Regenerated SigMap output. Full verification clean: `cargo test --lib` 447 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Started the modularization/TUI refactor track with a new `docs/superpowers/plans/2026-07-01-raios-modularization-tui-plan.md` and `src/app/services.rs` service boundary. Moved `Inbox`, `Scheduler`, `Policies`, and `Projects` list rendering onto typed service snapshots, extracted extension shell/config helpers out of `app/events/ext_actions.rs`, and removed render-path `systemctl` calls by precomputing extension service status during discovery. Full verification clean: `cargo test --lib` 450 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Continued the modularization pass by moving `ProjectDetail` loading off controller inline logic into `app/services.rs`: `memory.md`, `git log`, `git diff`, and `GRAPH_REPORT.md` now load through service helpers, and `BgMsg::ProjectOpened` carries a typed `ProjectDetailData` payload instead of ad-hoc vectors. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Continued trimming controller fat in `app/events/commands.rs`: remote daemon command JSON construction (`Search`, `GetLogs`, remote `raios` job submit) and vault-note creation moved behind shared `app/services.rs` helpers, and the duplicate local `append_memo()` implementation was deleted in favor of `app/events/helpers.rs`. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Continued the TUI/runtime boundary cleanup by extracting IPC bootstrap and protocol helpers out of `app/ipc.rs` into new `app/ipc_support.rs` (`resolve_daemon_addr`, local daemon auto-spawn, auth handshake, initial state request, standardized IPC log entries). `ipc.rs` now focuses on connection lifecycle and event dispatch. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Finished the next IPC split layer by moving the large daemon event decode/forward `match` out of `app/ipc.rs` into dedicated `app/ipc_events.rs`. The connection loop is now isolated from event payload translation, which makes a later runtime/surface crate split much less tangled. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Codex Kaira]: Continued collapsing the remaining TUI monolith by splitting `app/events/bg_messages.rs` internally into focused lifecycle/sync/file-change/extension handler methods. `handle_bg_msg()` is now mostly a router, and the heavier side effects sit behind named methods instead of one 500-line match block. Full verification clean: `cargo test --lib` 451 passed, `cargo clippy --lib -- -D warnings` clean.
- 2026-07-01 [Antigravity Kaira]: Pulled out keyboard orchestration logic (launch_agent, daemon JSON messages, git commit/push threads, health refresh triggers) from keyboard event handlers into dedicated App controller methods in actions.rs. Keyboard modules are now thin routers. Cleaned up unused imports and verified all tests pass successfully.
- 2026-07-01 [Antigravity Kaira]: Resolved the dependency leak in src/workers.rs by defining a generic RuntimeEvent enum and wiring up event forwarding inside App::new. src/workers.rs is now 100% decoupled from TUI surface (BgMsg). All tests passed.
- 2026-07-01 [Antigravity Kaira]: Physically partitioned monolithic codebase into a Cargo workspace with 5 crates: `raios-core` (db, config, task_graph, mempalace), `raios-runtime` (daemon, workers, swarm, search, server, filebrowser, system_scan), `raios-surface-tui` (app, ui, setup_wizard), `raios-surface-mcp` (stdio mcp server), and `raios-surface-cli` (cli implementation + `raios`/`aiosd` binaries). Configured self-alias crate packaging to allow smooth refactored imports compilation. staged checks compile clean and all 452 unit tests pass.
- 2026-07-02 [Claude Kaira]: Read-only security audit of the security kernel (`security/sandbox.rs`, `capabilities.rs`, `egress.rs`) surfaced two real bypasses, then fixed both: (1) `check_fs_capability` called `SandboxGuard::new(resolved_target)` — workspace root == the target being checked — making the filesystem-jail boundary trivially true for any path; now takes a distinct `workspace_root` param (`McpServer`'s `config.dev_ops_path`, verified via `entities::load_entities(dev_ops)` that registered projects already live there). (2) `SandboxGuard::check`'s blocklist matched the raw, unresolved target string before canonicalization, so `sub/../.secrets/key` never string-prefix-matched a blocked `.secrets` dir; blocklist now runs against the resolved/canonicalized target via component-based `Path::starts_with` (case-insensitive on Windows only). 6 regression tests added.
- 2026-07-02 [Claude Kaira]: Egress/SSRF hardening — `extract_domain` rewritten on the `url` crate (RFC 3986) instead of ad-hoc `split('/')`/`split(':')`, which truncated IPv6 literals at their first colon and never stripped `user@host` userinfo. Added `is_blocked_metadata_target`: cloud metadata IPs (`169.254.169.254`, AWS IPv6 equivalent, GCP/Azure metadata hostnames) and the full `169.254.0.0/16` link-local block are now denied unconditionally — even with `[egress] enabled = false` or an explicit allowlist entry. Deliberately excludes `127.0.0.1`/`localhost`: that's this codebase's own legitimate target (`capabilities::default_for`'s daemon-tool network capability, port 42069), not an SSRF target — blocking it would have broken `semantic_search`/`get_inbox`/etc. 8 regression tests added.
- 2026-07-02 [Claude Kaira]: Found and fixed a live, auth-gated shell-injection RCE: `proxy_store.rs`'s `Backend::Shell` did `command.replace("{input}", input)` then `sh -c`, and the WS command that reaches it (`RouteCapability`) defaults to `action = "allow"` in `raios-policy.toml` — no confirm-gate, unlike `ExecuteCapability`. AgentShield's 9-pattern denylist (Layer 1) never blocks the classic escapes (`; touch x`, `$(...)`). Fixed by tokenizing the command template once via `shlex` and substituting `{input}` into an already-tokenized argv slot, then spawning directly with `Command::new(program).args(args)` — no shell involved, so no substitution result is ever re-interpreted. Also fixed `daemon/proxy.rs::spawn_agent` (WS `Handover` → `sh -lc <client-supplied target>`, same bug class but currently latent since `Handover` defaults to `action = "confirm"` with no confirm→execute path wired up) to use an allowlist match mirroring `agent_runner::canonical_agent_identity` instead of a shell. And closed a related AgentShield blind spot: `CreateTaskGraph`'s nested `nodes[].shell_cmd` array was never fed into the Layer-1 payload scan (`daemon/handlers.rs` only checked top-level `input`/`query`/`shell_cmd`); extracted a testable `collect_scan_payload` that now includes nested node commands too. 13 regression tests added across the three fixes.
- 2026-07-02 [Claude Kaira]: `cargo audit` was never installed/run on this repo. Installed it, found 2 direct/near-direct fixes (`anyhow` 1.0.102→1.0.103 patches an unsound `downcast_mut`; `notify-rust` 4.17.0→4.18.0 bumped as a side effect) and 2 unfixable-today high-severity CVEs (`quick-xml` <0.41, DoS-only, reachable only via `notify-rust → tauri-winrt-notification` on Windows builds, which pins `quick-xml = "^0.37"` — verified no compatible version exists upstream via `cargo update --precise`). Added `.cargo/audit.toml` ignoring just those 2 advisory IDs with a dated justification (not a blanket suppression — left the 3 non-blocking "unmaintained" warnings, `number_prefix`/`paste`/`lru`, visible on purpose). `cargo audit` now exits 0. Also moved `secret_scan.rs`'s 7 regex patterns behind a `LazyLock` so they compile once instead of once per `looks_like_secret` call (was on the A2A `message/send` hot path).
- 2026-07-02 [Codex Kaira]: Added Sprint 1 Raios Trace Memory: central `tool_traces` schema + CRUD/search DB functions, `raios trace record/search/forget` CLI, exact dedup via content hash, and secret-like input refusal via existing `looks_like_secret()` with redacted refusal audit rows. Verified with `cargo test -p raios-core` and CLI smoke tests against a temp DB.
- 2026-07-02 [Codex Kaira]: Continued Raios Trace Memory with runtime recall integration: handoff creation now attaches relevant successful trace memory, `raios run` handoff delivery augments `[HANDOVER CONTEXT]` with trace recall, and post-run session reviews auto-record trace rows only when there is a failure/risk/learned decision. Added token fallback recall tests and session-review trace tests. Verified with `cargo test -p raios-runtime trace_recall`, `cargo test -p raios-core tool_trace`, `cargo test -p raios-surface-cli trace`, plus temp-DB CLI smoke showing `"trace_memory_attached": true`.
- 2026-07-02 [Codex Kaira]: Added Trace → Evolution bridge: `raios evolve from-traces [--project] [-n]` imports successful, non-redacted trace fixes as pending instinct candidates instead of auto-promoting them. Fixed an existing schema split where runtime expected `instinct_candidates.rule/promoted/expires_at` but core migration creates `suggestion/status`; `CandidateStore` now supports both schemas. Verified with `cargo test -p raios-runtime evolution`, `cargo test -p raios-surface-cli evolve`, and temp-DB CLI smoke producing one candidate from a trace row.
- 2026-07-02 [Codex Kaira]: Added Trace → MemPalace bridge export: `raios trace kg-export [query] [--project] [-n]` emits KG triple JSON (`subject`, `predicate`, `object`, `valid_from`, source trace id) for safe ingestion by MemPalace MCP `mempalace_kg_add`. Kept it export-only because R-AI-OS Rust CLI is not an MCP client and should not silently write to an external semantic store. Verified with `cargo test -p raios-surface-cli trace` and temp-DB CLI smoke producing project/agent/command/success/error/fix triples.
- 2026-07-02 [Codex Kaira]: Applied Claude review fixes for Trace Memory: split `ToolTraceQuery.project` filtering from `preferred_project` ranking so cross-project recall can prioritize the current project, removed a `needless_borrow` in session-review secret refusal, and added regression tests for redacted-row search exclusion plus preferred-project ranking. Verified with `cargo test -p raios-core tool_trace`, `cargo test -p raios-runtime trace_recall`, `cargo test -p raios-surface-cli trace`, and `cargo clippy -p raios-runtime` (only pre-existing `unexpected cfg(cortex)` warning remains).
- 2026-07-07 [Claude Kaira]: Brutal security audit of the daemon auth/token chain surfaced 7 findings, all fixed same day: (1) **CRITICAL** — `.ipc_token` carried the same secret as `.session_token` but was written with default (world-readable) permissions instead of matching its 0600 hardening, and was the file MCP/TUI/tray/VS Code actually authenticated with; removed entirely, all clients now read `.session_token` only. (2) Daemon TCP AUTH handshake used a plain `!=` compare; switched to the existing `constant_time_compare`. (3) Session tokens and the hub API key used a hand-rolled `SHA256(uuid‖pid‖time)` instead of a real CSPRNG draw; replaced with a direct `getrandom` call. (4) `hub api-key show` printed the full bearer token to stdout unconditionally; now masked by default (`--reveal` for full). (5) No `trusted_proxy` concept existed, so a same-host reverse proxy silently downgraded remote requests onto the (never-meant-to-leave-the-host) session-token auth path; added `[server.hub] trusted_proxy` + `X-Forwarded-For` handling, gated on the direct peer being loopback. (6) Windows had zero permission enforcement on token files, at write *or* read time; added `harden_file_perms` (icacls) on write and `verify_windows_only_owner_can_read` on read — the latter had to learn from real CI output that SYSTEM/Administrators legitimately remain in the ACL and must be trusted (they're already root-equivalent on Windows), only an *other* account is a real finding. (7) A stale `#[cfg]`-only `cortex` feature reference (never declared in this crate's own Cargo.toml) meant `DaemonConfig::startup_cortex_indexing` was hardcoded `false` regardless of whether real embeddings were compiled in — wired the feature through `raios-core`/`raios-runtime`/`raios-surface-cli`'s Cargo.toml properly. 5 commits, all CI-green across Linux/macOS/Windows.
- 2026-07-07 [Claude Kaira]: Test-suite coverage audit (prompted by "what's the actual test coverage") added `cargo-llvm-cov` to CI (workspace was at ~33% line coverage, previously unmeasured) and closed 4 concrete gaps found via the coverage report: a fixed-sleep flaky test in `lock_manager.rs` (same class already hit twice this session in `factory.rs`) converted to poll-with-deadline; a zero-coverage daemon AUTH handshake given a pure `check_auth_line` extraction + real-socket regression test; and baseline unit tests added for `semver.rs`/`control_plane.rs` (0% → tested) — the latter surfaced a real schema bug: `cp_detect_graph_cache_drift` queried a `task_graph_nodes.cp_task_id` column that only ever existed via `GraphStore`'s own bolted-on `ALTER TABLE`, never in the central `schema.rs` migration (dead code today, would have hit "no such column" the moment anything called it without a `GraphStore` having touched that DB file first). 546 tests green.
- 2026-07-07 [Claude Kaira]: Ran an adversarial ("prosecutor") re-review of that same day's own security fixes plus a fresh architecture/purpose pass, written up in `docs/adversarial-review-2026-07-07.md`. Investigated and resolved all 12 findings: wired the `cortex` feature properly (see above) rather than just suppressing the warning; found `daemon/server.rs`'s token-bootstrap block had 0% coverage despite being the exact file three security commits touched — extracted `bootstrap_session_token()` with a regression test asserting `.ipc_token` is never written again; built a real `axum::Router` + `tower::oneshot` harness (with a process-global HOME/XDG_CONFIG_HOME/cwd override guard, since `SessionTokenManager`/`PolicyConfig::try_load_default()` have no DI seam) covering the full HTTP auth matrix and a live-socket UMAI-deny-over-the-wire test; added pure-logic tests for `agent_runner.rs` (`canonical_agent_identity`, `strip_session_block`, `get_dir_size` — all previously 0%); consolidated `task_graph_nodes`/`swarm_tasks` schema into `schema.rs` (found `swarm_tasks` had the identical drift `task_graph_nodes` did, just not yet surfaced by a test) with a `PRAGMA table_info`-based regression test; resolved/documented all 11 `#[allow(dead_code)]` sites — found 2 more real bugs this way: `factory.rs`'s `write_lock` was constructed but never actually locked anywhere (wired around the 4 SQLite write methods; also had to switch it from `tokio::sync::Mutex` to `std::sync::Mutex` since the critical section is pure sync I/O), and `apply_antigravity_auth_metadata` was written and tested but `scan_antigravity_usage()` never called it — found the real auth file (`~/.gemini/antigravity-cli/antigravity-oauth-token`, content shape confirmed) and wired it in; and fixed README's Policy Manager example, which didn't just have one wrong field name but described a `path_glob`-based deny rule that never existed in the schema (added a doc-sync test that parses the actual README fence against `PolicyConfig`). 583 tests green, 0 clippy warnings, all CI-green.
- 2026-07-07 [Claude Kaira]: Repo-hygiene pass from a "fresh auditor" read-through: version number disagreed across three files (README/CHANGELOG said v3.0.0, `gitrepo.md` said v2.0.0-alpha, real version is v3.2.0 — set once at the crate-split refactor, never touched since, no v3.1.0 ever tagged); added a CHANGELOG v3.2.0 entry rather than reconstructing ~90 untagged commits in detail. `wiki_temp` was a dangling gitlink (mode 160000, no `.gitmodules` entry) that had been printing a harmless-but-uninvestigated git warning on every single CI checkout all session — untracked and gitignored. `graphify-out/` (74 files, 3MB of generated AST cache) was tracked in git with no `.gitignore` entry — untracked, files kept on disk, gitignored going forward. `status_report_v080.md` described `.ipc_token` as a current security feature (the exact mechanism removed above) and listed already-completed work as "Sıradaki Hedefler" — marked as a historical snapshot rather than rewritten. `atc-report.md` (untracked, recommending a Uniswap v4 DeFi hook integration — unrelated to project scope, never acted on) deleted.
- 2026-07-08 [Claude Kaira]: Documentation version bump for the VS Code extension: README still advertised v0.6.0 in three places (badge, section header, install command) while `package.json` was already at 0.8.0 and the only committed vsix was the stale `raios-0.7.0.vsix` — so the README's copy-paste install command pointed at a file that didn't exist. Compiled the extension (tsc clean), packaged `raios-0.8.0.vsix` (vsce 3.9.2, includes the post-0.8.0-bump `.session_token` consolidation from 9182c30), swapped it for the 0.7.0 vsix in git, and aligned all README references to v0.8.0. Known-but-deferred: `raios-policy.toml` `blocked_paths` still lists only Windows paths (no Linux equivalents like `~/.ssh`) — flagged during today's project review, not part of this docs pass.
- 2026-07-08 [Claude Kaira]: TUI usability pass. Fixed 4 real bugs: (1) `?`/`/help` set `AppState::HelpView` but the render arm just drew the dashboard again — the keypress looked like a no-op and the *next* keystroke was silently swallowed by the "any key closes help" handler; HelpView now renders a real full-screen help (header with version + two-column keys/commands reference + footer). (2) Tab was documented ("Tab shows full palette") to open the command palette but the render condition required a non-empty buffer, so Tab showed nothing until you typed — palette now renders whenever command mode is active. (3) The command palette popup capped at 11 visible rows with no windowing, so the cursor could walk below the visible area (already broken at the pre-existing 14 items); the list now windows around the cursor. (4) Antigravity was launch-broken end-to-end: the launcher modal offered no [A] option at all, and both `dispatch_to_agent` (tasks.rs) and `launch_agent` (helpers.rs) spawned the literal command `antigravity`, a binary that doesn't exist — the installed CLI is `agy`; added the modal entry + `a`/`A` key and mapped identity name → `agy` at both spawn sites. Also: help panel content was stale (documented only ~60% of actual bindings, listed none of the panel-specific keys) — rewritten to match the real keymap; palette gained 7 missing commands (/task /timeline /logs /audit /mempalace /ext /help); bottom bar now hints `[?] help`. Verified live in tmux (dashboard header shows v3.2.0, help view, palette windowing, launcher modal all exercised against the rebuilt binary). 159 tests green, clippy clean on touched crates (one pre-existing `items_after_test_module` warning in `raios-runtime/src/cli/preflight.rs`, untouched by this pass, left for a follow-up). Rebuilt release binary reinstalled to `~/.local/bin/raios` + `~/.cargo/bin/raios`.
- 2026-07-08 [Claude Kaira]: Fixed a real navigation trap in the Extensions panel (menu_cursor==15): Up/Down/Tab/Enter/e/Left/Right were all wired unconditionally on `menu_cursor == 15` alone, with no `right_panel_focus` gate unlike every other panel (Tasks/Search/Projects/file panels). Landing on Extensions from the menu and pressing Up/Down — the normal way to move the menu cursor — was instead silently redirected into the extension's internal cmd_cursor/cfg_cursor, so the user could never navigate away once they arrived at Extensions. Fixed by gating all Extensions-specific key arms on `right_panel_focus` (matching the established focus model), adding Extensions to the `can_focus` set so `→`/`l` actually enters it, and adding a universal `Esc` = "unfocus back to menu" arm usable from any panel (previously Esc did nothing on the Dashboard outside Extensions' own edit-cancel case). Also fixed a general discoverability gap: the footer hint bar only ever showed `[→] files` for file-backed panels (Rules/Agents/Policies/MemPalace) and a single generic "[e] edit [o] VS Code" hint for every focused panel regardless of relevance — Tasks/Search/Projects/Extensions had no on-screen indication that `→` did anything, and the focused-hint text was actively wrong for them (e.g. "VS Code" hint on the Tasks panel). Footer now shows panel-aware hints for both the unfocused (`[→] tasks`/`results`/`projects`/`extensions`) and focused states. Extended the help screen (`?`) with the missing EXTENSIONS keybinding section. Verified live in tmux against the rebuilt binary: Up from unfocused Extensions correctly moves to Scheduler; →/Esc and →/Tab/Left(at boundary) correctly focus/unfocus. 159 tests green, clippy clean.
- [2026-07-09] [Claude Kaira]: Ported TencentDB-Agent-Memory concepts: mem_nodes/mem_lineage (L0 evidence + revision archive, fixes append-only body bloat), L1 atomic facts w/ hash slugs, L2 daily scenes, L3 deterministic persona, layer-aware export, Mermaid session canvas (raios sessions --canvas).
- [2026-07-09] [Codex Kaira]: Fixed sigmap config drift — `customOutput` was an unrecognized key (silently ignored), sigmap actually defaults to writing `.github/copilot-instructions.md`. Corrected to `{"output": "SIGMAP.md", "modelContextLimit": 200000}` to match AGENT_CONSTITUTION's SigMap workflow assumptions and improve coverage grade. `SIGNATURES.md` at repo root is now confirmed orphaned (not written by any current sigmap invocation) — left in place pending a decision on whether to delete it.
- [2026-07-09] [Codex Kaira]: Completed caution-area fixes in isolated worktree: CLI security/refactor scans now disclose pattern/heuristic limitations, `session_memory` split into `mod.rs` + `transcript_io.rs` + `heuristics.rs` + `distillation.rs` with behavior preserved, and `raios usage` now reads a fresh Claude statusLine cache for remaining quota without touching plan/auth metadata.
- [2026-07-10] [Antigravity Kaira]: Executed search timing diagnostic plan (docs/superpowers/plans/2026-07-10-search-timing-diagnostic.md). Confirmed Branch A: Cortex::init() -> VectorEngine::load() HNSW rebuild dominates runtime (6.03s in release mode, 205.7s in debug mode). Pre-scoping vs post-scoping difference was due to running unoptimized debug binaries under high load. Documented findings and handed off back to Claude Kaira.
- [2026-07-10] [Antigravity Kaira]: Implemented BM25 cache wiring plan (docs/superpowers/plans/2026-07-10-bm25-cache-wiring.md). Exposed default_db_path(), added scope-aware retain logic and canonicalization to load_or_build to prevent cross-scope eviction, transactionalized writes, optimized index_file to return postings for O(1) cold-write lookup, added force parameter, and wired load_or_build into cmd_search (CLI) and tool_semantic_search (MCP). All 619 workspace tests green.
- [2026-07-10] [Codex Kaira]: Implemented `raios grep <pattern>` trigram search in isolated worktree `feat/trigram-grep`: runtime trigram schema/cache/candidate intersection/regex verification, CLI command, and MCP `grep_search`. Verification: `cargo test --workspace` 629 passed, release forced reindex 32.31s, warm grep 0.04s, set-normalized parity with `grep -rn --include="*.rs"` over `crates/` was clean.
- [2026-07-10] [Antigravity Kaira]: Implemented Cortex Daemon Residency plan (docs/superpowers/plans/2026-07-10-cortex-daemon.md). Derived Deserialize on VectorResult, created CortexWorker/CortexRequest loop with DirtyTracker, threaded cortex_tx through DaemonState, updated VectorSearch handler to serve queries via resident Cortex and return raw vector_hits, added CortexReindex handler, and wired client delegation/silent fallback into cmd_search. All 624 workspace tests green.
- [2026-07-11] [Antigravity Kaira]: Verified MCP search tools and implemented Dart/Flutter ecosystem support (docs/superpowers/plans/2026-07-11-search-tools-mcp-and-ecosystem-verification.md). Allowed grep_search/semantic_search and loopback domains (127.0.0.1/localhost) in raios-policy.toml. Added dart to INDEXED_EXTS and .fastembed_cache to SKIP_DIRS in indexer.rs. Verified MCP tool list schemas, grep_search exact/case-insensitive matching on GT-Launcher, and semantic_search on R-AI-OS. Verified Dart indexing and search on NEXUS and esp32flutter projects. All 634 tests green.
- [2026-07-11] [Claude Kaira]: Independently reverified Antigravity's 2026-07-11 handoff live (test count, diff content, policy.toml scope all matched claims) before merging feat/dart-support → master. Then found a real, previously-undocumented perf bug while live-testing myself: `tool_semantic_search` (MCP) never got the daemon-delegation treatment `raios search` (CLI) received in yesterday's cortex-daemon-residency work — every MCP semantic_search call paid a full in-process `Cortex::init()` + `index_project()` + HNSW rebuild; measured >60s per call on R-AI-OS itself, never completing within a 60s timeout. Fixed by mirroring cli/search.rs's `daemon_vector_search_opt` (TCP 127.0.0.1:42069, AUTH via `.session_token`, `VectorSearch` command, 300ms connect timeout, silent fallback) into tools_workspace.rs. Verified live post-install: first call after an aiosd restart pays a one-time ~30-60s warmup, every call after that ~1s (matches CLI's daemon-warm numbers). 635 tests green (634+1 new fallback test), binaries rebuilt/reinstalled/daemon restarted.
- [2026-07-11] [Claude Kaira]: Renamed `raios grep`/MCP `grep_search` → `raios locate`/`locate_search` end-to-end (CLI, MCP, core `trigram::grep`→`locate`, `GrepMatch`→`LocateMatch`, raios-policy.toml rule) at user request, to stop the command name colliding with "grep" as a concept. While re-verifying parity post-rename, found and fixed a real duplicate-match bug: `SKIP_DIRS` never listed "worktrees", so any repo with a Claude Code isolated-worktree checkout under `.claude/worktrees/<id>/` (GT-Launcher carries a 56MB one) got every match reported twice. Added to the shared `SKIP_DIRS` constant (all three search engines pick it up). Verified live: `raios locate` and MCP `locate_search` both return the same 25 matches with zero worktree duplicates, exact line-for-line parity with `grep -rn` across every `INDEXED_EXTS` extension. 635 tests green, clippy clean, binaries rebuilt/reinstalled. Note: first commit attempt silently under-staged (a bad pathspec in a combined `git add` aborted before staging 7 of 8 files) — caught immediately via `git show --stat` before reporting completion, fixed with a second commit; worth remembering to verify `git diff --cached --stat` before committing multi-file changes, not just check `git status` for red/green.
- [2026-07-15] [Codex Kaira]: Completed structured `HandoffReport` support for `raios handoff --report <json>` while preserving legacy `--msg`. Report data now round-trips through handoff metadata, Inbox, MCP and delivery context; secret scanning covers every free-text field. Replaced the growing workflow argument list with `HandoffWorkflowInput` to satisfy clippy without suppressions. Verified 650 workspace tests, targeted `-D warnings` clippy, and isolated CLI smoke tests; aiosd restarted.
- [2026-07-15] [Antigravity Kaira]: Implemented Jcode roadmap Tasks 4-7: (4) policy promotion from approval streak (record_approval_decision); (5) Policy Manager scriptable lifecycle hooks ([hooks] TOML schema, security/hooks.rs, pre_tool_call hook validation, exit code 0/2 contract, RAIOS_HOOKS_DISABLED=1 recursion guard); (6) automated verify_gate before handoff SUCCESS (insert_verify_gate_node, node_kind column on cp_task_graph_nodes, create_handoff_workflow gate validation); (7) raios doctor <agent> tiered health check (DoctorTier Offline/Auth/Full, stale creds flag >24h, agent_doctor_runs schema, CLI + MCP tool parity). All 221 tests green, 0 clippy warnings.
- [2026-07-15] [Antigravity Kaira]: Implemented R-AI-OS TUI Rearchitecture as an incremental control-plane migration across 6 phases. Created standalone serialization DTO crate `raios-contracts` (`Query`, `Command`, `Event`, `Problem`, `SnapshotEnvelope`), added projection snapshots (`NowSnapshot`, `WorkSnapshot`, `ExploreSnapshot`, `GovernSnapshot`) and IPC command dispatcher (`dispatch_control_command` with idempotency caching in `cp_idempotency` table & audit log recording). Rearchitected `raios-surface-tui` into typed `route`, `store`, `intent`, `reducer`, `client` modules and replaced legacy panel indices with 4 typed routes (`Now`, `Work`, `Explore`, `Govern`). Added contracts serde tests, control-plane idempotency/snapshot tests, reducer tests, and Ratatui `TestBackend` golden render tests for all 4 routes. Verified full workspace test suite (670 passed, 0 failed) and updated `SIGMAP.md`.
- [2026-07-15] [Codex Kaira]: Recovered the incomplete TUI control-plane migration. Replaced the visible 16-item legacy menu with the typed four-route navigation and consumed dashboard control keys before legacy handlers. Dashboard entry now requests a coherent system snapshot; Store records daemon connectivity and typed command results. Fixed command handling so authenticated transport actors, policy denies, payload-bound idempotency, transactional audit entries, exact approval-id handoff state transitions, and explicit not-found/unsupported errors are enforced. Removed AGY's deadlocking, invalid TCP test (it waited on accept before connecting and did not use its test DB); added an isolated workflow → snapshot → approve → audit-chain integration test. `cargo test --workspace -q`: 672 passed, 0 failed, 2 ignored. Remaining architectural gap: authentication does not yet carry a per-user principal or approval-owner field, so true ownership authorization cannot be claimed until the auth contract and schema add them.
- [2026-07-17] [Codex Kaira]: Continued the TUI migration and Windows portability work. Added native Windows Scheduled Task installation to `raios hub install`, `netstat.exe` PID lookup, PowerShell lifecycle hooks and agent wrappers, a portable `install-system.ps1` that builds/installs/configures `raios.exe` + `aiosd.exe`, and fixed Windows tray startup path assumptions. Updated Windows installation docs and CI to build/test the locked workspace on all OSes. Cleaned clippy blockers in migration tests and indexer/preflight; verification: sandbox-external `cargo test --workspace` passed 678 tests with 2 ignored, workspace clippy `-D warnings` passed. Cross-target check is pending a MinGW linker (`x86_64-w64-mingw32-gcc`) on this Linux host; GitHub Windows CI remains the authoritative Windows build.
- [2026-07-17] [Codex Kaira]: Fixed the typed WORK view when an older live `aiosd` omits the newly added memory fields. On snapshot ingestion, the local TUI now enriches only workspace-contained project paths with a bounded `memory.md` preview; canonical path checks reject remote or traversal-controlled paths. Verification: 15 `raios-surface-tui` tests, including the workspace-boundary fallback test, and targeted clippy passed. Active agent processes kept the orphaned daemon from being restarted.
- [2026-07-18] [Codex Kaira]: Started Product Factory Phase 0/1 under the approved skeleton-only boundary. Added disabled-by-default Factory configuration, typed lifecycle domain and runtime adapter interfaces, a compatibility `JobExecutor` alias for the legacy async queue, central idempotent Product Factory schema tables, isolated Factory contracts, schema/serde coverage, and readiness/rollback documentation. No lifecycle business logic, cache migration, artifact write, external integration, or store action was added. Verification: 440 `raios-core` tests, 5 `raios-contracts` tests, runtime check, TUI check, and targeted clippy passed.
- [2026-07-18] [Codex Kaira]: Completed Product Factory Phase 2 read-only overview. Added canonical `cp_factory_*` aggregate queries, an additive `WorkSnapshot.factory` contract with legacy-daemon serde defaults, and a compact Product Factory panel inside the existing WORK route. The panel reports enabled state, product/cycle/change/support counts, and latest product; it has no mutation, planner, external integration, or store submission path. Verification: targeted core (2), contracts (5), runtime control-plane (7), TUI route (4) tests and all four targeted clippy checks passed.
- [2026-07-18] [Codex Kaira]: Completed Product Factory Phase 3 local discovery mutation boundary. Added owner-bound, payload-idempotent and audit-logged commands for workspace creation, product drafts, guided intake sessions and answers, plus immutable Charter drafts. Factory mutation remains disabled by default and remote actors are rejected; no public transport, planner/executor, integration, store action, or artifact migration was enabled. Short draft content stays in the durable control plane behind bounded validation until the reviewed artifact store phase.
- [2026-07-18] [Codex Kaira]: Added Product Factory Phase 4 local-TUI transport. The command palette can serialize the isolated Factory contract over the existing daemon IPC with `/factory workspace`, `product`, `intake`, `answer`, and `charter`; the daemon dispatches it only for a loopback actor and only when trusted configuration enables Factory. Remote sessions, disabled configuration, ownership violations, and secret-like content remain server-side rejections. No HTTP Factory write endpoint was introduced.
- [2026-07-18] [Codex Kaira]: Completed Product Factory Phase 5 discovery baseline. New and resumed open intake sessions now contain exactly five stable, required prompts: problem statement, target user, core outcome, first platform, and success metric. Prompt rows are idempotently seeded with a versioned builtin reference; answer values remain separate mutable user data. The WORK route exposes this baseline so the local TUI has a clear intake path before Charter drafting.
- [2026-07-18] [Codex Kaira]: Completed Product Factory Phase 6 Charter readiness gate. Runtime Charter creation now queries the current open intake session and rejects the command with the exact missing prompt keys until all required answers are non-empty. The gate is server-side and transactional; it is not a TUI-only hint. Core tests cover missing-key calculation and runtime tests cover rejected-then-accepted Charter creation.
- [2026-07-18] [Codex Kaira]: Completed Product Factory Phase 7 deterministic Charter proposal. `GenerateCharterDraft` composes a structured Markdown Charter from the canonical five intake answers and the stored product title, then persists it as a normal immutable revision through the existing owner/idempotency/audit boundary. The manual Charter command remains the explicit user-override route; no model call or external content source is used.
- [2026-07-18] [Codex Kaira]: Completed Product Factory Phase 8 Charter-backed requirement drafts. A new local command creates a stable-key requirement, immutable revision content, and an explicit `derived_from` link to the product’s current Charter revision in one transaction. Creation is refused without an owned Charter. Requirement content gets the same bounded and secret-screened runtime validation as other Factory text.
- [2026-07-18] [Codex Kaira]: Completed Product Factory Phase 9 local change-control and planning slice. Owner-bound change requests deterministically record affected requirement/Charter targets as stale, require explicit assessment acceptance, and can then create an immutable next requirement revision. Owner-created plans require a separate approval before one idempotent lifecycle cycle with seven pending stages is materialized. This slice deliberately does not run stages, launch agents, create evidence, publish releases, or expose public transport.
- [2026-07-18] [Codex Kaira]: Added Product Factory Phase 10 controlled stage-task-graph materialization. An owned cycle stage can produce one canonical task graph with a non-executable node, durable run contract, and pending owner approval. The stage keeps its graph reference; no agent or shell execution is started by materialization.
- [2026-07-19] [Codex Kaira]: Added evidence-gated stage completion and verified-release readiness. An approved stage becomes active only after owner approval; it cannot complete without a durable stage evidence link. Release draft creation now additionally requires a completed verify stage and evidence-backed required quality checks.
- [2026-07-19] [Codex Kaira]: Hardened Factory release readiness against unresolved change decisions. The owner-visible readiness payload now reports pending impact assessments; a release cannot be drafted until every ready assessment has an explicit accept/reject decision. Accepted and rejected assessments remain auditable history rather than permanent blockers. Core, runtime, and TUI contract tests pass.
- [2026-07-19] [Codex Kaira]: Hardened evidence persistence at the repository boundary. Stage evidence and quality-check evidence now reject empty or whitespace-only references before any row is written, matching the runtime validation and preventing direct callers from creating unusable release evidence.
- [2026-07-19] [Codex Kaira]: Added first-class React Native recognition to the general build detector and a typed, read-only Factory capability inspector. Expo/React Native manifests now classify before generic Node; the inspector reports managed/prebuild/bare shape, native roots, package manager, TypeScript, EAS configuration, local Android/macOS capability, and deliberately unassessed signing state. No command, credential, build, or store action is launched by inspection.
- [2026-07-19] [Codex Kaira]: Added owner-bound Factory cycle controls. Paused cycles block stage activation and completion; resume preserves the correct planned/active state; cancellation is terminal and marks only pending/active stages cancelled while retaining completed evidence, task graphs, approvals, and run history. The implementation introduces no executor or external side effect.
- [2026-07-19] [Codex Kaira]: Added dependency-aware evidence staleness. Owner-bound stage evidence can explicitly verify a requirement; applying an accepted requirement revision marks only those linked evidence records stale. Release readiness reports stale evidence separately and blocks release drafts until replacement evidence is current. Empty evidence and unrelated records remain unaffected.
- [2026-07-19] [Codex Kaira]: Added the Factory content-addressed artifact baseline. Runtime storage writes SHA-256-addressed bytes outside SQLite with a size limit, atomic writes, and secret-like text rejection. A Factory stage can link only a canonical artifact belonging to its exact task-graph node, preserving the digest reference in evidence while keeping artifact bodies out of the control-plane database.
- [2026-07-19] [Codex Kaira]: Completed a local Factory security/reliability review. Full core/contracts/runtime/TUI suites passed (449/5/223/16); Rust dependencies have no known CVEs; the Expo pilot local verification passed with no high/critical audit result. Recorded the remaining 10 moderate Expo-chain advisories, four non-Factory pattern-scan findings, the shared DB budget overage, and the external device/store gates without claiming them as complete.
- [2026-07-19] [Codex Kaira]: Added an idempotent owner-bound React Native closed-testing quality profile. It seeds required checks for TypeScript, Expo configuration, web export, high/critical dependency audit, Android device/closed-testing evidence, and iOS/TestFlight evidence. The profile records policy only; passing evidence remains explicit and no device, EAS, or store operation is invoked.
- [2026-07-19] [Codex Kaira]: Added Product Factory MCP access for agents. `factory_overview` is read-only; `factory_execute` accepts only the typed `FactoryCommand` envelope with a mandatory idempotency key and dispatches it through the same local owner-bound, audit-logged, secret-screened Factory service as the TUI. The MCP policy boundary permits drafts, intake, analysis, plan materialization, evidence/quality and support work, while blocking plan/release approvals, requirement application, stage activation/completion, and cycle cancellation for explicit human action in the Factory UI. No shell execution, external integration, credential access, or HTTP Factory write route was added. Verification: `cargo test -p raios-surface-mcp --lib` (4 passed).
- [2026-07-19] [Codex Kaira]: Added explicit Product Factory operating modes. `quick` reduces discovery to three required prompts (problem, core outcome, success metric) and produces a compact Charter without empty governed-only sections; `governed` remains the default and keeps the five-question intake. Mode changes are owner-bound, idempotent, audited, validated through MCP/TUI, and do not relax any human approval, stage, release, or external-distribution control. Verification: Product Factory runtime tests (9), MCP tests (4), and TUI check passed.
- [2026-07-19] [Antigravity Kaira]: Executed authorized Product Factory roadmap: (1) Workspace tests green (228 passed), 0 clippy warnings, verified MCP manifest pin (`2354203e...`) and audit chain integrity (3448 entries). (2) Completed ScaffoldProject: explicit human-only TUI action (blocked for MCP agents), rejected pre-existing non-empty target directory, mapped first_platform to allowed categories (`mobile`, `web`, `embedded`, `ai`, `tools`), persisted product project_path, wrote Factory product_id/mode/path into `.raios.yaml`, and verified idempotency/ownership. (3) Added verified passive adapters for Flutter, React Native/Expo, Web, and Rust with zero auto-downloads or external store publish calls. (4) Exposed mode, project path, stack, scaffold state, quality blockers, and release blockers in TUI Product Factory detail view (`work.rs`). (5) Idempotently linked local build/test/device evidence to Factory quality profiles and stale-impact views. (6) Governed release artifact preparation (signed-build readiness, closed-testing draft, notes, rollback reference) retaining mandatory human approval (`ApproveClosedTestingRelease`). (7) Rebuilt and reinstalled `raios` release binary to `~/.cargo/bin`. Proposed retention/archival for 2.8GB `workspace.db`. No unrequested commits or deletions performed.
- [2026-07-21] [Codex Kaira]: Integrated ANKA (Agent Narrative Knowledge Archive) into the main worktree without resetting existing Factory/Control Plane changes. ANKA uses a separate owner-only, redacted, rebuildable transcript cache; CLI supports `status/index/search/blame/forget` and MCP exposes read-only `anka_recall` as untrusted historical evidence. Verified main-source CLI/MCP compilation plus isolated Codex-history smoke indexing (879 records), empty-query behavior, and cache mode `0600`. ANKA does not write to `workspace.db`, auto-inject context, synchronize, share, or promote records to curated memory.
- [2026-07-21] [Codex Kaira]: Added local Product Factory CLI commands: `raios factory overview` returns the canonical read-only Factory snapshot, and `raios factory execute --file <command.json>` accepts one bounded regular-file `FactoryCommand` envelope. The CLI rejects malformed input and human-only approval, requirement-apply, cancellation, stage activation/completion, and release-approval commands before opening the mutation path; allowed commands reuse the local-owner, feature-gated, idempotent, audit-logged dispatcher. Verification: CLI tests (14 passed), live `factory overview --json` smoke, dependency scan clean; full clippy remains blocked by an unrelated existing ANKA lint.
- [2026-07-21] [Antigravity Kaira]: Executed approved remediation plan phases 0-6 in order: (0) Fixed clippy single-char `insert_str` lint in `anka.rs` and resolved security findings (`db_budget` fixed query match, VSCode CommandBridge HTTPS/loopback URL validation, sidebar DOM/textContent refactor). (1) Validated UMAI allow/deny/confirm/AgentShield/capability/egress and audit hash behavior (143 security tests green). (2) Completed `workspace.db` maintenance under explicit user approval: backed up to `workspace.db.bak.20260721` (2.9GB), purged rebuildable trigram/BM25 indexes and VACUUMed DB down to 66MB (~98% reduction), verified post-VACUUM integrity and 100% data preservation of `cp_*`/`mem_items`/`audit_log`. (3) Indexed 1,232 transcript records across 84 sources into local owner-only ANKA cache under approved harness scope (Claude, Codex, OpenCode, Antigravity) with secret redactions enabled. (4) Full workspace validation: 680+ workspace unit tests passed (0 failed), `cargo clippy` clean (`-D warnings`), `cargo audit` clean. `SIGMAP.md` architecture map updated.

- [2026-07-21] [Codex Kaira]: Fixed Agent Wrapper lifecycle handling. Informational `--help`/`--version` calls no longer claim or consume pending handoffs, and periodic memory-sync shutdown now checks once per second instead of delaying session finalization for up to 90 seconds. Verified targeted runtime tests, clippy, security/dependency scans, and a live installed-release `raios run codex -- --version` smoke: the wrapper session closed as `succeeded` in one second.

- [2026-07-21] [Codex Kaira]: Validated Agent Wrapper memory readers with fixture tests for Claude, Codex, AGY, and OpenCode plus the full raios-runtime suite (235 passed), clippy, security, and dependency scans. Claude is project-scoped and AGY is workspace-scoped. Codex is time-scoped only and OpenCode uses file mtime plus the last 60 global prompts; neither source format currently provides project isolation, so their extracted facts can cross-contaminate concurrent projects. Missing or stale transcript files correctly yield no extraction. Do not treat `RAIOS_INSTINCTS` env injection as guaranteed upstream-agent context delivery.

- [2026-07-21] [Codex Kaira]: Started the Agent Wrapper memory-isolation hardening plan. Added a typed transcript-scope contract: only Claude project-bound and AGY workspace-bound histories may automatically create project facts; Codex and OpenCode global histories are intentionally skipped until their upstream formats provide trustworthy project/session identity. AGY records now require an exact workspace field. Fixture tests cover all readers and the scope boundary; live Codex smoke confirmed `mem_items` stayed 132→132 while sync reported the safe skip.

- [2026-07-21] [Codex Kaira]: Continued Wrapper memory isolation with a narrowly scoped Codex/OpenCode fallback. Automatic import from their global histories remains disabled. After a successful wrapper run, only one explicit bounded positional launch prompt can be persisted through the existing L0→L3 lineage path, bound to the local project and `cp_agent_runs` ID; flags, multi-argument calls, oversized input, and secret-like text are rejected. Interactive follow-up turns remain intentionally uncaptured pending trustworthy upstream identity.
- [2026-07-21] [Codex Kaira]: Added project-bound interactive wrapper notes. `raios wrapper-note` receives an opaque child run ID, accepts only an active matching wrapper run, rejects cross-project, late, oversized, and secret-like content, records a `cp_wrapper_events` audit row, then feeds accepted text into the existing L0→L3 memory path.
- [2026-07-21] [Claude Kaira]: Closed the wrapper-note gaps logged in the prior audit. (1) Added a real Unix-socket E2E test (`agent_runner.rs::tests::wrapper_note_ipc_two_projects_parallel_no_cross_leak`) covering two projects running in parallel with no cross-leak; required adding a `RAIOS_DB_PATH` env override to `raios-core::db::db_path()` (TDD: red confirmed, green confirmed) so the test never touches the live `workspace.db`. Verified the test is discriminating by temporarily removing the socket's run-ID guard and confirming the exact expected failure, then reverting. (2) Bumped all 6 crates to v3.7.0 and wrote a full CHANGELOG.md entry covering wrapper-note plus the 8 other previously-unlogged commits merged since v3.6.0 (Product Factory phases 0-10, ANKA, wrapper launch-prompt capture/isolation, TUI numbered route tabs, workspace/extension security hardening). `cargo test --workspace --lib`: 731 passed, 0 failed. `cargo clippy --workspace --all-targets --all-features -- -D warnings`: clean. `cargo audit`: no vulnerabilities (3 pre-existing unmaintained/unsound advisories only). (3) Rebuilt and installed the v3.7.0 `raios` binary (via `mv`+`cp` to avoid `ETXTBSY` against the several `raios run` sessions already live on this machine), then live-validated `raios wrapper-note` from a real `codex exec` child. **Found a real bug**: under Codex's actual `workspace-write` sandbox (even with `--add-dir ~/.config/raios`), the socket connect fails with `Operation not permitted` — Landlock blocks the Unix-socket connect regardless of directory allow-listing, and `cmd_wrapper_note` has no fallback for a set-but-unreachable socket. Confirmed the diagnosis by rerunning with `--sandbox danger-full-access`, which succeeded and wrote a real row to `cp_wrapper_events` (`a35581c0`, left in place as evidence).
- [2026-07-21] [Claude Kaira]: Per explicit user decision, switched `WrapperNoteIpc`'s transport from a Unix domain socket to loopback TCP (`TcpListener::bind(("127.0.0.1", 0))`, `agent_runner.rs`; client side in `cli/session.rs` swapped `UnixStream`→`TcpStream`) — the opaque `run_id` UUID remains the actual access-control boundary, the transport change only affects reachability from inside a sandbox. Updated the E2E test to connect over TCP, re-ran the same mutation check (guard removed → test fails for the expected reason, reverted → green), full `cargo test --workspace --lib` (731 passed) and `cargo clippy --workspace --all-targets --all-features -D warnings` (clean) both still pass, rebuilt and reinstalled the binary again. **Live re-test showed the fix is real but incomplete**: TCP loopback still failed identically under Codex's actual default sandbox (`Operation not permitted`, tested both with no explicit `--sandbox` flag and with `--sandbox workspace-write`, in this exact already-`trust_level=trusted` project) — it only succeeded once `-c sandbox_workspace_write.network_access=true` was passed explicitly (row `a031b72e`). Logged the refined finding and both remaining options (raios injects `network_access=true` for codex children vs. document the config prerequisite) as an Active Objective, then asked the user to choose rather than deciding a security-posture trade-off alone.
- [2026-07-21] [Claude Kaira]: Per the user's explicit choice, implemented the injection option. Extracted the one place a `codex` child is built into `codex_command()` (`agent_runner.rs`) so the `-c sandbox_workspace_write.network_access=true` override can't be added at one of the two call sites (`run_agent`, `spawn_agent_detached`) and forgotten at the other; documented on both the constant and the function why this is a deliberate egress-widening trade-off, not an incidental default. Added `codex_command_always_enables_workspace_write_network_access` (asserts on `Command::get_program()`/`get_args()` without spawning a process) — full `cargo test --workspace --lib` (raios-runtime now 239) and `clippy --all-targets --all-features -D warnings` both clean. Rebuilt/reinstalled the binary again and re-ran the live check with **zero explicit sandbox or `-c` flags** — the exact form a real user types — confirming `raios wrapper-note` now succeeds out of the box (`cp_wrapper_events` row `e30753a3`). Active Objective above marked done.

- [2026-07-24] [Codex Kaira]: Closed the release-readiness implementation gap for v3.7.1. Added a pinned Rust toolchain, format/Clippy/security/locked-build checks, a 40% line-coverage gate (locally measured 42.35%), cross-platform installer smoke tests, immutable action pins, release archives/checksums/provenance automation, VS Code extension packaging with an AGPL notice, and `--locked` installer behavior. Added regression coverage for compliance scanning and lifecycle-status boundaries. Updated Ratatui 0.29→0.30.2 to remove the RUSTSEC-unsound `lru 0.12.5` transitively; `cargo audit` now has only two pre-existing unmaintained-package notices and no vulnerability findings. Local verification: 747 Rust tests passed, `cargo fmt --check`, strict Clippy, extension frozen install/compile/package/audit, and a fresh staged Unix install all passed. GitHub branch protection, a verified signing key, CI execution, and publication remain the release-control steps after this change is committed.

- [2026-07-24] [Codex Kaira]: Added `FactoryCommand::AttachExistingProject` for onboarding an existing local Git repository without running the new-project scaffold. The runtime requires an absolute, canonical repository root, verifies `origin` and a full `HEAD` SHA using argv-only Git calls, rejects credential-bearing HTTP(S) remotes, persists path/remote/revision atomically under the owner, and exposes attached provenance in CLI/MCP/TUI Factory summaries. Targeted Factory surface tests passed (contracts, core, runtime, CLI, MCP, and TUI); live attached Garanti Platform at `/home/alaz/dev/web/garanti-platform` with its verified Git provenance.

- [2026-07-26] [Claude Kaira]: Per explicit user decision, rebranded the Product Factory lifecycle system's user-facing name to **Ocak** (Turkish, "forge/hearth"), display-only scope — no internal identifiers (module `product_factory`, type `FactoryCommand`, DB tables `factory_*`, MCP tool names `factory_overview`/`factory_execute`) were renamed. Confirmed no collision with the existing distinct **ANKA** transcript-recall cache (`raios anka`) before picking the name. Changed: CLI `raios factory` subcommand now displays/primarily runs as `raios ocak` with `factory` kept as a compatibility alias (`#[command(name = "ocak", alias = "factory")]`); TUI command palette and `/factory` slash-command handler gained `/ocak` as the primary form (`/factory` still accepted); TUI WORK route panel title changed from " Product Factory " to " Ocak "; MCP `factory_overview`/`factory_execute` tool *descriptions* (not names) now say "Ocak (Product Factory)"; README's Ocak CLI table row updated to show both forms. Explicitly left untouched: the unrelated legacy async job queue also historically called "Factory Mode" (`raios-runtime/src/factory.rs`, broadcast+webhook on job completion) — a different, older subsystem that happens to share the English word "Factory"; and all historical `docs/superpowers/{specs,plans,reviews}/*factory*.md` records, which stay under their original names as an immutable design-history record. Full workspace verification: `cargo build` clean, `cargo test --workspace --lib` 749 passed/0 failed, `cargo clippy --all-targets` 0 warnings, live binary check of both `raios ocak overview --json` and `raios factory overview --json` (alias) against the real DB.
- [2026-08-01] [Claude Kaira]: Rewrote `raios bootstrap` on branch `bootstrap-safety-plan` (6 approved tasks, `4c24f60`..`0d27619`) to be safe for a stranger to run: replaced the hardcoded personal-identity install (which used to write a specific person's MASTER.md and pull specific external agent ecosystems unconditionally) with a config-driven `[bootstrap]` section in `config.toml` (`BootstrapConfig`/`ClaudeMarketplace`/`RuleSyncRepo` in `raios-core/src/config.rs`) that's empty by default, a pure `build_plan()` for `--dry-run`/plan-printing with zero side effects, and a plan-then-confirm gate (`--yes`/`-y` to skip) before `execute()` touches anything. A whole-branch final review then found 5 real issues individual task reviews missed, all fixed in this pass: (1) `cargo fmt --all` violations across 4 files (module ordering in `lib.rs`, dispatch-arm/assert_eq!/three `return`-arm formatting) — ran rustfmt, no hand-editing. (2) Bootstrap wasn't idempotent — `claude plugin marketplace add`/`install`/`enable` non-zero exits were always classified `Failed` even for "already added/installed/enabled," so re-running an already-bootstrapped machine exited 1; worse, `add_marketplace_and_install` returned early on an "already added" marketplace, so a newly-configured plugin on an existing marketplace never got installed on re-run. Fixed by switching those three call sites from `.status()` to `.output()`, adding `output_indicates_already_done()` (case-insensitive substring match for "already" across combined stdout+stderr — deliberately permissive, since `claude`'s exact wording isn't guaranteed and a false-negative-as-Failed is worse for idempotency than a false-positive-as-done), and no longer returning early on an "already added" marketplace so the plugin-install loop still runs; outcome semantics: `Skipped` when every step in an action was a no-op (consistent with `install_npm_tool`'s existing "already on PATH" `Skipped`), `Ok` as soon as any step does real work. 5 new unit tests added for the matching logic. (3) README described the old hardcoded Maestro/ECC auto-sync behavior in 3 places (vision blurb, Quick Start walkthrough, CLI reference table) — rewritten to describe the actual config-driven plan+confirm flow with `--dry-run`/`--yes` documented, plus a new "Bootstrap Configuration" section modeled on the existing `[daemon]` docs, with the full `[bootstrap]` TOML schema pulled from the real struct fields. (4) Both TUI setup-wizard config-save sites (`setup_wizard/exec.rs::exec_initialize`, `app/events/keyboard/setup.rs::handle_setup_key`) built a fresh `Config { .. }` struct literal and called `.save()`, which serializes the entire struct over the existing `config.toml` — silently destroying any hand-authored `[bootstrap]`/`[daemon]`/`[factory]`/`github_user` on every wizard re-run (pre-existing defect predating this branch, incidentally touched but not noticed by this branch's own Task 2 fix in `560093f`). Fixed both sites to `Config::load().unwrap_or_default()` first and only overwrite the fields each site is actually wizard-collecting (workspace paths + agent-wrapper choice), leaving everything else — including `bootstrap` — exactly as loaded. (5) `sync_rules` cloned into a fully predictable `std::env::temp_dir().join("raios-bootstrap-<repo>")` path under shared, world-writable `/tmp` — a pre-existing or maliciously pre-planted directory there would have `git pull` run inside it with no ownership check, a local hijack risk. Moved to `dirs::cache_dir().join("raios").join("bootstrap").join(<repo>)`, under the user's own home-derived cache dir. Hygiene: regenerated `SIGMAP.md` via the installed `sigmap` CLI; this changelog entry. Verification: `cargo fmt --all -- --check` clean, `cargo build --workspace` clean, `cargo test --workspace --lib` all green (raios-runtime bootstrap module: 15 tests, up from 4 pre-existing + no regressions elsewhere), `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.
- [2026-08-02] [Antigravity Kaira]: Completed handoff from `claude_kaira` to wire `raios-factory-ui` (`tools/raios-factory-ui/`, untracked React/Vite dashboard) to real `raios-runtime` HTTP daemon API instead of `mockData.js`. Backend (`crates/raios-runtime/src/server/http/`): registered `GET /api/factory/overview` (returns `FactoryOverviewSnapshot` from `load_work_snapshot`), `POST /api/factory/command` (dispatches `FactoryCommand` via `dispatch_factory_command` with `http_may_execute` human-approval enforcement), and added CORS OPTIONS preflight & origin handling for local development ports in `auth_middleware`. Frontend (`tools/raios-factory-ui/`): updated `App.jsx` and `Header.jsx` with token state (read/saved to `localStorage`), `fetchOverview` daemon sync targeting `localhost:42071`, and live HTTP indicator. Verification: `pnpm run build` in `tools/raios-factory-ui` clean (268ms), `cargo fmt --all` clean, `cargo test --workspace --lib` clean (368 passed/0 failed, including 1 new unit test for `http_may_execute`), `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean (0 warnings), `SIGMAP.md` regenerated.
- 2026-08-02 [Antigravity Kaira]: Implemented Sub-project B (interactive AGENT_CONSTITUTION.md generator for TUI setup wizard) on feature branch `feature/interactive-constitution-wizard`. Created `ConstitutionParams` struct in `setup_wizard::types` and updated `master_template` to dynamically substitute `system_name`, `claude_name`, `codex_name`, `opencode_name`, `antigravity_name`, `communication_lang`, `dev_ops_path`, and `github_user` instead of hardcoding maintainer persona and `~/dev` path. Updated `WizardState` in `app::state` with fields, `Default` impl, `to_constitution_params()` helper, and 7-field navigation/editing in `actions.rs` and `keyboard/setup.rs`. Replaced static `MASTER_PREVIEW` banner in `render_master` with live dynamic preview pane rendering `master_template(&params)` on every keystroke. Added 2 unit tests in `setup_wizard::templates` (`master_template_substitutes_custom_params`, `master_template_falls_back_to_defaults_on_empty_strings`). Full verification: `cargo test --workspace --lib` clean (369 passed/0 failed), `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean (0 warnings), `cargo fmt --all -- --check` clean, `SIGMAP.md` regenerated.
- 2026-08-02 [Antigravity Kaira]: Implemented strict CORS origin hardening on feature branch `feature/cors-origin-hardening`. Added `cors_allowed_origins: Vec<String>` to `HubPolicy` in `crates/raios-core/src/security/policy.rs` (defaults to `["http://localhost:5173", "http://127.0.0.1:5173"]`, configurable under `[server.hub]` in `raios-policy.toml`). Replaced prefix-matching (`starts_with("http://localhost:")`) in `crates/raios-runtime/src/server/http/auth.rs` for both OPTIONS preflight and post-request response headers with exact origin matching against policy allowed origins. Added 5 integration unit tests covering preflight & post-request matching/rejection and policy file override. Full verification: `cargo test --workspace --lib` clean (374 passed/0 failed), `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean (0 warnings), `cargo fmt --all -- --check` clean, `SIGMAP.md` regenerated.
- 2026-08-06 [Codex Kaira]: Hardened the uncommitted `raios mem export-portable` / `import-portable` feature. Export now fails rather than silently dropping unreadable rows and refuses secret-like title, description, or body content. Import bounds untrusted JSON to 16 MiB and 50,000 items, applies every item through one SQLite transaction, and rolls everything back if any item is invalid. Added round-trip, rollback, and secret-refusal regression tests; documented both commands in README. Verified `cargo test --workspace --lib` (855 passed), `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `raios security`, and `raios deps`.
- 2026-08-06 [Codex Kaira]: Reworked the typed TUI's `NOW` route into an operational console. A trusted snapshot now selects a valid project and derives a bounded action list; the screen has separate Attention, Project, and Next Actions focus areas, with keyboard and mouse routing. `Enter` is limited to snapshot refresh or local route/focus changes, while `a`/`r` keep using owner-checked idempotent approval commands. The legacy direct terminal agent launcher was intentionally not carried into this flow because typed `LaunchAgent` has no runtime command implementation. Verified TUI tests (65), Clippy with warnings denied, and security scan A/95 (only pre-existing auth test `.unwrap()` low finding).
- 2026-08-06 [Codex Kaira]: Made the typed TUI `WORK` route operational for personal tasks. `CreateTask` now accepts the selected absolute `project_path` (with legacy `project_id` JSON accepted on input), validates title/path bounds and secret-like titles, then atomically writes a `source_kind='tui'` task plus list association under existing idempotency/audit control. `UpdateTaskStatus` accepts only bounded operator states and only touches those TUI-managed personal tasks; agent, handoff, swarm, Factory, and legacy Markdown tasks remain protected. Added the keyboard composer (`n`, `+`/`-`, `Enter`, `Esc`) and status keys (`i`/`b`/`c`), snapshot priority projection, runtime/contract/TUI tests, and README instructions. Verified workspace tests, Clippy, security A/95, and clean dependencies.
- 2026-08-06 [Codex Kaira]: Added the missing mouse parity for typed `WORK` tasks. The renderer and event layer now share a `WorkRouteLayout`, which fixes the prior task-row click offset caused by the Ocak summary. Users can click projects/tasks, the visible `Task Actions` strip, and composer priority/cancel/create buttons. Mouse actions reuse the same authenticated task submission/status handlers as keyboard shortcuts, while only explicit hit regions are accepted. Added geometry/hit-test coverage and updated README usage.
- 2026-08-06 [Codex Kaira]: Implemented the first real typed TUI agent-launch path. NOW exposes `Launch Codex session` for the selected registered project; the command is server-authorized, policy/audit/idempotency guarded, validates a four-agent allowlist plus bounded non-secret prompt and exact registered project path, then opens `raios run` in a separate terminal. A new direct-argv terminal primitive keeps the wrapper interactive without client-built shell text; the existing wrapper retains its control-plane session tracking. Verified launch validation tests, all 69 TUI tests, workspace Clippy, security A/95, and clean dependencies.
- 2026-08-06 [Codex Kaira]: Rebranded the typed TUI from the supplied ASCII artwork. Added one `ui::brand` source for the sampled hot-orange (`#FF8600`), electric-blue (`#00ACFF`), and dark-navy palette plus a responsive six-row circular ASCII mark. The dashboard header and recent-project surface now render that same two-tone mark; all shared UI semantic colors inherit the palette. Verified `cargo test -p raios-surface-tui --lib` (70 passed).
- 2026-08-06 [Codex Kaira]: Made typed `EXPLORE` operational. Its shared mouse/render layout now provides a focused search input, selectable daemon-index results, tool traces, and logs; `/` edits, `Enter` submits/opens. The TUI accepts only bounded non-secret search queries and serializes the daemon request, while opening a result requires a canonical local file inside the configured workspace (remote paths stay blocked). Hardened the older full-screen search to use the same serializer and input checks. Verified 72 TUI tests and Clippy with warnings denied.
- 2026-08-06 [Codex Kaira]: Made typed `GOVERN` scheduler controls operational. The route now shares its panel geometry with mouse hit testing and exposes only explicit `Run now` / `Pause/Resume` controls for a selected cron job (`r` / `p` with keyboard). Both actions use the existing typed, authenticated, policy/audit/idempotent server path; idempotency keys include the snapshot sequence so a duplicate gesture is suppressed without permanently preventing later runs. Verified 73 TUI tests and Clippy with warnings denied.
- 2026-08-07 [Claude Kaira]: Researched `PrimeIntellect-ai/prime-agent` (MIT, TS monorepo, ~4.9k stars, active) as a candidate integration for raios. Decision: **do not** wrap it as a 5th `raios run`/`handoff` agent identity — its Claude Pro/Max OAuth path bills as per-token "extra usage," not against plan quota (`providers.md`), which would regress the already-solved "one harness, native-subscription, zero extra cost" goal that raios's existing 4-agent wrapper (`agent_command()` in `crates/raios-runtime/src/daemon/proxy.rs`, whitelist also duplicated in `agent_runner.rs` at ~L276-298 and ~L627-634) already achieves more cheaply. Confirmed via source read (not just docs) that raios's current `spawn_agent`/`raios handoff` path is fire-and-forget with a Death Timer plus a DB-only reassignment (`create_handoff_workflow` in `crates/raios-core/src/db/wf_handoff.rs`) — there is no live/resident agent process and no way to steer a running agent mid-turn today; building that would require raios to own its own model-call/tool-loop (like prime-agent's TS-owned `AgentSession`), not just wrap external CLIs — out of scope unless the external CLIs (Claude Code/Codex/Copilot) ever expose their own "inject into a running session" primitive. Two ideas judged worth borrowing later, as patterns not dependencies: (1) MCP integrations authored as importable Python/typed packages with dynamic `list_tools()` discovery instead of a hardcoded tool schema (see `packages/coding-agent/docs/mcp-integrations.md`'s `McpIntegration` base class) — applicable to how raios skills/sigmap integrations get authored; (2) delivery-mode semantics for agent-to-agent messages (`auto`/`steer`/`follow_up`, with `delivered` vs `queued` receipts) as a reference vocabulary if raios's handoff/messaging model ever grows beyond single-shot `assignee_id` reassignment. Full findings (architecture doc summaries, whitelist file/line refs) live in this conversation's context only — not separately filed.
- 2026-08-10 [Claude Kaira]: Merged PR #16 (`feature/cors-origin-hardening` → `master`). The branch actually bundled 3 unrelated concerns; split into atomic commits: (1) the CORS exact-match hardening itself (`policy.rs`/`auth.rs`, already described above), (2) control-plane input validation hardening (`validate_task_title`/`validate_project_path`/`validate_agent_name`, secret-detection, `CreateTask.project_id` renamed to `project_path` with `#[serde(alias = "project_id")]` for backward compatibility), (3) the typed TUI "operational console" work — merged via `git rebase origin/master` against already-merged PR #15's independent `WorkFocus` (Projects/Ocak/Tasks) model, reconciling it with this branch's `OperationPanel` (Attention/Project/Actions) model for the Now route without dropping either. The rebase surfaced and fixed a real latent bug: Work-route task-status keys (`i`/`b`/`c`) checked `right_panel_focus` (true for both Ocak and Tasks sub-focus) instead of `work_focus == WorkFocus::Tasks`, which would have let Ocak-focused keypresses mutate the wrong task. Also fixed a Windows-only CI failure: `tui_personal_task_create_and_status_update_are_transactional` hardcoded a Unix-style `/workspace/raios` path, which `Path::is_absolute()` doesn't consider absolute on Windows — made the literal `cfg!(windows)`-conditional. All 13 required CI checks green before merge.
- 2026-08-11 [Claude Kaira]: Wrote the first real unit tests for `raios-surface-cli` (identified as the workspace's weakest-covered crate at 7.0%, see 2026-08-10 coverage-floor entry). PR #18 added 10 tests for `reflect.rs`'s pure `calculate_score`/`build_recommendations` functions (used by `raios reflect`'s workspace health scoring) — score clamping at 0/100, dirty-file presence-not-count weighting, recommendation ordering. Chose pure scoring/recommendation logic over I/O-heavy CLI wiring as the highest-ROI starting point for this crate.
- 2026-08-10 [Claude Kaira]: Raised the CI line-coverage floor from 42% to 47% (`.github/workflows/ci.yml`'s `--fail-under-lines`) after measuring real workspace coverage with `cargo llvm-cov --workspace` post-PR#16-merge: 47.48% lines overall, with a wide spread by crate (`raios-core` 72.7%, `raios-contracts` 65.9%, `raios-runtime` 56.1%, `raios-surface-tui` 24.1%, `raios-surface-mcp` 19.6%, `raios-surface-cli` 7.0% — the last two are the real gaps, not TUI's largely-render-only code). Set the floor at 47 (not 47.48) to leave a small buffer for normal churn while still locking in the improvement as a regression guard. `cargo audit` in the same pass: 0 vulnerabilities, only the same 3 pre-existing unmaintained/unsound advisories (`number_prefix`, `paste`, `event-listener`) seen in prior audits. Updated README's coverage-floor mention (42%→47%) to match; did not touch CHANGELOG.md since that's only updated at `raios version-bump` time, not per-commit.
- 2026-08-17 [Claude Kaira]: Found aiosd dead since 2026-08-15 19:37 (~44h) — user reported "aiosd çalışmıyo". `systemctl --user status` showed `status=0/SUCCESS`, no crash; `ss`/`pgrep` confirmed nothing listening on 42069/42070/42071. Root cause via systematic debugging: `Kernel::run()` (`crates/raios-runtime/src/kernel.rs`) races the daemon-TCP/MCP-TCP/HTTP listener tasks with `tokio::select!`, but every arm only `eprintln!`'d whichever task finished first and then unconditionally returned `Ok(())` — silently discarding the real result, contradicting its own doc comment ("error is propagated"). That night's overlapping restarts had left port 42070 transiently held; the 19:37 boot's MCP-over-TCP listener hit `EADDRINUSE`, which `select!` treated as "done," shutting the whole kernel down and exiting 0 — and `Restart=on-failure` (systemd unit) never fires on a clean exit, so it just stayed dead. Fixed via TDD: added `flatten_join_result()` (collapses a spawned task's `Result<Result<()>, JoinError>` into `Result<()>`), 3 new unit tests (`kernel::tests::flatten_join_result_*`) written and watched fail (E0425, function didn't exist) before implementing, then wired into `run()` so the select arms' outcome is actually returned instead of hardcoded `Ok(())`. Full workspace suite green (1049 tests across 6 crates, 0 failed), clippy clean. Rebuilt release binaries, ran `install.sh` (synced `~/.local/bin/{raios,aiosd}`, restarted `aiosd.service`), live-verified all three ports bound again (`ss -ltnp`) with the new binary's SHA-256 matching the fresh build. Committed `74fcedb`. Net effect: a future transient bind failure now yields a non-zero exit, so `RestartSec=5` will actually recover the daemon instead of leaving it dead until someone notices and restarts it by hand.
- 2026-08-17 [Claude Kaira]: Closed the 2026-07-15 `docs/BUDGET.md` follow-up ("investigate what's actually consuming the 2.2 GB") after user asked to check kaira-launcher's top-bar extension then tackle DB cleanup. Top-bar check was clean: `kaira-launcher@k-ai-ra.org` ACTIVE, installed `extension.js`/`weatherIndicator.js` byte-identical to source (no reload drift), zero JS ERROR lines attributable to it this boot (the only `get_boolean`/`this._settings is null` spam in the journal traced to GNOME core's own `dateMenu.js`, triggered by gsconnect notifications — unrelated). DB investigation found `workspace.db` at 4.4 GB, dominated by `bm25_postings` (2.2 GB) and trigram tables (~1.5 GB): (1) `core/R-AI-OS-audit` — a redundant full clone (5.2 GB, 5.0 GB of it an uncleaned `target/`) made for a one-off audit task over a month earlier; git-clean, and its one commit (`385f3e5`) was already confirmed present in `master`'s own history — safe full delete. (2) `core/R-AI-OS-worktrees/raios-tray-desktop-independent` — an orphaned worktree (1.1 GB) with real uncommitted work: a complete GTK/AyatanaAppIndicator3 → portable Qt `QSystemTrayIcon` rewrite of `tools/raios-tray/raios-tray.py` plus a new `desktop_runtime.py` (session detection + host guidance for Budgie/GNOME/Plasma). Confirmed via diff this refactor didn't exist anywhere in master yet; copied the 4 changed files in, `py_compile` + a manual sanity check of `detect_desktop_session`/`tray_host_guidance` across 4 env combos, committed `20e9743`, then deleted the worktree. (3) `JUCE` and `ghostty` — pinned upstream reference clones (104 MB / 1.6 GB, git-clean, not authored source) contributing ~4,200 indexed files; added to `search::indexer::SKIP_DIRS` via TDD (`fs_mtimes_skips_pinned_upstream_reference_clones` written failing first) — commit `6a59937`. User ran the two `rm -rf`s themselves (recursive force-delete is blocked for the assistant even with `dangerouslyDisableSandbox`, by design — had to hand those two off via `!`-prefixed commands). Sequence per pass: rebuild+`install.sh`, `raios search "x" --reindex --dir ~/dev` (BM25 prune), `raios locate "x" --reindex --dir ~/dev` (trigram prune, ran ~15min the first time over the full pre-cleanup tree), stop `aiosd`, `sqlite3 workspace.db "VACUUM;"`, restart `aiosd` — repeated once after the two deletions landed. Final result: **4.4 GB → 2.0 GB** DB, **+6.3 GB** disk reclaimed from the deleted directories. Raised `DB_TOTAL_SIZE_SOFT_CAP_BYTES` 500 MB → 2.5 GB (commit `ec664c9`, `docs/BUDGET.md` updated with a full "Measured on 2026-08-17" section) since the remaining ~2 GB is legitimate index content for this workspace's real ~16K-file count, not further junk — `raios health` now reports `2.0 GB / 2.4 GB cap ✓`. Five commits total this session on top of the aiosd fix: `74fcedb`, `ab5eaf5`, `20e9743`, `6a59937`, `ec664c9`.

## Instincts
- Before pruning existing recovery backups, fsync and directory-sync the verified replacement snapshot and checksum; persistence failure must preserve old recovery points.
- Situation: consuming an append-only event log incrementally. Signal: multiple rows can share the same timestamp, so a timestamp-only cursor can permanently skip rows created at the cursor boundary. Response: advance delivery with a unique monotonic key such as the database row ID; retain timestamps only for display or interval gating, and regression-test rows inserted before and after a poll with identical timestamps. Expected payoff: at-most-once cursor progression without same-timestamp data loss.
- When retiring legacy TUI cursor state, trace every renderer, keyboard handler, background event, and mouse path first; replace reachable behavior with typed AppState views before deleting the old block.
- Situation: a typed TUI route exposes a server-supported operation but only as static status. Signal: command contracts and dispatcher cases exist while keyboard and mouse provide no explicit action. Response: add a shared render/hit-test layout and bind only visible controls to the existing authenticated command path with snapshot-scoped idempotency. Expected payoff: operational controls without UI geometry drift, duplicate side effects, or authority bypass.
- Situation: compact terminal branding needs the same mark in multiple views. Signal: each panel carries its own banner string and gradient. Response: centralize palette constants and styled ASCII lines in ui::brand, then consume the shared renderer. Expected payoff: visual consistency without duplicated layout or color drift.
- Situation: extending a typed TUI operation flow. Signal: a legacy UI shortcut invokes a local side effect while its matching typed command is unimplemented server-side. Response: derive console actions from trusted snapshots and expose only IPC operations enforced by the server; retain explicit owner-checked approval commands. Expected payoff: useful TUI controls without authority drift or a local execution bypass.
- Situation: portable memory JSON crosses device boundaries. Signal: export code can hide read failures or import records one-by-one. Response: fail on unreadable rows, bound untrusted input, scan for secret-like content, and import through one transaction. Expected payoff: no silent backup loss, secret leakage, or partial restore.
- Correction to prior entry ('recurring cron jobs reference unsupported agent... pause invalid jobs'): that diagnosis was wrong. Situation: aiosd.service sets StandardOutput=journal, but an old ~/.config/raios/aiosd.log file from a pre-journald daemon version (frozen since 2026-07-08) still existed on disk and looked like a live log. Signal: grepping that file showed 624 historical 'Unsupported agent: atc' scheduler failures, which were actually all resolved by commit 290b4fb before 2026-07-08 itself (see the same file: a successful 'Fired ... -> atc' line right after the fix, followed by zero further failures) - 'atc' is a legitimately registered raios extension (ahead-the-curve), not a broken agent name. Response: before treating any raios-runtime log file as current evidence, check its mtime against the daemon's actual StandardOutput config (journal vs file) and cross-check with 'raios cron list' run_count / last_run_at, which only increments on real success. Archived the stale file to aiosd.log.stale-2026-07-08. Expected payoff: stops repeat false alarms from dead log files being mistaken for live daemon state.
- Refactor debt tracked: 21 HIGH + 58 MED files (size/nesting/unwrap) as of 2026-07-28, mostly pre-existing (vendor/, daemon/, security/, mempalace.rs). WIP on factory/attach-existing-project grew 5 already-flagged files (hub.rs, tools_workspace.rs, services.rs, app/mod.rs, state.rs) with pure additions. Deferred: scope this as a separate planned refactor session, not bundled into feature commits.
- Situation: auditing product release readiness. Signal: master CI is green but the published tag may point to an older tree. Response: compare HEAD, tag SHA, tag tree contents, release assets, signatures, and branch protections before declaring ready. Expected payoff: prevents shipping an outdated or legally incomplete release despite a green default branch.
- Situation: designing an IPC transport meant to be reachable from inside a sandboxed coding agent's shell commands (Codex, Copilot, Cursor). Signal: unit tests pass because they call the transport from the unsandboxed test process, not from a real sandboxed child. Response: before declaring a socket/pipe-based channel "sandboxed-agent-safe," live-test it from an actual `codex exec --sandbox workspace-write` (or equivalent) invocation, not just an in-process test — Landlock/seatbelt sandboxes can deny `connect()` on a Unix domain socket even when its containing directory is on the allow-list, because the restriction is on the syscall/socket-family, not the path. Loopback TCP is reachable through the *same* sandbox but only when its network-access config flag is explicitly enabled (not the default) — switching transport families changes what's *possible*, not what's true out of the box; re-verify against the sandbox's actual default config, not just its most permissive one, before calling a fix complete. Expected payoff: catches a whole class of "works in tests, fails for the real sandboxed caller it was built for" bugs that no amount of same-process testing can surface, and avoids declaring victory on a fix that only works with a non-default flag the caller doesn't know to pass.
- Situation: exposing an existing privileged domain service through a local CLI. Signal: the service already has a typed canonical dispatcher with ownership, feature-gate, idempotency, audit, and secret checks. Response: parse a bounded typed local file, reject human-only variants before opening the mutation path, then delegate instead of adding a parallel write path. Expected payoff: transport parity without authority drift.
- Situation: extending R-AI-OS memory with raw agent transcripts. Signal: workspace.db is already 2.8 GB against its 500 MB budget and the curated L0-L3 memory already exists. Response: keep any rebuildable transcript-recall index in a separate owner-only cache, expose it as read-only recall, and preserve workspace.db for curated facts and control-plane state. Expected payoff: retroactive recall without worsening DB bloat or authority confusion.
- Situation: an MCP tool manifest changes. Signal: a rebuilt binary reports a manifest hash mismatch. Response: rebuild both CLI and daemon binaries, update every installed binary path used by PATH and systemd, then run pin-reset, restart the daemon, and verify a live tool call. Expected payoff: new agent tools become available without bypassing supply-chain pinning.
- For lifecycle changes, materialize impact records before mutation, require explicit owner acceptance, then preserve history as immutable revisions; create cycle stage rows without starting execution.
- Situation: a lifecycle artifact depends on required discovery information. Signal: the client can submit a draft before all answers are present. Response: calculate readiness from canonical persisted answers at the server-side mutation boundary, reject with stable missing keys, and do not write a partial artifact or receipt. Expected payoff: incomplete work is visible and recoverable without relying on UI behavior.
- Situation: an intake workflow evolves after early sessions may already exist. Signal: a new required prompt set is introduced. Response: version prompts with stable keys, seed them transactionally for new sessions, and repair missing prompts on session resumption without overwriting answers. Expected payoff: consistent discovery data without invalidating earlier local work.
- Situation: adding a local TUI workflow for a privileged product lifecycle feature. Signal: the UI needs to submit commands but public exposure is not approved. Response: use an isolated typed command contract over existing authenticated IPC, derive locality at the daemon, load the feature gate only from trusted config, and let the server enforce authorization and validation. Expected payoff: usable local flow without duplicating security policy in the UI or accidentally opening an API.
- Situation: adding a product-lifecycle write flow before public transport exists. Signal: data crosses workspace, product, intake, and charter boundaries. Response: keep the command surface disabled by default, derive actor identity from the transport, bind every write to stored ownership, hash the full payload for idempotency, and audit in the same transaction. Expected payoff: safe local iteration without accidental authority expansion.
- Situation: Adding a new control-surface summary to an evolving daemon protocol. Signal: newer UI data must coexist with older daemon snapshots and the feature must not create mutation authority. Response: add an additive default-valued snapshot field, project it from canonical read-only queries, and render it inside an existing route. Expected payoff: visible lifecycle state without daemon restart coupling, transport breakage, or accidental write paths.
- Situation: Adding a new persisted config section in a cross-surface Rust workspace. Signal: Config has direct struct literals in setup and auto-detection flows, so a new field compiles in Default but fails elsewhere. Response: locate every Config literal, preserve serde defaults for legacy files, and check all consuming surface crates. Expected payoff: prevents partial configuration migrations and runtime setup regressions.
- Situation: Designing a product-lifecycle layer on R-AI-OS. Signal: the legacy Factory is an async shell-job queue while workspace.db growth is dominated by rebuildable search indexes. Response: reserve Factory for the product domain, migrate the queue toward JobExecutor, and separate durable control state, rebuildable search cache, and content artifacts before adding lifecycle tables. Expected payoff: prevents semantic collision, unsafe execution inheritance, and unbounded canonical DB growth.
- When a new TUI field depends on a daemon restart but active agents make restart unsafe, enrich only canonical project paths inside the configured workspace with a bounded read-only local fallback; never trust remote snapshot paths for filesystem reads.
- When extending a control-plane DTO consumed by a long-running daemon, mark additive fields with serde(default) and test an older JSON payload so a freshly updated TUI remains compatible before the daemon restarts.
- When shipping cross-platform Rust changes from a Linux host, keep Windows behavior behind cfg branches and validate the full workspace in Windows CI; local GNU cross-checks can stop at a missing MinGW linker and do not prove runtime installer behavior.
- Situation: Recovering a partially implemented control-plane feature with a newly added E2E test. Signal: the test awaits a server-side accept before starting its client and the handler opens a global DB instead of the fixture. Response: remove the invalid harness, test the actual workflow against an isolated database, and add dependency injection before claiming transport-level E2E coverage. Expected payoff: prevents hangs and false confidence from tests that never exercise the intended state.
- Situation: Determining an agent's current R-AI-OS work from control-plane data. Signal: cp_agent_runs reports many sessions as running but their event streams are empty. Response: do not infer ownership or task content from session status alone; correlate sessions with active-process evidence, git diff, and a concrete handoff/task artifact before acting. Expected payoff: avoids false active-task assumptions and accidental concurrent edits.
- When using rustfmt in a Rust module tree, avoid running it on mod.rs unless skip_children is set; otherwise rustfmt can traverse child modules and create unrelated format diffs, including forbidden conflict files.
- When a generated context command is part of final verification, run git status afterward because some tools update timestamp-only files and can make a clean worktree dirty again.
- When troubleshooting timing/performance regressions, always run a benchmark with a release/optimized profile, as unoptimized debug profile math operations (like building HNSW graph indices) can be orders of magnitude slower and present false-positive regressions.
- Situation: a supervised daemon (systemd `Restart=on-failure`) silently stops and never comes back. Signal: `systemctl status` shows a clean `status=0/SUCCESS` exit, not a crash — on-failure restart policies never fire on exit 0. Response: when a `tokio::select!` (or equivalent race-the-tasks pattern) races multiple long-running subtasks, never let it collapse every branch into an unconditional `Ok(())` — propagate the actual outcome of whichever branch finished, including task-panic (`JoinError`), so a subtask's transient failure (e.g. `EADDRINUSE` from an overlapping restart) produces a non-zero process exit the supervisor can act on. Expected payoff: transient failures become supervisor-recoverable instead of silently permanent; catch this class of bug by checking exit *status*, not just presence/absence of a crash log, whenever "it just stopped running" is reported.
- Situation: `workspace.db` is over its size budget and the largest tables are BM25/trigram search indexes. Signal: an incremental (non-forced) `load_or_build`/reindex only diffs mtimes, so rows for paths that were deleted or newly added to `SKIP_DIRS` stay cached forever unless a *forced* reindex runs (`raios search "x" --reindex --dir <root>` / `raios locate "x" --reindex --dir <root>`), and even after that prunes the rows, the `.db` file itself does not shrink — SQLite reuses freed pages but never returns them to the OS without an explicit `VACUUM`. Response: stop `aiosd` (release its open connection) before running `sqlite3 workspace.db "VACUUM;"` directly, then restart it; do the forced reindex pass *before* `VACUUM`, not after, so the vacuum only has to run once. Expected payoff: DB size actually reflects current content instead of accumulating monotonically regardless of what gets deleted from disk.
- Situation: about to `rm -rf` a large directory the user has approved deleting. Signal: the Bash tool rejects it with a bare "Recursive force delete" error, and retrying with `dangerouslyDisableSandbox: true` gets the identical rejection — this is a harness-level guard, not a sandbox restriction, so no Bash-tool parameter routes around it. Response: hand the exact `rm -rf` command back to the user as a `!`-prefixed line instead of hunting for an equivalent non-`rm -rf` command (`find -delete`, `rsync --delete`, etc.) that would functionally bypass the same guard the harness is deliberately enforcing. Expected payoff: respects a deliberate safety boundary instead of working around it, and the user's own terminal history ends up with a record of what got deleted.
- 2026-08-17 [Claude Kaira]: Found and fixed a real false-positive spanning 8 call sites: `raios health`/`reflect`/`pre-flight`/the TUI all independently checked `path.join("SIGMAP.md").exists()`, but the installed `sigmap` (v8.18.0+, brew) hasn't produced that literal file by default for a while now — it writes multi-adapter signature blocks into `CLAUDE.md`/`AGENTS.md`/`.github/copilot-instructions.md`/`.github/gemini-context.md` instead, dropping `gen-context.config.json` as its init marker (no `SIGMAP.md`-producing flag exists anywhere in current `sigmap --help`). Every project using current sigmap read as "missing SIGMAP.md" even immediately after a real `sigmap` run — caught live while cleaning up `chernobyl-rbmk-sim`/`unicontrol-nextion-mockup` (workspace `raios reflect` score stuck at 65 despite both projects being freshly sigmap'd). Added `raios_core::entities::has_sigmap_context()` as the single source of truth (TDD, 3 tests written failing first) checking `SIGMAP.md` (legacy) OR `gen-context.config.json` (current init marker — deliberately not `AGENTS.md`/`CLAUDE.md` presence alone, since a project could hand-write those for unrelated reasons) and rewired the 6 computation sites (the other 2 — `instinct.rs`, TUI `projects.rs` — only read the resulting boolean field, needed no change). Full workspace suite green (1053 tests), clippy clean, committed `9dcb023`, rebuilt+reinstalled. Live-verified: workspace `SIGMAP.md` coverage read 7/9 present (up from 4/9) immediately after, `raios reflect` score 65→75, and the two genuinely-still-missing projects (`gtab`, `VCT-ai360`) are both already-archived/stale ones that were correctly flagged before too.
- [2026-08-18] [Codex Kaira]: Completed the final whole-branch review remediation for background activity notifications. Replaced timestamp-only delivery cursors with monotonic event-ID cursors and legacy-schema upgrades; made active High/Critical security finding notification state transactional and fingerprint-deduplicated; validated API client IDs; persisted a per-install tray UUID with a safe deterministic fallback; restored the documented `cursor_ts` response; aligned README/design/progress documentation; and made the branch rustfmt-clean. Isolated verification passed across the full workspace tests, strict all-target/all-feature Clippy, rustfmt check, Python compilation, diff hygiene, dependency CVE scan, and security pattern scan. The live daemon, systemd service, and production workspace database were not touched.
- [2026-08-18] [Codex Kaira]: Fixed a real audit-ledger TOCTOU race found during product-readiness review: `record_audit_event` previously selected the current tail hash and inserted the next row as separate autocommit statements, so concurrent `agent.steer` writes created four forks at IDs 3489/3491/3499/3500 even though every individual row hash remained valid. Standalone appends now use `BEGIN IMMEDIATE`; callers already inside control-plane/Product Factory transactions upgrade their existing transaction before reading the tail, avoiding nested transactions. Added a deterministic eight-connection file-backed regression test (red on the old code, green after the fix) plus caller-owned transaction coverage. With explicit owner approval, stopped daemon/tray, created an integrity-checked mode-0600 2.2GB backup and immutable linkage manifest under `~/.config/raios/audit-repairs/audit-chain-repair-20260818T085529Z` (backup SHA-256 `1194720622a0d53c4e6b238516af452321f63f1d71f091672b8ac2ba3173a7fc`, manifest SHA-256 `da819cfab7f32619d6f0813845b07143469bf68ac213856f45f0b6ec443c40e4`), re-chained 41 metadata rows without changing event payload/timestamp/actor, and appended transparent `audit.chain_repaired` event 3530. Deployed source-identical release binaries; removed a VS Code-auto-spawned orphan daemon that briefly occupied all three ports during restart; live systemd daemon PID 71518 owns 42069/42070/42071 with zero restarts, tray is active, and `raios verify-chain` verifies all 3530 entries.
- [2026-08-18] [Codex Kaira]: Resolved the newly published `RUSTSEC-2026-0258` HTTP/2 denial-of-service advisory by updating transitive `h2` 0.4.15→0.4.16. `cargo update` initially rewrote ten unrelated Windows `windows-sys` resolution references under Cargo 1.96; restored those unrelated lock entries so the final `Cargo.lock` diff contains only the `h2` version/checksum. Verification: `cargo metadata --locked`, 1,093 workspace tests (plus 2 intentionally ignored doctests), strict all-target/all-feature Clippy, rustfmt, `cargo audit` with zero known CVEs, and `raios deps` with zero known CVEs.
- [2026-08-18] [Codex Kaira]: Fixed the tray's repeatable cold-boot Wayland crash at the lifecycle boundary. Boot evidence showed `raios-tray.service` starting from `default.target` at 11:25:16 and aborting because `wl_display` did not exist; `graphical-session.target` only became active at 11:25:20. `After=graphical-session.target` was already present but ineffective because the target was not pulled into the same transaction. Changed `[Install] WantedBy` to `graphical-session.target`, retained `After` and `PartOf`, added a standard-library unit-file regression test to CI (red before, green after), and documented install/refresh commands. Reinstalled and reenabled the live unit: the default-target symlink is gone, `graphical-session.target.wants/raios-tray.service` exists, and the tray is active with zero restarts. A real reboot is deferred to the final deployment verification so it does not disrupt the remaining sequential fixes.
- [2026-08-18] [Codex Kaira]: Fixed daemon-owned zombie processes from scheduled detached work. Both `spawn_agent_detached` and `spawn_ext_command_detached` returned a PID and immediately dropped the live `Child` handle, leaving exited cron children as `<defunct>` descendants of long-running `aiosd`. Added one process-wide `raios-child-reaper` thread that owns all detached handles, polls them without blocking on long-running peers, and consumes their exit status; if the reaper cannot start or accept a child, the spawn path kills and waits for that child before returning an error. A portable short-lived-child test failed before the implementation and now proves the reaper waits successfully; all 364 runtime unit tests plus 3 integration tests and strict all-target/all-feature Clippy pass.
- [2026-08-18] [Codex Kaira]: Deployed the detached-child reaper in release build `276bdb10efa9303dee4456addd661ce84285aae6c2837d21715e45bff937f5e6`. Target, installed `~/.local/bin/aiosd`, and live `/proc/117011/exe` hashes matched; systemd reported active/running, zero restarts, and ownership of ports 42069/42070/42071. The daemon had no direct child processes and no daemon-owned zombies after restart. The one host zombie observed was `xdg-terminal-ex` under unrelated parent PID 7305, outside R-AI-OS.
- [2026-08-18] [Codex Kaira]: Added a bounded SQLite maintenance/recovery surface for the shared `workspace.db`: `raios db check [--full]`, `db checkpoint [--truncate]`, and `db backup [--keep 1..=10]`. Snapshots use SQLite's online backup API, source/destination quick checks, SHA-256 sidecars, 0700/0600 Unix permissions, fsync, and managed-only retention; each replacement snapshot and checksum are made durable before any older recovery point is pruned. Checkpoint refuses to create a missing DB, and restore remains an explicit offline owner procedure documented in `docs/DATABASE_RECOVERY.md`. Five isolated maintenance tests plus the full 1,100-test workspace suite and strict all-target/all-feature Clippy passed. Live validation: quick check reported healthy at 2.16 GiB with one free page; a busy-free truncate checkpoint reduced WAL 72.3 MiB→0; the first 2.16 GiB online snapshot `workspace-20260818T094511464438291Z-3cbd281529c3.db` passed independent `sha256sum -c` with SHA-256 `3c1bf7ac8f7c773474fdd5f1465fcac2027db074c498a2df117699ed76926b65`. Empty SQLite-created snapshot WAL/SHM companions were removed after validation and future backups now clean them automatically only when the WAL is empty.
- [2026-08-18] [Codex Kaira]: Closed the local version/distribution drift. GitHub inspection showed public latest remains signed `v3.8.0` while master is 30 commits ahead and current CI is green. Centralized all six Rust packages under root `[workspace.package] version = "3.9.0"`, taught version discovery to resolve `version.workspace = true` from member directories, and added a regression test for the formerly silent workspace bump failure. Prepared the changed VS Code extension as 0.9.0 and successfully built `raios-0.9.0.vsix` with a clean pnpm audit. Added a release preflight that requires strict semver, one matching workspace version, finalized dated CHANGELOG, matching README installer/badges/VSIX filename, and a GitHub-verified signed annotated tag before any platform packaging job runs. Positive and fail-closed preflight paths were both exercised. Full final suite passed with 1,101 tests plus 2 intentionally ignored doctests; strict Clippy, rustfmt, locked metadata, workflow YAML parse, Cargo audit (zero CVEs; four allowlisted warnings), and pnpm audit passed. Installed final local 3.9.0 after the durability review: daemon PID 241789 and tray PID 241804 are active with zero restarts; daemon target/installed SHA-256 is `b0eb06ddadfe60d65144538f1b44651983be188b99a12bdf3f2326babc40bf68`, CLI target/installed SHA-256 is `1b174f2770b7dfd82df9b80bb97d90fcfc34d11998b435b51758d5b4b0911fa7`, ports 42069/42070/42071 are owned by the service, no daemon child/zombie exists, audit chain 3531/3531 verifies, and SQLite quick-check is healthy with a 4.1 KiB WAL. Public tag/release creation was deliberately not performed without explicit external-publication authorization.
- [2026-08-18] [Codex Kaira]: Final GNOME top-bar acceptance passed after logout/login: Kaira loaded 33 R-AI-OS projects, its 15-second important/digest poll ran without queue, fetch, or DateMenu errors, authenticated HTTP notification retrieval returned `ok`, weather and battery were live, and the daemon/tray stayed active. The login exposed a separate VS Code/systemd race: the installed VS Code extension could trust a fresh token and detached-spawn `aiosd` while `aiosd.service` was starting, splitting ports between two processes and forcing 12 systemd restarts before recovery. `DaemonManager` now uses a real `AUTH`/`SessionStarted` readiness handshake and asks the Linux systemd user manager to start the canonical service; detached spawn remains only a non-systemd fallback. Added source-contract tests, compiled/package-audited the extension, packaged and locally installed `alazndy.raios@0.9.0` over 0.8.1.
- [2026-08-18] [Codex Kaira]: Fixed Windows CI SQLite backup failures where `File::sync_all` on a read-only handle returned `ERROR_ACCESS_DENIED`; `sync_file` now reopens snapshots with read/write access before flushing, preserving durability semantics across platforms. The six targeted maintenance tests pass locally.
