# CI/CD Status Tracking Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `raios ci [project]` komutu GitHub Actions son run + job breakdown'ı gösterir.

**Architecture:** Yeni `src/core/ci.rs` modülü `gh api` shell komutu ile GitHub REST API'yi sorgular. `core/mod.rs`'e `pub mod ci;` eklenir. CLI'a `Commands::Ci` eklenir. Mevcut `core/build.rs` deseni izlenir.

**Tech Stack:** Rust, `std::process::Command` (`gh api`), `serde_json`, `clap`, `anyhow`

---

## File Map

| Dosya | Değişiklik |
|-------|-----------|
| `src/core/ci.rs` | Yeni — `CiRun`, `CiJob`, `CiReport`, `get_ci_status()`, `parse_owner_repo()`, 3 unit test |
| `src/core/mod.rs` | `pub mod ci;` ekle |
| `src/cli.rs` | `Commands::Ci` + match arm + `cmd_ci()` + `print_ci_report()` |

---

## Task 1: `src/core/ci.rs` — Types + Logic + Tests

**Files:**
- Create: `src/core/ci.rs`
- Modify: `src/core/mod.rs`

- [ ] **Step 1.1: Failing unit testleri yaz**

`src/core/ci.rs` dosyasını oluştur ve içine YAZ (sadece testler, henüz implementation yok):

```rust
use std::path::Path;
use anyhow::Result;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CiRun {
    pub id: u64,
    pub workflow_name: String,
    pub status: String,
    pub conclusion: Option<String>,
    pub branch: String,
    pub created_at: String,
    pub html_url: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CiJob {
    pub name: String,
    pub status: String,
    pub conclusion: Option<String>,
    pub duration_secs: Option<u64>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CiReport {
    pub run: CiRun,
    pub jobs: Vec<CiJob>,
}

pub fn parse_owner_repo(remote_url: &str) -> Option<String> {
    todo!()
}

pub fn get_ci_status(project_path: &Path) -> Result<CiReport> {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parse_https_url() {
        let result = parse_owner_repo("https://github.com/alazndy/R-AI-OS.git");
        assert_eq!(result, Some("alazndy/R-AI-OS".to_string()));
    }

    #[test]
    fn parse_https_url_no_git_suffix() {
        let result = parse_owner_repo("https://github.com/alazndy/R-AI-OS");
        assert_eq!(result, Some("alazndy/R-AI-OS".to_string()));
    }

    #[test]
    fn parse_ssh_url() {
        let result = parse_owner_repo("git@github.com:alazndy/R-AI-OS.git");
        assert_eq!(result, Some("alazndy/R-AI-OS".to_string()));
    }

    #[test]
    fn parse_invalid_url_returns_none() {
        assert_eq!(parse_owner_repo("invalid"), None);
        assert_eq!(parse_owner_repo(""), None);
        assert_eq!(parse_owner_repo("https://gitlab.com/foo/bar"), None);
    }
}
```

- [ ] **Step 1.2: `pub mod ci;` ekle**

`src/core/mod.rs` sonuna ekle:

```rust
pub mod ci;
```

- [ ] **Step 1.3: Testleri çalıştır — FAIL beklenir**

```bash
cargo test --lib core::ci::tests -- --nocapture 2>&1 | head -20
```

Beklenen: `parse_https_url ... FAILED` (todo! panics)

- [ ] **Step 1.4: `parse_owner_repo` implementasyonunu yaz**

`src/core/ci.rs`'teki `todo!()` yerine:

```rust
pub fn parse_owner_repo(remote_url: &str) -> Option<String> {
    // HTTPS: https://github.com/owner/repo.git
    if let Some(rest) = remote_url.strip_prefix("https://github.com/") {
        let repo = rest.trim_end_matches(".git");
        let parts: Vec<&str> = repo.splitn(2, '/').collect();
        if parts.len() == 2 && !parts[0].is_empty() && !parts[1].is_empty() {
            return Some(format!("{}/{}", parts[0], parts[1]));
        }
    }
    // SSH: git@github.com:owner/repo.git
    if let Some(rest) = remote_url.strip_prefix("git@github.com:") {
        let repo = rest.trim_end_matches(".git");
        let parts: Vec<&str> = repo.splitn(2, '/').collect();
        if parts.len() == 2 && !parts[0].is_empty() && !parts[1].is_empty() {
            return Some(format!("{}/{}", parts[0], parts[1]));
        }
    }
    None
}
```

- [ ] **Step 1.5: `get_ci_status` implementasyonunu yaz**

`src/core/ci.rs`'teki `get_ci_status` `todo!()` yerine:

```rust
pub fn get_ci_status(project_path: &Path) -> Result<CiReport> {
    // 1. Get remote URL
    let remote_url = get_remote_url(project_path)
        .ok_or_else(|| anyhow::anyhow!("No GitHub remote found for this project"))?;

    // 2. Parse owner/repo
    let slug = parse_owner_repo(&remote_url)
        .ok_or_else(|| anyhow::anyhow!("Could not parse GitHub owner/repo from: {}", remote_url))?;

    // 3. Fetch latest run
    let runs_json = gh_api(&format!("/repos/{}/actions/runs?per_page=1", slug))?;
    let runs = runs_json["workflow_runs"]
        .as_array()
        .and_then(|a| a.first())
        .ok_or_else(|| anyhow::anyhow!("No CI runs found for {}", slug))?;

    let run = CiRun {
        id: runs["id"].as_u64().unwrap_or(0),
        workflow_name: runs["name"].as_str().unwrap_or("CI").to_string(),
        status: runs["status"].as_str().unwrap_or("unknown").to_string(),
        conclusion: runs["conclusion"].as_str().map(str::to_string),
        branch: runs["head_branch"].as_str().unwrap_or("?").to_string(),
        created_at: runs["created_at"].as_str().unwrap_or("").to_string(),
        html_url: runs["html_url"].as_str().unwrap_or("").to_string(),
    };

    // 4. Fetch jobs for this run
    let jobs_json = gh_api(&format!("/repos/{}/actions/runs/{}/jobs", slug, run.id))?;
    let jobs: Vec<CiJob> = jobs_json["jobs"]
        .as_array()
        .unwrap_or(&vec![])
        .iter()
        .map(|j| {
            let started = j["started_at"].as_str().unwrap_or("");
            let completed = j["completed_at"].as_str().unwrap_or("");
            let duration_secs = parse_duration_secs(started, completed);
            CiJob {
                name: j["name"].as_str().unwrap_or("?").to_string(),
                status: j["status"].as_str().unwrap_or("unknown").to_string(),
                conclusion: j["conclusion"].as_str().map(str::to_string),
                duration_secs,
            }
        })
        .collect();

    Ok(CiReport { run, jobs })
}
```

- [ ] **Step 1.6: Helper fonksiyonları ekle**

`get_ci_status`'un altına:

```rust
fn get_remote_url(project_path: &Path) -> Option<String> {
    let out = std::process::Command::new("git")
        .args(["remote", "get-url", "origin"])
        .current_dir(project_path)
        .output()
        .ok()?;
    if out.status.success() {
        Some(String::from_utf8_lossy(&out.stdout).trim().to_string())
    } else {
        None
    }
}

fn gh_api(endpoint: &str) -> Result<serde_json::Value> {
    let out = std::process::Command::new("gh")
        .args(["api", endpoint])
        .output()
        .map_err(|e| {
            if e.kind() == std::io::ErrorKind::NotFound {
                anyhow::anyhow!("gh CLI not found — install GitHub CLI: https://cli.github.com")
            } else {
                anyhow::anyhow!("gh command failed: {e}")
            }
        })?;

    if !out.status.success() {
        let stderr = String::from_utf8_lossy(&out.stderr);
        if stderr.contains("401") || stderr.contains("authentication") {
            anyhow::bail!("Not authenticated — run: gh auth login");
        }
        anyhow::bail!("GitHub API error: {}", stderr.trim());
    }

    let json: serde_json::Value = serde_json::from_slice(&out.stdout)
        .map_err(|e| anyhow::anyhow!("Failed to parse GitHub API response: {e}"))?;
    Ok(json)
}

fn parse_duration_secs(started_at: &str, completed_at: &str) -> Option<u64> {
    if started_at.is_empty() || completed_at.is_empty() {
        return None;
    }
    // RFC3339: "2026-05-15T10:00:00Z" — parse minutes:seconds difference
    // Simple string-based: take last components, avoid chrono dependency
    let start_secs = rfc3339_to_secs(started_at)?;
    let end_secs = rfc3339_to_secs(completed_at)?;
    end_secs.checked_sub(start_secs)
}

fn rfc3339_to_secs(ts: &str) -> Option<u64> {
    // "2026-05-15T10:23:45Z" → parse as epoch seconds (approximate, good enough for display)
    let ts = ts.trim_end_matches('Z');
    let parts: Vec<&str> = ts.split('T').collect();
    if parts.len() != 2 {
        return None;
    }
    let date_parts: Vec<u64> = parts[0].split('-').filter_map(|s| s.parse().ok()).collect();
    let time_parts: Vec<u64> = parts[1].split(':').filter_map(|s| s.parse().ok()).collect();
    if date_parts.len() < 3 || time_parts.len() < 3 {
        return None;
    }
    // Rough epoch approximation (days since 1970-01-01 × 86400 + time)
    let y = date_parts[0].saturating_sub(1970);
    let d = y * 365 + (date_parts[1] - 1) * 30 + date_parts[2];
    Some(d * 86400 + time_parts[0] * 3600 + time_parts[1] * 60 + time_parts[2])
}
```

- [ ] **Step 1.7: Testleri çalıştır — PASS beklenir**

```bash
cargo test --lib core::ci::tests -- --nocapture
```

Beklenen: `test result: ok. 4 passed`

- [ ] **Step 1.8: cargo check**

```bash
cargo check --lib 2>&1 | tail -5
```

Beklenen: hata yok.

- [ ] **Step 1.9: Commit**

```bash
git add src/core/ci.rs src/core/mod.rs
git commit -m "feat(core): ci module — get_ci_status, parse_owner_repo via gh api"
```

---

## Task 2: CLI — `Commands::Ci` + `cmd_ci`

**Files:**
- Modify: `src/cli.rs`

- [ ] **Step 2.1: `Commands::Ci` varyantını ekle**

`src/cli.rs`'teki `Commands` enum'una (diğer varyantların yanına) ekle:

```rust
/// Show GitHub Actions CI/CD status for a project
Ci {
    /// Project name or path (default: current directory)
    project: Option<String>,
},
```

- [ ] **Step 2.2: Match arm ekle**

`match cmd` bloğuna ekle:

```rust
Commands::Ci { project } => {
    cmd_ci(project, &cfg.dev_ops_path, cli.json);
}
```

- [ ] **Step 2.3: `cmd_ci` fonksiyonunu ekle**

```rust
fn cmd_ci(project: Option<String>, dev_ops: &std::path::Path, json: bool) {
    use crate::core::ci::get_ci_status;

    // Find project path
    let project_path = if let Some(ref name) = project {
        let projects = crate::entities::load_entities(dev_ops);
        let n = name.to_lowercase();
        let found = projects.into_iter().find(|p| {
            p.name.to_lowercase().contains(&n)
                || p.local_path.to_string_lossy().to_lowercase().contains(&n)
        });
        match found {
            Some(p) => p.local_path,
            None => {
                eprintln!("Project not found: {}", name);
                std::process::exit(1);
            }
        }
    } else {
        std::env::current_dir().unwrap_or_default()
    };

    match get_ci_status(&project_path) {
        Ok(report) => print_ci_report(&report, json),
        Err(e) => eprintln!("CI error: {e}"),
    }
}
```

- [ ] **Step 2.4: `print_ci_report` fonksiyonunu ekle**

```rust
fn print_ci_report(report: &crate::core::ci::CiReport, json: bool) {
    if json {
        let out = serde_json::json!({
            "run": {
                "id": report.run.id,
                "workflow": report.run.workflow_name,
                "status": report.run.status,
                "conclusion": report.run.conclusion,
                "branch": report.run.branch,
                "created_at": report.run.created_at,
                "url": report.run.html_url
            },
            "jobs": report.jobs.iter().map(|j| serde_json::json!({
                "name": j.name,
                "status": j.status,
                "conclusion": j.conclusion,
                "duration_secs": j.duration_secs
            })).collect::<Vec<_>>()
        });
        match serde_json::to_string_pretty(&out) {
            Ok(s) => println!("{s}"),
            Err(e) => eprintln!("JSON error: {e}"),
        }
        return;
    }

    let status_icon = match report.run.conclusion.as_deref() {
        Some("success") => "✓",
        Some("failure") => "✗",
        Some("cancelled") => "○",
        _ => "…",
    };

    println!(
        "CI: {} @ {} — {} {}  ({})",
        report.run.workflow_name,
        report.run.branch,
        status_icon,
        report.run.conclusion.as_deref().unwrap_or(&report.run.status),
        &report.run.created_at[..10]
    );

    for job in &report.jobs {
        let icon = match job.conclusion.as_deref() {
            Some("success") => "✓",
            Some("failure") => "✗",
            Some("skipped") => "-",
            _ => "…",
        };
        let dur = match job.duration_secs {
            Some(s) if s >= 60 => format!("{}m {}s", s / 60, s % 60),
            Some(s) => format!("{}s", s),
            None => String::new(),
        };
        let failed = matches!(job.conclusion.as_deref(), Some("failure"));
        if failed {
            println!("  {} {:<20} {}  ← FAILED", icon, job.name, dur);
        } else {
            println!("  {} {:<20} {}", icon, job.name, dur);
        }
    }

    if !report.run.html_url.is_empty() {
        println!("\n  {}", report.run.html_url);
    }
}
```

- [ ] **Step 2.5: Build**

```bash
cargo build --bin raios 2>&1 | head -30
```

Beklenen: hata yok.

- [ ] **Step 2.6: Tüm testleri çalıştır**

```bash
cargo test --lib 2>&1 | tail -5
```

Beklenen: `test result: ok. 89 passed` (85 mevcut + 4 yeni)

- [ ] **Step 2.7: Smoke test**

```bash
cargo run --bin raios -- ci 2>&1
```

Beklenen: GitHub Actions çıktısı (veya auth hatası — her ikisi de doğru davranış).

- [ ] **Step 2.8: Final commit**

```bash
git add src/cli.rs
git commit -m "feat(cli): raios ci command — GitHub Actions status + job breakdown"
```

---

## Self-Review

**Spec coverage:**
- [x] `raios ci [project]` CLI komutu → Task 2.1-2.3
- [x] `gh api` ile son run → Task 1.5 (`gh_api("/repos/.../actions/runs?per_page=1")`)
- [x] Job breakdown → Task 1.5 (`gh_api("/repos/.../actions/runs/{id}/jobs")`)
- [x] `parse_owner_repo` HTTPS + SSH → Task 1.4
- [x] `gh` kurulu değil hatası → Task 1.6 `gh_api()` NotFound branch
- [x] Auth yok hatası → Task 1.6 `gh_api()` 401 branch
- [x] Remote URL yok hatası → Task 1.5 `get_remote_url()` None branch
- [x] No CI runs hatası → Task 1.5 `runs` None branch
- [x] Terminal çıktı: status + job breakdown → Task 2.4
- [x] JSON çıktı → Task 2.4
- [x] 4 unit test → Task 1.1

**Placeholder scan:** Yok.

**Type consistency:**
- `CiRun`, `CiJob`, `CiReport` Task 1.1'de tanımlandı, Task 1.5'te kullanıldı ✓
- `get_ci_status(project_path: &Path) -> Result<CiReport>` → Task 2.3'te `get_ci_status(&project_path)` ✓
- `parse_owner_repo(remote_url: &str) -> Option<String>` → Task 1.4, 1.5'te kullanıldı ✓
