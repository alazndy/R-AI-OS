

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.15.0 -->
<!-- Updated: 2026-07-06T14:31:36.705Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
vscode-extension/src/bridge/JumpToCode.ts ← ipc/DaemonClient
vscode-extension/src/commands/CommandBridge.ts ← ipc/DaemonClient, utils/raiosBinary, providers/DiagnosticProvider
vscode-extension/src/extension.ts ← ipc/DaemonClient, providers/StatusBarProvider, commands/CommandBridge, providers/DiffInboxProvider, providers/DiagnosticProvider
vscode-extension/src/ipc/DaemonClient.ts ← utils/raiosBinary
vscode-extension/src/ipc/DaemonManager.ts ← utils/raiosBinary
vscode-extension/src/ipc/TokenBridge.ts ← utils/raiosBinary
.tmp/raios-tray-work/raios_tray.py ← __future__, PySide6, platform, shlex, psutil
tools/raios-tray/raios-tray.py ← __future__, PySide6, gi, platform, shlex
```

## .tmp

### .tmp/hf-hub-0.3.2/.github/workflows/rust.yml
```
keys: [name, on, jobs]
job: build
```

### .tmp/hf-hub-0.3.2/.pre-commit-config.yaml
```
keys: [repos]
```

### .tmp/hf-hub-0.3.2/Cargo.toml
```
table [package]
table [dependencies.dirs]
table [dependencies.futures]
table [dependencies.indicatif]
table [dependencies.log]
table [dependencies.native-tls]
table [dependencies.num_cpus]
table [dependencies.rand]
table [dependencies.reqwest]
table [dependencies.serde]
table [dependencies.serde_json]
table [dependencies.thiserror]
table [dependencies.tokio]
table [dependencies.ureq]
table [dev-dependencies.hex-literal]
table [dev-dependencies.sha2]
table [dev-dependencies.tokio-test]
table [features]
key edition
key name
key version
key description
key homepage
key documentation
key readme
```

### .tmp/hf-hub-0.3.2/README.md
```
h1 How to use
code-fence bash
code-fence plain
code-fence rust
```

### .tmp/hf-hub-0.3.2/src/api/mod.rs
```
pub struct Siblings
pub struct RepoInfo
```

### .tmp/hf-hub-0.3.2/src/api/sync.rs
```
pub struct HeaderAgent
pub struct ApiBuilder
pub struct Api
pub struct ApiRepo
pub enum ApiError
impl HeaderAgent
impl ApiBuilder
impl ApiBuilder
  pub fn new() → Self
  pub fn from_cache(cache: Cache) → Self
  pub fn with_progress(mut self, progress: bool) → Self
  pub fn with_cache_dir(mut self, cache_dir: PathBuf) → Self
  pub fn with_token(mut self, token: Option<String>) → Self
  pub fn build(self) → Result<Api, ApiError>
impl Api
  pub fn new() → Result<Self, ApiError>
  pub fn client(&self) → &HeaderAgent
impl ApiRepo
impl ApiRepo
  pub fn url(&self, filename: &str) → String
  pub fn get(&self, filename: &str) → Result<PathBuf, ApiError>
  pub fn download(&self, filename: &str) → Result<PathBuf, ApiError>
  pub fn info(&self) → Result<RepoInfo, ApiError>
  pub fn info_request(&self) → Request
```

### .tmp/hf-hub-0.3.2/src/api/tokio.rs
```
pub struct ApiBuilder
pub struct Api
pub struct ApiRepo
pub enum ApiError
impl ApiBuilder
impl ApiBuilder
  pub fn new() → Self
  pub fn from_cache(cache: Cache) → Self
  pub fn with_progress(mut self, progress: bool) → Self
  pub fn with_cache_dir(mut self, cache_dir: PathBuf) → Self
  pub fn with_token(mut self, token: Option<String>) → Self
  pub fn build(self) → Result<Api, ApiError>
impl Api
  pub fn new() → Result<Self, ApiError>
  pub fn client(&self) → &Client
impl ApiRepo
impl ApiRepo
  pub fn url(&self, filename: &str) → String
  pub async fn get(&self, filename: &str) → Result<PathBuf, ApiError>
  pub async fn download(&self, filename: &str) → Result<PathBuf, ApiError>
```

### .tmp/hf-hub-0.3.2/src/lib.rs
```
pub struct Cache
pub struct CacheRepo
pub struct Repo
pub enum RepoType
impl Cache
  pub fn new(path: PathBuf) → Self
  pub fn path(&self) → &PathBuf
  pub fn token_path(&self) → PathBuf
  pub fn token(&self) → Option<String>
  pub fn repo(&self, repo: Repo) → CacheRepo
  pub fn model(&self, model_id: String) → CacheRepo
  pub fn dataset(&self, model_id: String) → CacheRepo
  pub fn space(&self, model_id: String) → CacheRepo
impl CacheRepo
  pub fn get(&self, filename: &str) → Option<PathBuf>
  pub fn create_ref(&self, commit_hash: &str) → Result<(), std::io::Error>
impl Cache
impl Repo
  pub fn new(repo_id: String, repo_type: RepoType) → Self
  pub fn with_revision(repo_id: String, repo_type: RepoType, revision: String) → Self
  pub fn model(repo_id: String) → Self
  pub fn dataset(repo_id: String) → Self
  pub fn space(repo_id: String) → Self
  pub fn folder_name(&self) → String
  pub fn revision(&self) → &str
```

### .tmp/raios-tray-work/README.md
```
h1 raios-tray
h2 Supported Platforms
h2 Stack
h2 Files
h2 Install
h2 Platform Notes
h3 Linux
h3 macOS
h3 Windows
h2 Runtime Expectations
code-fence bash
code-fence plain
```

### .tmp/raios-tray-work/memory.md
```
h1 Project Memory: raios-tray
h2 Context
h2 Active Objectives
h2 Technical Decisions
h2 Important Links & Paths
h2 Current Focus
h2 Change Log & Agent Trail
```

### .tmp/raios-tray-work/raios_tray.py
```
@dataclass Agent(name, commands)  :85-87
@dataclass TrayState(online, health?, projects?, projects_from_cache, usage?, aiosd_cpu, aiosd_ram_mb, error)  :91-99
class FetchWorker(QObject)  :473-501
  def run() → None
class PathInput(QWidget)  :504-528
  def __init__(initial_value: str, mode: str, parent: QWidget | None)
  def value() → str
  def pick_path() → None
class SettingsDialog(QDialog)  :531-690
  def __init__(parent: QWidget | None)
  def build_config() → dict
  def validate_config(config: dict) → str | None
  def persist() → bool
  def save_only() → None
  def save_and_restart() → None
  def open_config_dir() → None
class ProjectsDialog(QDialog)  :693-768
  def __init__(parent: QWidget | None, projects: list[dict], usage: dict, on_launch: Callable[[str, Agent, str], bool])
  def filter_rows(text: str) → None
class RaiosTray(QObject)  :771-984
  def __init__(app: QApplication)
  def refresh() → None
  def rebuild_menu() → None
  def show_projects_dialog() → None
  def open_settings() → None
```

## tools

### tools/raios-tray/README.md
```
h1 raios-tray
h2 Supported Platforms
h2 Stack
h2 Files
h2 Install
h2 Platform Notes
h3 Linux
h3 macOS
h3 Windows
h2 Runtime Expectations
code-fence bash
code-fence plain
```

### tools/raios-tray/handoff.md
```
h1 raios-tray Codex Handoff Prompt
h2 Kimlik
h2 Proje
h2 Son Mimari Karar
h2 Mevcut Durum
h2 Şu An Beklenen UI
h2 Yakın Zamandaki Önemli Commitler
h2 Kritik Tespit
h2 Servis
h2 Manuel Çalıştırma
h2 Kullanıcıdan Gelen Son Geri Bildirim
h2 Bir Sonraki Agent İçin Hedef
h2 Not
code-fence bash
code-fence plain
```

### tools/raios-tray/memory.md
```
h1 Project Memory: raios-tray
h2 Context
h2 Active Objectives
h2 Technical Decisions
h2 Important Links & Paths
h2 Current Focus
h2 Change Log & Agent Trail
```

### tools/raios-tray/raios-tray.py
```
@dataclass Agent(name, commands)  :153-155
@dataclass TrayState(online, health?, projects?, projects_from_cache, usage?, aiosd_cpu, aiosd_ram_mb, error, dirty_projects, mem_items, tasks, weather_summary)  :159-171
class PathInput(QWidget)  :809-844
  def __init__(initial_value: str, mode: str, parent: QWidget | None)
  def value() → str
  def pick_path() → None
class SettingsDialog(QDialog)  :847-1006
  def __init__(parent: QWidget | None)
  def build_config() → dict
  def validate_config(config: dict) → str | None
  def persist() → bool
  def save_only() → None
  def save_and_restart() → None
  def open_config_dir() → None
class MemoryBrowserDialog(QDialog)  :1009-1152
  def __init__(items: list[dict], parent: QWidget | None)
class QuickAddTaskDialog(QDialog)  :1155-1208
  def __init__(projects: list[dict], parent: QWidget | None)
class TaskListDialog(QDialog)  :1211-1331
  def __init__(tasks: list[dict], projects: list[dict], parent: QWidget | None)
class ProjectEditDialog(QDialog)  :1334-1356
  def __init__(parent: QWidget | None, name: str, path: str)
  def result() → tuple[str, str]
class ProjectManagerDialog(QDialog)  :1359-1618
  def filter_rows(text: str) → None
```

## vscode-extension

### vscode-extension/README.md
```
h1 R-AI-OS VS Code Extension
h2 Features
h2 Requirements
h2 Commands
h2 Configuration
h2 Connection
h2 Development
code-fence bash
code-fence plain
```

### vscode-extension/install.sh
```
# Build and install the R-AI-OS VS Code extension, replacing any
```

### vscode-extension/media/sidebar.css
```
var --bg-color
var --panel-bg
var --panel-border
var --accent-gradient
var --text-main
var --text-muted
var --success-color
var --error-color
var --warning-color
var --gray-color
```

### vscode-extension/pnpm-lock.yaml
```
keys: [lockfileVersion, settings, importers, packages, snapshots]
```

### vscode-extension/src/bridge/JumpToCode.ts
```
export class JumpToCode  :4-29
  constructor(private readonly client)  :5-5
  activate() → void  :7-28
```

### vscode-extension/src/commands/CommandBridge.ts
```
export class CommandBridge  :8-107
  constructor(private readonly client, private readonly outputChannel, private readonly diagnosticProvider)  :9-13
  register(context) → void  :15-63
```

### vscode-extension/src/extension.ts
```
export function activate(context) → void  :21-95
export function deactivate() → void  :97-100
```

### vscode-extension/src/ipc/DaemonClient.ts
```
export class DaemonClient  :8-117
  constructor(private readonly port, private readonly host)  :16-19
  onMessage(handler) → void  :25-27
  connect() → void  :29-81
  send(method, params, unknown> = {}) → void  :83-87
  sendRaw(payload, unknown>) → void  :89-92
  disconnect() → void  :94-99
```

### vscode-extension/src/ipc/DaemonManager.ts
```
export class DaemonManager  :10-91
  isTokenFresh() → boolean  :19-27
  async ensureRunning() → Promise<boolean>  :33-38
  async spawn() → Promise<boolean>  :41-64
  dispose() → void  :87-90
```

### vscode-extension/src/ipc/TokenBridge.ts
```
export class TokenBridge  :7-157
  constructor(private readonly context)  :10-10
  readToken() → string | null  :16-41
  async getToken() → Promise<string | nul  :46-64
  async request(endpoint, method, body?) → Promise<any>  :69-138
  async handleMessage(message, webview) → Promise<void>  :143-157
```

### vscode-extension/src/providers/RefactorTreeProvider.ts
```
export interface RefactorFileData  :4-9
  file: string  :5-5
  severity: "HIGH" | "MEDIUM"  :6-6
  lines: number  :7-7
  reasons: string[]  :8-8
class RefactorNode  :13-22
  constructor(public readonly kind, label, collapsible, public readonly fileData?)  :14-21
```

### vscode-extension/src/utils/raiosBinary.ts
```
export function resolveRaiosBinary() → string  :11-51
export function resolveAiosdBinary() → string | null  :57-88
export function raiosConfigDir() → string  :94-100
export function tokenFilePath() → string  :105-107
```
