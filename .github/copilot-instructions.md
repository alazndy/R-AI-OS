

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.15.0 -->
<!-- Updated: 2026-06-25T13:42:33.825Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
.tmp/raios-tray-work/raios_tray.py ← __future__, PySide6, platform, shlex, psutil
vscode-extension/src/ipc/TokenBridge.ts ← utils/raiosBinary
vscode-extension/src/ipc/DaemonClient.ts ← utils/raiosBinary
tools/raios-tray/raios-tray.py ← __future__, PySide6, platform, shlex, psutil
```

## todos
```
src/setup_wizard.rs:600  # TODO: implement later`. Always full, compilable context.
```

## .tmp

### .tmp/raios-tray-work/raios_tray.py
```
@dataclass Agent  :85-87
@dataclass TrayState  :91-99
class FetchWorker  :473-501
  def run() → None
class PathInput  :504-528
  def __init__(initial_value: str, mode: str, parent: QWidget | None)
  def value() → str
  def pick_path() → None
class SettingsDialog  :531-690
  def __init__(parent: QWidget | None)
  def build_config() → dict
  def validate_config(config: dict) → str | None
  def persist() → bool
  def save_only() → None
  def save_and_restart() → None
  def open_config_dir() → None
class ProjectsDialog  :693-768
  def __init__(parent: QWidget | None, projects: list[dict], usage: dict, on_launch: Callable[[str, Agent, str], bool])
  def filter_rows(text: str) → None
class RaiosTray  :771-984
  def __init__(app: QApplication)
  def refresh() → None
  def rebuild_menu() → None
  def show_projects_dialog() → None
  def open_settings() → None
```

### .tmp/raios-tray-work/README.md
```
h1 raios-tray
h2 Supported Platforms
h2 Stack
h2 Files
h2 Install
h2 Platform Notes
h3 Linux
h3 macOS
h3 Windows
h2 Runtime Expectations
code-fence bash
code-fence plain
```

### .tmp/raios-tray-work/memory.md
```
h1 Project Memory: raios-tray
h2 Context
h2 Active Objectives
h2 Technical Decisions
h2 Important Links & Paths
h2 Current Focus
h2 Change Log & Agent Trail
```

## src

### src/security/patterns.rs
```
pub fn scan_file(path: &Path) → Vec<SecurityIssue>
```

### src/security/policy.rs
```
pub struct PolicyConfig
pub struct ServerPolicy
pub struct EgressPolicy
pub struct FilesystemPolicy
pub struct ToolsPolicy
pub struct ToolRule
pub enum PolicyAction
impl PolicyAction
  pub fn as_str(&self) → &'static str
impl PolicyConfig
  pub fn load_from_file(path: &Path) → Result<Self>
  pub fn tool_action(&self, tool_name: &str) → &PolicyAction
  pub fn validate_tool_call(&self, tool_name: &str) → Result<()>
  pub fn needs_confirmation(&self, tool_name: &str) → bool
  pub fn try_load_default() → Option<Self>
```

### src/security/quarantine.rs
```
pub struct QuarantineConfig
pub struct QuarantineItem
pub struct QuarantineStore
pub enum QuarantineError
impl QuarantineStore
  pub fn new(config: QuarantineConfig) → Self
  pub fn disabled() → Self
  pub fn from_policy(config: Option<QuarantineConfig>) → Self
  pub fn is_enabled(&self) → bool
  pub fn check(&self, conn: &Connection, tool: &str, args_json: &str,) → Result<(), QuarantineError>
impl QuarantineError
pub fn ensure_table(conn: &Connection) → rusqlite::Result<()>
pub fn list_pending(conn: &Connection) → rusqlite::Result<Vec<Quaran...
pub fn approve(conn: &Connection, id: &str) → rusqlite::Result<bool>
pub fn deny(conn: &Connection, id: &str) → rusqlite::Result<bool>
pub fn clear(conn: &Connection, id: &str) → rusqlite::Result<bool>
pub fn list_all(conn: &Connection) → rusqlite::Result<Vec<Quaran...
```

### src/security/rate_limiter.rs
```
pub struct RateLimitConfig
pub struct ToolRateRule
pub struct RateLimiter
pub struct RateLimitError
pub struct ToolRateStatus
impl RateLimitConfig
impl RateLimitError
impl RateLimiter
  pub fn new(config: RateLimitConfig) → Self
  pub fn disabled() → Self
  pub fn from_policy(config: Option<RateLimitConfig>) → Self
  pub fn check(&mut self, tool: &str) → Result<(), RateLimitError>
  pub fn status(&self) → Vec<ToolRateStatus>
  pub fn is_enabled(&self) → bool
```

### src/security/sandbox.rs
```
pub struct SandboxGuard
impl SandboxGuard
  pub fn new(workspace: PathBuf) → Self
  pub fn with_blocked_paths(mut self, paths: Vec<String>) → Self
  pub fn check(&self, target: &Path) → Result<PathBuf>
  pub fn is_allowed(&self, target: &Path) → bool
pub fn validate_path(target: &Path, workspace: &Path) → Result<PathBuf>
pub fn is_path_safe(target: &Path, workspace: &Path) → bool
```

### src/security/scanner.rs
```
pub fn scan_project(path: &Path) → SecurityReport
```

### src/security/secret_lease.rs
```
pub struct SecretLease
pub enum SecretLeaseError
impl SecretLeaseError
pub fn parse_ttl(s: &str) → Result<u64, String>
pub fn ensure_table(conn: &Connection) → rusqlite::Result<()>
pub fn grant(conn: &Connection, tool: &str, env_var: &str, ttl_secs: u64,) → rusqlite::Result<String>
pub fn revoke(conn: &Connection, id: &str) → rusqlite::Result<bool>
pub fn expire_old(conn: &Connection) → rusqlite::Result<usize>
pub fn list_active(conn: &Connection) → rusqlite::Result<Vec<Secret...
pub fn list_all(conn: &Connection) → rusqlite::Result<Vec<Secret...
pub fn active_env_for_tool(conn: &Connection, tool: &str) → Vec<(String, String)>
```

### src/security/tool_pin.rs
```
pub enum ToolPinError
impl ToolPinError
pub fn hash_manifest(manifest_json: &str) → String
pub fn verify_or_pin(manifest_json: &str) → Result<bool, ToolPinError>
pub fn reset_pin() → Result<(), ToolPinError>
pub fn current_pin() → Option<String>
```

### src/ui/health.rs
```
pub fn render_diagnostics(frame: &mut Frame, area: Rect, app: &App)
pub fn render_health_view(frame: &mut Frame, app: &App)
pub fn render_system_audit(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/panels/policies.rs
```
pub fn render_policies(frame: &mut Frame, area: Rect, app: &App)
```

### src/swarm/mod.rs
```
pub struct SwarmTask
pub enum SwarmStatus
```

### src/task_graph.rs
```
pub struct NodeSpec
pub struct GraphNode
pub struct TaskGraph
pub struct GraphStore
pub enum NodeStatus
impl NodeStatus
impl GraphStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn create(&self, goal: &str, agent: &str, nodes: Vec<NodeSpec>) → Result<String>
  pub fn get(&self, graph_id: &str) → Option<TaskGraph>
```

### src/server/http.rs
```
pub async fn start_http_server(port: u16, daemon_state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>,) → anyhow::Result<()>
```

### src/mcp/mod.rs
```
impl RpcResponse
impl McpServer
pub fn run_stdio() → Result<()>
```

### src/mcp/tools_workspace.rs
```
impl McpServer
```

### src/daemon/state.rs
```
pub struct SentinelFileStatus
pub struct FileChangeApproval
pub struct ValidationError
pub struct PendingDiff
pub struct DaemonState
impl DaemonState
  pub fn new() → Arc<RwLock<Self>>
  pub fn refresh_pending_from_db(&mut self)
  pub fn sync_payload(&self) → serde_json::Value
```

### src/mempalace.rs
```
pub struct MemProject
pub struct MemRoom
pub fn build(dev_ops: &Path) → Vec<MemRoom>
```

### src/entities.rs
```
pub struct EntityProject
pub fn load_entities(dev_ops: &Path) → Vec<EntityProject>
pub fn save_entities(_dev_ops: &Path, projects: Vec<EntityProject>) → std::io::Result<()>
pub fn discover_entities(dev_ops: &Path) → Vec<EntityProject>
```

### src/app/ipc.rs
```
pub fn connect_daemon(tx: Sender<BgMsg>) → Option<Sender<String>>
```

### src/daemon/health.rs
```
pub async fn start_health_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration,)
```

### src/daemon/git.rs
```
pub async fn start_git_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration,)
```

### src/daemon/sentinel.rs
```
pub async fn start_sentinel_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration,)
```

### src/daemon/lifecycle.rs
```
pub async fn start_lifecycle_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration, standby_days: u64, archive_days: u64,)
```

### src/daemon/server.rs
```
pub struct Server
impl Server
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub async fn run_with_tx(&self, tx: broadcast::Sender<String>) → anyhow::Result<()>
  pub async fn run(&self) → anyhow::Result<()>
```

### src/app/events/commands.rs
```
impl App
```

### src/requirements.rs
```
pub struct Requirement
pub fn check_requirements() → Vec<Requirement>
```

### src/config.rs
```
pub struct DaemonConfig
pub struct Config
pub struct DetectResult
impl DaemonConfig
impl Config
impl Config
  pub fn config_file() → PathBuf
  pub fn load() → Option<Self>
  pub fn save(&self) → Result<()>
  pub fn auto_detect() → DetectResult
  pub fn from_detect_result(detected: DetectResult) → Self
```

### src/sync.rs
```
pub fn sync_universe(dev_ops: &Path, master: &Path) → Result<String>
```

### src/new_project.rs
```
pub struct NewProjectConfig
pub struct NewProjectResult
pub fn create(cfg: &NewProjectConfig) → NewProjectResult
```

### src/kernel.rs
```
pub struct Kernel
impl Kernel
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub async fn run(&self) → Result<()>
impl McpTcpSession
```

### src/intelligence/router.rs
```
pub struct AgentRouter
impl AgentRouter
impl AgentRouter
  pub fn new() → Self
  pub fn init() → anyhow::Result<Self>
  pub fn route(&self, task: &str) → anyhow::Result<Option<String>>
pub fn route_capability(query: &str) → Option<String>
```

### src/mcp/tools.rs
```
impl McpServer
```

### src/factory.rs
```
pub struct Job
pub struct Factory
pub enum JobStatus
impl JobStatus
impl Job
  pub fn new(description: &str, agent: &str, project: Option<&str>, webhook_url: Option<&str>,) → Self
impl Factory
  pub fn new(tx: broadcast::Sender<String>) → Self
  pub fn submit(&self, job: Job, task: TaskFn) → Uuid
  pub fn get(&self, id: &Uuid) → Option<Job>
  pub fn list_inbox(&self, limit: usize) → Vec<Job>
```

### src/swarm/store.rs
```
pub struct SwarmStore
impl SwarmStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn create(&self, project_name: &str, project_path: &Path, description: &str, agent: &str,) → Result<SwarmTask>
  pub fn get(&self, id: &str) → Option<SwarmTask>
```

### src/proxy_store.rs
```
pub struct Capability
pub struct CapabilityStore
pub struct CapabilityProxy
pub enum Backend
impl CapabilityStore
  pub fn new() → Self
  pub fn register(&mut self, cap: Capability)
  pub fn get(&self, name: &str) → Option<&Capability>
  pub fn list(&self) → Vec<&Capability>
impl CapabilityProxy
  pub fn new(store: CapabilityStore) → Self
  pub fn execute(&self, capability: &str, input: &str) → Result<String>
  pub fn route(&self, query: &str, input: &str) → Result<String>
  pub fn store(&self) → &CapabilityStore
```

### src/radar.rs
```
pub struct Whisper
pub struct RadarChannel
pub enum WhisperKind
pub enum Severity
impl WhisperKind
  pub fn label(&self) → &'static str
impl Whisper
  pub fn compile_error(project: &str, file: PathBuf, line: u32, message: &str) → Self
  pub fn security_vuln(project: &str, file: Option<PathBuf>, message: &str, cve: Option<&str>,) → Self
  pub fn arch_violation(project: &str, file: PathBuf, message: &str, hint: Option<&str>) → Self
  pub fn lock_conflict(project: &str, resource: &str, owner: &str) → Self
  pub fn codebase_rule(project: &str, rule: &str, detail: &str) → Self
impl RadarChannel
  pub fn new(tx: broadcast::Sender<String>) → Self
  pub fn emit(&self, whisper: Whisper)
  pub fn emit_many(&self, whispers: impl IntoIterator<Item = Whisper>)
```

### src/app/mod.rs
```
pub struct PaletteItem
pub struct App
impl App
impl App
  pub fn new() → Self
  pub fn tick(&mut self)
  pub fn current_menu_files(&self) → Vec<FileEntry>
  pub fn sorted_project_indices(&self) → Vec<usize>
pub fn filtered_palette(query: &str) → Vec<&'static PaletteItem>
```

### src/app/state.rs
```
pub struct Activity
pub struct LogEntry
pub struct SetupField
pub struct PortfolioStats
pub struct RuleCategory
pub struct SearchState
pub struct MempalaceState
pub struct HealthState
pub struct TaskState
pub struct SystemState
pub struct WizardState
pub struct SetupState
pub struct InventoryState
pub struct UIState
pub struct TimelineState
pub struct ProjectState
pub struct EditorState
pub enum AppState
pub enum BgMsg
pub enum SortMode
impl SetupField
  pub fn new(label: &'static str, hint: &'static str) → Self
  pub fn with_detected(mut self, path: Option<PathBuf>) → Self
impl SortMode
  pub fn next(&self) → Self
```

### src/app/events/actions.rs
```
impl App
```

### src/app/events/keyboard/setup.rs
```
impl App
```

### src/ui/setup.rs
```
pub fn render_setup(frame: &mut Frame, app: &App)
```

### src/system_scan.rs
```
pub struct SystemAiTool
pub struct UsageSnapshot
pub struct AiAuditReport
pub enum ToolStatus
pub enum UsageConfidence
pub enum UsageSource
impl UsageSnapshot
pub fn scan_system() → AiAuditReport
```

### src/filebrowser.rs
```
pub struct FileEntry
pub struct AgentRuleGroup
pub struct RecentProject
pub struct GitCommitResult
impl FileEntry
  pub fn new(name: impl Into<String>, path: impl Into<PathBuf>) → Self
  pub fn readonly(mut self) → Self
  pub fn exists(&self) → bool
impl AgentRuleGroup
  pub fn exists(&self) → bool
pub fn discover_all_agent_rules(dev_ops: &Path) → Vec<AgentRuleGroup>
pub fn get_master_rule_files(master_md: &Path) → Vec<FileEntry>
pub fn get_agent_config_files() → Vec<FileEntry>
pub fn get_policy_files() → Vec<FileEntry>
pub fn get_mempalace_files(dev_ops: &Path) → Vec<FileEntry>
pub fn discover_memory_files(base: &Path, limit: usize) → Vec<FileEntry>
pub fn load_recent_projects(base: &Path) → Vec<RecentProject>
pub fn load_file_content(path: &PathBuf) → String
pub fn save_file_content(path: &Path, content: &str) → std::io::Result<()>
pub fn find_file_by_name(query: &str, master_md: &Path) → Option<FileEntry>
pub fn git_get_remote_url(dir: &Path) → Option<String>
pub fn git_is_dirty(dir: &Path) → Option<bool>
pub fn get_git_log(dir: &Path) → Vec<String>
pub fn git_commit(dir: &Path, msg: &str) → GitCommitResult
pub fn git_push(dir: &Path) → Result<(), String>
```

### src/tasks.rs
```
pub struct Task
impl Task
  pub fn display(&self) → &str
  pub fn is_persisted(&self) → bool
  pub fn from_personal_row(r: crate::db::PersonalTaskRow) → Self
  pub fn agent_label(&self) → Option<&str>
pub fn parse_task_line(line: &str) → Option<Task>
pub fn load_tasks(dev_ops: &Path) → Result<Vec<Task>>
pub fn save_tasks(dev_ops: &Path, tasks: &[Task]) → Result<()>
pub fn dispatch_to_agent(task: &Task, agent: &str, project_path: Option<&PathBuf>, sentinel_errors: Option<Vec<String>>,) → String
```

### src/ui/panels/tasks.rs
```
pub fn render_tasks_panel(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/components.rs
```
pub fn render_boot(frame: &mut Frame, app: &App)
pub fn render_bouncing_alert(frame: &mut Frame, app: &App)
pub fn render_launcher(frame: &mut Frame, area: Rect, app: &App)
pub fn render_command_palette(frame: &mut Frame, app: &App)
pub fn render_file_changed_badge(frame: &mut Frame, _app: &App)
pub fn render_launcher_modal(frame: &mut Frame, app: &App)
pub fn center_rect(width: u16, height: u16, parent: Rect) → Rect
pub fn render_handover_modal(frame: &mut Frame, app: &App)
```

### src/app/events/keyboard/dashboard.rs
```
impl App
```

### src/app/events/keyboard/mod.rs
```
impl App
  pub fn handle_key(&mut self, key: KeyEvent) → Result<()>
```

### src/app/events/keyboard/project.rs
```
impl App
```

### src/discovery.rs
```
pub struct AgentInfo
pub struct SkillInfo
impl AgentInfo
  pub fn exists(&self) → bool
pub fn discover_agents() → Vec<AgentInfo>
pub fn discover_skills(skills_path: &Path) → Vec<SkillInfo>
pub fn open_in_editor(path: &Path) → anyhow::Result<()>
```

### src/setup_wizard.rs
```
pub struct AgentStatus
pub struct WizardAction
pub enum WizardStep
impl WizardStep
  pub fn next(&self) → Self
  pub fn index(&self) → usize
  pub fn total() → usize
  pub fn title(&self) → &'static str
impl WizardAction
pub fn detect_agents() → AgentStatus
pub fn exec_workspace(dev_ops: &Path, github_user: &str) → Vec<WizardAction>
pub fn exec_master(master_path: &Path, github_user: &str) → Vec<WizardAction>
pub fn exec_claude(dev_ops: &Path, master_path: &Path) → Vec<WizardAction>
pub fn exec_codex(master_path: &Path) → Vec<WizardAction>
pub fn exec_opencode() → Vec<WizardAction>
pub fn exec_skills(dev_ops: &Path) → Vec<WizardAction>
pub fn exec_initialize(dev_ops: &Path, master_path: &Path, skills_path: &Path, vault_path: Option<&Path>,) → Vec<WizardAction>
```

### src/session.rs
```
pub struct Session
pub struct SessionEvent
pub struct SessionStore
impl SessionStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn start(&self, agent: &str, project: Option<&str>) → String
  pub fn end(&self, id: &str, summary: Option<&str>)
  pub fn record_event(&self, session_id: &str, event_type: &str, data: &str)
  pub fn get(&self, id: &str) → Option<Session>
  pub fn events(&self, session_id: &str) → Vec<SessionEvent>
  pub fn current_open(&self) → Option<Session>
```

### src/control_plane.rs
```
pub struct ControlTask
pub struct AgentRun
pub struct Artifact
pub struct Approval
pub struct BudgetLedgerEntry
pub struct FileChangeWorkflowIds
pub struct HandoffWorkflowIds
pub struct HandoffContext
pub enum TaskStatus
pub enum AgentRunStatus
pub enum ArtifactKind
pub enum ArtifactStatus
pub enum ApprovalType
pub enum ApprovalStatus
pub enum BudgetConfidence
```

### src/cli/mod.rs
```
pub struct Cli
pub enum InstinctCmd
pub enum Commands
pub enum HandoffTarget
pub enum HandoffStatus
pub enum QuarantineAction
pub enum SecretAction
pub enum SwarmAction
pub enum EvolveAction
pub enum GitCommands
impl HandoffTarget
  pub fn as_str(&self) → &'static str
impl HandoffStatus
  pub fn as_str(&self) → &'static str
pub fn run(cli: Cli)
pub fn run_refactor_flag(json: bool)
```

### src/lock_manager.rs
```
pub struct LockStatus
pub struct LockManager
pub enum LockPriority
pub enum LockKey
impl LockKey
impl LockEntry
impl LockManager
  pub fn new() → Self
  pub async fn acquire(&self, key: LockKey, owner: impl Into<String>, priority: LockPriority, timeout: Option<Duration>,) → Result<()>
  pub async fn release(&self, key: &LockKey, caller: &str, caller_priority: LockPriority,) → Result<()>
  pub async fn is_locked(&self, key: &LockKey) → bool
  pub async fn list(&self) → Vec<LockStatus>
  pub async fn sweep_expired(&self) → usize
impl LockManager
pub fn spawn_sweeper(manager: LockManager, interval: Duration)
```

### src/agent_runner.rs
```
pub fn run_agent(agent: &str, project_dir: Option<String>, timeout_secs: Option<u64>,) → Result<(), String>
```

### src/db.rs
```
pub struct DbProject
pub struct PortfolioStats
pub struct DbTask
pub struct PersonalTaskInput
pub struct PersonalTaskRow
pub struct ProviderCapabilities
pub struct RunContract
pub struct RunContractBuilder
pub struct SchedulerTask
pub struct UnifiedTaskRow
pub struct ApprovalInboxRow
pub struct RunOverviewRow
pub struct DaemonSnapshot
pub struct DriftReport
pub struct ControlGraphNodeRow
pub struct FileChangeApprovalData
pub enum BudgetGate
pub enum ProviderFailureKind
impl BudgetGate
  pub fn is_blocked(&self) → bool
impl ProviderCapabilities
  pub fn seed_known() → Vec<Self>
impl ProviderFailureKind
  pub fn as_str(&self) → &str
  pub fn from_stored(s: &str) → Self
```

## tools

### tools/raios-tray/README.md
```
h1 raios-tray
h2 Supported Platforms
h2 Stack
h2 Files
h2 Install
h2 Platform Notes
h3 Linux
h3 macOS
h3 Windows
h2 Runtime Expectations
code-fence bash
code-fence plain
```

### tools/raios-tray/memory.md
```
h1 Project Memory: raios-tray
h2 Context
h2 Active Objectives
h2 Technical Decisions
h2 Important Links & Paths
h2 Current Focus
h2 Change Log & Agent Trail
```

### tools/raios-tray/raios-tray.py
```
@dataclass Agent  :89-91
@dataclass TrayState  :95-104
class PathInput  :608-643
  def __init__(initial_value: str, mode: str, parent: QWidget | None)
  def value() → str
  def pick_path() → None
class SettingsDialog  :646-805
  def __init__(parent: QWidget | None)
  def build_config() → dict
  def validate_config(config: dict) → str | None
  def persist() → bool
  def save_only() → None
  def save_and_restart() → None
  def open_config_dir() → None
class ProjectEditDialog  :808-830
  def __init__(parent: QWidget | None, name: str, path: str)
  def result() → tuple[str, str]
class ProjectManagerDialog  :833-1088
  def filter_rows(text: str) → None
  def add_project() → None
  def edit_project(name: str) → None
  def remove_project(name: str) → None
  def toggle_pin(name: str) → None
class RaiosTray  :1091-1344
  def __init__(app: QApplication)
```

### tools/raios-tray/handoff.md
```
h1 raios-tray Codex Handoff Prompt
h2 Kimlik
h2 Proje
h2 Son Mimari Karar
h2 Mevcut Durum
h2 Şu An Beklenen UI
h2 Yakın Zamandaki Önemli Commitler
h2 Kritik Tespit
h2 Servis
h2 Manuel Çalıştırma
h2 Kullanıcıdan Gelen Son Geri Bildirim
h2 Bir Sonraki Agent İçin Hedef
h2 Not
code-fence bash
code-fence plain
```

## vscode-extension

### vscode-extension/install.sh
```
# Build and install the R-AI-OS VS Code extension, replacing any
```

### vscode-extension/media/sidebar.css
```
var --bg-color
var --panel-bg
var --panel-border
var --accent-gradient
var --text-main
var --text-muted
var --success-color
var --error-color
var --warning-color
var --gray-color
```

### vscode-extension/README.md
```
h1 R-AI-OS VS Code Extension
h2 Features
h2 Requirements
h2 Commands
h2 Configuration
h2 Connection
h2 Development
code-fence bash
code-fence plain
```

### vscode-extension/src/utils/raiosBinary.ts
```
export function resolveRaiosBinary  :11-51
export function resolveAiosdBinary  :57-88
export function raiosConfigDir  :94-100
export function tokenFilePath  :105-107
```

### vscode-extension/src/ipc/TokenBridge.ts
```
export class TokenBridge  :7-157
constructor  :10-10
readToken  :16-41
async getToken  :46-64
async request  :69-138
async handleMessage  :143-157
```

### vscode-extension/src/ipc/DaemonClient.ts
```
export class DaemonClient  :8-117
constructor  :16-19
onMessage  :25-27
connect  :29-81
send  :83-87
sendRaw  :89-92
disconnect  :94-99
```
