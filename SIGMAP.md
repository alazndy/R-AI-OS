

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.15.0 -->
<!-- Updated: 2026-07-01T09:41:22.128Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
vscode-extension/src/commands/CommandBridge.ts ← ipc/DaemonClient, utils/raiosBinary, providers/DiagnosticProvider
tools/raios-tray/raios-tray.py ← __future__, PySide6, gi, platform, shlex
```

## changes (last 10 commits — 12 hours ago)
```
src/ui/setup/steps/action.rs                  +olarak
.github/copilot-instructions.md               +Agent  +TrayState  +PathInput  +__init__
```

## .github

### .github/copilot-instructions.md
```
h2 Auto-generated signatures
h2 SigMap commands
h1 Code signatures
h2 deps
h2 changes (last 10 commits — 9 minutes ago)
h2 .github
h3 .github/copilot-instructions.md
h2 src
h3 src/ui/panels/inbox.rs
h3 src/security/umai.rs
h3 src/mcp/tools.rs
h3 src/mcp/mod.rs
h3 src/daemon/proxy.rs
h3 src/mcp/tools_workspace.rs
h3 src/security/scanner.rs
h3 src/health.rs
h3 src/daemon/health.rs
h3 src/daemon/scheduler.rs
h3 src/cli/cron.rs
h3 src/intelligence/edge.rs
h3 src/ui/panels/scheduler.rs
h3 src/agent_wrapper.rs
h3 src/cli/agent_wrapper.rs
h3 src/config.rs
h3 src/app/events/actions.rs
```

## src

### src/ui/panels/inbox.rs
```
pub fn render_inbox(frame: &mut Frame, area: Rect, app: &App)
```

### src/security/umai.rs
```
pub struct Umai
pub enum UmaiDecision
impl UmaiDecision
  pub fn is_allowed(&self) → bool
  pub fn into_error_json(self) → serde_json::Value
impl Umai
  pub fn new(policy: Option<PolicyConfig>) → Self
  pub fn from_default_policy() → Self
  pub fn check(&self, command: &str, payload: Option<&str>) → UmaiDecision
```

### src/mcp/tools.rs
```
impl McpServer
```

### src/mcp/mod.rs
```
impl RpcResponse
impl McpServer
pub fn run_stdio() → Result<()>
```

### src/daemon/proxy.rs
```
pub struct AgentProcess
pub struct ExecutionProxy
impl ExecutionProxy
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub fn with_event_tx(mut self, tx: tokio::sync::broadcast::Sender<String>) → Self
  pub async fn spawn_agent(&self, agent_name: &str, project_path: &str, timeout_secs: u64,) → Result<String>
```

### src/mcp/tools_workspace.rs
```
impl McpServer
```

### src/security/scanner.rs
```
pub fn scan_project(path: &Path) → SecurityReport
pub fn scan_project_fast(path: &Path) → SecurityReport
```

### src/health.rs
```
pub struct ProjectHealth
pub fn check_project_fast(proj: &EntityProject) → ProjectHealth
pub fn check_project(proj: &EntityProject) → ProjectHealth
pub fn check_build_test_deps(h: &mut ProjectHealth)
pub fn check_project_with_security(proj: &EntityProject) → ProjectHealth
pub fn check_graphify(path: &Path) → (bool, Option<PathBuf>)
pub fn find_graphify_script(dev_ops: &Path) → Option<PathBuf>
pub fn validate_file(path: &Path, proj: &EntityProject) → Vec<ValidationError>
```

### src/daemon/health.rs
```
pub async fn start_health_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration,)
```

### src/daemon/scheduler.rs
```
pub async fn start_scheduler_worker(_state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, check_interval: Duration,)
```

### src/cli/cron.rs
```
pub fn cmd_cron(action: CronAction, json: bool)
```

### src/intelligence/edge.rs
```
pub struct EdgeRouter
impl EdgeRouter
  pub fn new(descriptions: Vec<(String, String) → Self
  pub fn route(&self, query: &str) → Option<&str>
```

### src/ui/panels/scheduler.rs
```
pub fn render_scheduler(frame: &mut Frame, area: Rect, app: &App)
```

### src/agent_wrapper.rs
```
pub struct WrapperResult
pub struct AgentShimStatus
impl WrapperResult
pub fn detect_rc_paths() → Vec<PathBuf>
pub fn install(agents: &[&str]) → Vec<WrapperResult>
pub fn remove(filter: Option<&[&str]>) → Vec<WrapperResult>
pub fn status() → Vec<AgentShimStatus>
```

### src/cli/agent_wrapper.rs
```
pub enum AgentWrapperAction
pub fn cmd_agent_wrapper(action: AgentWrapperAction, json: bool)
```

### src/config.rs
```
pub struct DaemonConfig
pub struct Config
pub struct DetectResult
impl DaemonConfig
impl Config
impl Config
  pub fn config_file() → PathBuf
  pub fn load() → Option<Self>
  pub fn save(&self) → Result<()>
  pub fn auto_detect() → DetectResult
  pub fn from_detect_result(detected: DetectResult) → Self
```

### src/app/events/actions.rs
```
impl App
```

### src/app/events/keyboard/setup.rs
```
impl App
```

### src/security/policy.rs
```
pub struct PolicyConfig
pub struct ServerPolicy
pub struct HubPolicy
pub struct EgressPolicy
pub struct FilesystemPolicy
pub struct ToolsPolicy
pub struct ToolRule
pub enum PolicyAction
impl HubPolicy
impl HubPolicy
impl PolicyAction
  pub fn as_str(&self) → &'static str
impl PolicyConfig
  pub fn load_from_file(path: &Path) → Result<Self>
  pub fn tool_action(&self, tool_name: &str) → &PolicyAction
  pub fn validate_tool_call(&self, tool_name: &str) → Result<()>
  pub fn needs_confirmation(&self, tool_name: &str) → bool
  pub fn try_load_default() → Option<Self>
```

### src/security/mod.rs
```
pub struct SecurityIssue
pub struct SecurityReport
pub enum Severity
pub enum ProjectType
impl Severity
  pub fn label(&self) → &'static str
  pub fn deduction(&self) → i32
impl SecurityReport
  pub fn grade_from_score(score: u8) → &'static str
  pub fn critical_count(&self) → usize
  pub fn high_count(&self) → usize
pub fn score_color(score: u8) → &'static str
pub fn severity_emoji(s: &Severity) → &'static str
```

### src/kernel.rs
```
pub struct Kernel
impl Kernel
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub async fn run(&self) → Result<()>
impl McpTcpSession
```

### src/cli/hub.rs
```
pub fn cmd_start(json: bool)
pub fn cmd_stop(json: bool)
pub fn cmd_status(json: bool)
pub fn cmd_install(enable: bool, json: bool)
pub fn cmd_logs(lines: usize)
pub fn cmd_api_key_show()
pub fn cmd_api_key_generate(force: bool)
```

### src/ui/panels/header.rs
```
pub fn render_header(frame: &mut Frame, area: Rect, app: &App)
```

### src/cli/reflect.rs
```
pub fn cmd_reflect(dev_ops_path: &Path, json: bool)
```

### src/app/ipc.rs
```
pub fn connect_daemon(tx: Sender<BgMsg>) → Option<Sender<String>>
pub fn connect_daemon_addr(tx: Sender<BgMsg>, remote_host: Option<String>) → Option<Sender<String>>
```

### src/app/state.rs
```
pub struct ExtCmdInfo
pub struct ExtConfigField
pub struct ExtensionInfo
pub struct ExtState
pub struct Activity
pub struct LogEntry
pub struct SetupField
pub struct PortfolioStats
pub struct RuleCategory
pub struct SearchState
pub struct MempalaceState
pub struct HealthState
pub struct TaskState
pub struct SystemState
pub struct WizardState
pub struct SetupState
pub struct InventoryState
pub struct UIState
pub struct TimelineState
pub struct ProjectState
pub struct EditorState
pub enum ExtFocus
pub enum AppState
pub enum BgMsg
pub enum SortMode
```

### src/app/events/bg_messages.rs
```
impl App
  pub fn handle_bg_msg(&mut self, msg: BgMsg)
```

### src/ui/panels/content.rs
```
pub fn render_content(frame: &mut Frame, area: Rect, app: &App)
pub fn render_content_body(frame: &mut Frame, area: Rect, app: &App)
```

### src/app/events/keyboard/dashboard.rs
```
impl App
```

### src/app/mod.rs
```
pub struct PaletteItem
pub struct App
impl App
impl App
  pub fn new() → Self
  pub fn new_remote(host: String) → Self
  pub fn tick(&mut self)
pub fn filtered_palette(query: &str) → Vec<&'static PaletteItem>
```

### src/factory.rs
```
pub struct Job
pub struct Factory
pub enum JobStatus
impl JobStatus
impl Job
  pub fn new(description: &str, agent: &str, project: Option<&str>, webhook_url: Option<&str>,) → Self
impl Factory
  pub fn new(tx: broadcast::Sender<String>) → Self
  pub fn submit(&self, job: Job, task: TaskFn) → Uuid
  pub fn get(&self, id: &Uuid) → Option<Job>
```

### src/app/events/commands.rs
```
impl App
```

### src/entities.rs
```
pub struct EntityProject
pub fn load_entities(dev_ops: &Path) → Vec<EntityProject>
pub fn save_entities(_dev_ops: &Path, projects: Vec<EntityProject>) → std::io::Result<()>
pub fn discover_entities(dev_ops: &Path) → Vec<EntityProject>
```

### src/daemon/lifecycle.rs
```
pub async fn start_lifecycle_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration, standby_days: u64, archive_days: u64,)
```

### src/agent_runner.rs
```
pub fn run_agent(agent: &str, project_dir: Option<String>, timeout_secs: Option<u64>, extra_args: Vec<String>,) → Result<(), String>
pub fn spawn_agent_detached(agent: &str, task_prompt: &str, project_dir: Option<&str>,) → Result<u32, String>
```

### src/session_memory.rs
```
pub fn find_latest_conversation(project_path: &str, min_mtime: Option<SystemTime>,) → Option<PathBuf>
pub fn extract_transcript(jsonl_path: &Path) → String
pub fn generate_memory_entry(transcript: &str) → Option<String>
pub fn append_to_memory_md(project_path: &str, entry: &str) → std::io::Result<()>
pub fn post_session_memory_prompt(project_path: &str, session_started: SystemTime)
pub fn cmd_memory_gen(project: Option<&str>, json: bool)
pub fn auto_sync_agent_memory(agent: &str, project_path: &str, session_started: SystemTime, verbose: bool,)
pub fn auto_sync_claude_memory(project_path: &str, session_started: SystemTime)
```

### src/db/control_plane.rs
```
pub struct SchedulerTask
pub struct UnifiedTaskRow
pub struct ApprovalInboxRow
pub struct RunOverviewRow
pub struct DaemonSnapshot
pub struct DriftReport
pub fn cp_scheduler_list_ready(conn: &Connection) → Result<Vec<SchedulerTask>>
pub fn cp_task_graph_node_ids(conn: &Connection, task_id: &str) → Result<Option<(String, Stri...
pub fn cp_task_graph_shell_cmd(conn: &Connection, task_id: &str) → Result<Option<String>>
pub fn cp_query_active_tasks(conn: &Connection) → Result<Vec<UnifiedTaskRow>>
pub fn cp_query_blocked_tasks(conn: &Connection) → Result<Vec<UnifiedTaskRow>>
pub fn cp_query_pending_approvals(conn: &Connection) → Result<Vec<ApprovalInboxRow>>
pub fn cp_query_active_runs(conn: &Connection) → Result<Vec<RunOverviewRow>>
pub fn cp_daemon_snapshot(conn: &Connection) → Result<DaemonSnapshot>
pub fn cp_detect_graph_cache_drift(conn: &Connection, graph_id: &str) → Result<DriftReport>
pub fn cp_rebuild_task_graph_cache(conn: &Connection) → Result<usize>
```

### src/cli/task_update.rs
```
pub fn run_refactor_flag(json: bool)
```

### src/db/wf_handoff.rs
```
pub fn create_handoff_workflow(conn: &Connection, project_path: &str, from_agent: &str, to_agent: &str, status: &str, msg: &str, diff_stat: Option<&str>,) → Result<crate::control_plane...
pub fn cp_take_pending_handoff(conn: &Connection, project_id: Option<i64>, to_agent: &str,) → Result<Option<crate::contro...
pub fn cp_consume_handoff(conn: &Connection, ctx: &crate::control_plane::HandoffContext, resolved_by: &str,) → Result<()>
```

### src/daemon/cmd/mod.rs
```
pub enum CmdResult
```

### src/daemon/cmd/file.rs
```
pub async fn handle_request_file_change(v: &serde_json::Value, state: &SharedState, sessions: &crate::session::SessionStore, session_id: &str, writer: &mut W,)
pub async fn handle_approve_file_change(v: &serde_json::Value, state: &SharedState,) → super::CmdResult
pub async fn handle_human_approval(v: &serde_json::Value, state: &SharedState, writer: &mut W,)
pub async fn handle_get_pending_diffs(state: &SharedState, writer: &mut W,)
pub async fn handle_approve_diff(v: &serde_json::Value, state: &SharedState, writer: &mut W,) → super::CmdResult
pub async fn handle_reject_diff(v: &serde_json::Value, state: &SharedState, writer: &mut W,)
```

### src/daemon/cmd/jobs.rs
```
pub async fn handle_submit_job(v: &serde_json::Value, factory: &Arc<Factory>, writer: &mut W,)
pub async fn handle_get_job(v: &serde_json::Value, factory: &Arc<Factory>, writer: &mut W,)
pub async fn handle_list_inbox(v: &serde_json::Value, factory: &Arc<Factory>, writer: &mut W,)
pub async fn handle_list_running(factory: &Arc<Factory>, writer: &mut W,)
```

### src/daemon/cmd/capability.rs
```
pub async fn handle_execute_capability(v: &serde_json::Value, proxy: &Arc<CapabilityProxy>, writer: &mut W,)
pub async fn handle_route_capability(v: &serde_json::Value, proxy: &Arc<CapabilityProxy>, writer: &mut W,)
pub async fn handle_list_capabilities(proxy: &Arc<CapabilityProxy>, writer: &mut W,)
```

### src/daemon/cmd/task_graph.rs
```
pub async fn handle_create_task_graph(v: &serde_json::Value, graph_store: &Arc<GraphStore>, writer: &mut W,)
pub async fn handle_execute_task_graph(v: &serde_json::Value, graph_store: &Arc<GraphStore>, factory: &Arc<Factory>, writer: &mut W,)
pub async fn handle_get_task_graph(v: &serde_json::Value, graph_store: &Arc<GraphStore>, writer: &mut W,)
```

### src/daemon/cmd/swarm.rs
```
pub async fn handle_create_swarm_task(v: &serde_json::Value, swarm_store: &Arc<SwarmStore>, writer: &mut W,)
pub async fn handle_get_swarm_task(v: &serde_json::Value, swarm_store: &Arc<SwarmStore>, writer: &mut W,)
pub async fn handle_list_swarm_tasks(swarm_store: &Arc<SwarmStore>, writer: &mut W,)
pub async fn handle_approve_swarm_task(v: &serde_json::Value, swarm_store: &Arc<SwarmStore>, writer: &mut W,)
pub async fn handle_reject_swarm_task(v: &serde_json::Value, swarm_store: &Arc<SwarmStore>, writer: &mut W,)
```

### src/daemon/cmd/evolution.rs
```
pub async fn handle_list_instinct_candidates(v: &serde_json::Value, writer: &mut W,)
pub async fn handle_promote_instinct(v: &serde_json::Value, writer: &mut W,)
pub async fn handle_list_evolution_candidates(v: &serde_json::Value, evolution_store: &Arc<CandidateStore>, writer: &mut W,)
pub async fn handle_promote_evolution_candidate(v: &serde_json::Value, evolution_store: &Arc<CandidateStore>, writer: &mut W,)
pub async fn handle_prune_expired_candidates(evolution_store: &Arc<CandidateStore>, writer: &mut W,)
```

### src/daemon/handlers.rs
```
pub struct ClientHandle
pub async fn handle_client_connection(h: ClientHandle)
```

### src/db/budget_gate.rs
```
pub enum BudgetGate
impl BudgetGate
  pub fn is_blocked(&self) → bool
pub fn cp_upsert_budget_ledger(conn: &Connection, scope_kind: &str, scope_id: &str, provider: Option<&str>, metric: &str, limit_value: Option<f64>, used_value: Option<f64>, remaining_value: Option<f64>, confidence: &str, source: &str,) → Result<()>
pub fn cp_check_provider_budget_gate(conn: &Connection, provider: &str) → Result<BudgetGate>
pub fn cp_check_contract_budget_gate(conn: &Connection, task_id: &str) → Result<BudgetGate>
```

### src/db/mod.rs
```
pub fn open_db() → Result<Connection>
pub fn cp_log_append(conn: &Connection, sender: &str, content: &str) → Result<()>
pub fn cp_logs_replay(conn: &Connection, limit: usize) → Result<Vec<(String, String,...
pub fn migrate_existing(conn: &Connection) → Result<()>
pub fn import_from_json(dev_ops: &Path, conn: &Connection) → usize
```

### src/cli/args.rs
```
pub struct Cli
pub enum InstinctCmd
pub enum Commands
```

### src/cli/mod.rs
```
pub fn run(cli: Cli)
```

### src/ui/sentinel.rs
```
pub fn render_sentinel_hub(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/panels/menu.rs
```
pub fn render_menu(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/panels/logs.rs
```
pub fn render_logs(frame: &mut Frame, area: Rect, app: &App)
```

### src/app/events/ext_actions.rs
```
impl App
  pub fn load_extensions(&mut self)
  pub fn run_ext_cmd(&mut self)
  pub fn save_ext_config_field(&mut self)
```

### src/cli/action_types.rs
```
pub enum HubAction
pub enum HubApiKeyAction
pub enum MemAction
pub enum AgentWrapperCmd
pub enum CronAction
pub enum HandoffTarget
pub enum HandoffStatus
pub enum QuarantineAction
pub enum SecretAction
pub enum SwarmAction
pub enum EvolveAction
pub enum GitCommands
impl HandoffTarget
  pub fn as_str(&self) → &'static str
impl HandoffStatus
  pub fn as_str(&self) → &'static str
```

### src/cli/ext/mod.rs
```
pub fn cmd_ext_list(dev_ops_path: &Path, json: bool)
pub fn cmd_ext(name: &str, ext_args: &[String], dev_ops_path: &Path, json: bool)
```

### src/cli/preflight.rs
```
pub fn cmd_preflight(project: Option<String>, dev_ops_path: &Path) → bool
```

### src/cli/security/owasp.rs
```
pub fn cmd_security(target: Option<String>, full: bool, watch: bool, dev_ops: &Path, json: bool,)
pub fn cmd_security_watch(path: &Path, json: bool) → anyhow::Result<()>
```

### src/cli/security/infra.rs
```
pub fn cmd_quarantine(action: crate::cli::QuarantineAction, json: bool)
pub fn cmd_secret(action: crate::cli::SecretAction, json: bool)
```

### src/cli/security/audit.rs
```
pub fn cmd_license(project: Option<String>, dev_ops: &Path, json_out: bool)
pub fn cmd_verify_chain(last: usize, json: bool)
pub fn cmd_rate_status(json: bool)
pub fn cmd_pin_reset(json: bool)
pub fn cmd_pin_status(json: bool)
```

### src/core/process/command.rs
```
pub fn command_exists(command: &str) → bool
pub fn resolve_command_path(command: &str) → Option<PathBuf>
pub fn python_command() → (String, Vec<String>)
pub fn shell_command(command: &str) → (String, Vec<String>)
pub fn open_in_system_editor(path: &Path) → std::io::Result<()>
pub fn copy_to_clipboard(text: &str) → bool
pub fn launch_in_terminal(command: &str, work_dir: &Path) → bool
```

### src/core/process/kill.rs
```
pub fn kill_port(port: u16) → KillResult
pub fn kill_pid(pid: u32) → KillResult
```

### src/core/process/ports.rs
```
pub fn list_ports() → Vec<PortEntry>
```

### src/core/version/mod.rs
```
pub fn info(dir: &Path) → Option<VersionInfo>
pub fn bump(dir: &Path, bump_type: &BumpType, update_changelog: bool, tag: bool) → BumpResult
pub fn changelog(dir: &Path) → String
```

### src/core/process/processes.rs
```
pub fn list_processes(top_n: usize) → Vec<ProcessEntry>
```

### src/core/process/mod.rs
```
pub struct PortEntry
pub struct ProcessEntry
pub struct KillResult
```

### src/core/version/types.rs
```
pub struct VersionInfo
pub struct BumpResult
pub enum BumpType
impl BumpType
  pub fn parse(s: &str) → Option<Self>
  pub fn label(&self) → &'static str
```

### src/daemon/server.rs
```
pub struct Server
impl Server
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub async fn run_with_tx(&self, tx: broadcast::Sender<String>) → anyhow::Result<()>
  pub async fn run(&self) → anyhow::Result<()>
```

### src/db/health.rs
```
pub struct PortfolioStats
pub fn upsert_health(conn: &Connection, project_id: i64, compliance_grade: &str, compliance_score: Option<u8>, security_grade: Option<&str>, security_score: Option<u8>, security_issues: usize, security_critical: usize, git_dirty: bool, has_memory: bool, has_sigmap: bool, remote_url: Option<&str>, refactor_grade: &str, refactor_score: u8, refactor_high: usize,) → Result<()>
pub fn query_stats(conn: &Connection) → Result<PortfolioStats>
```

### src/db/mem.rs
```
pub struct MemItemRow
pub fn mem_upsert(conn: &Connection, project_key: &str, item_type: &str, slug: &str, title: &str, description: &str, body: &str, session_id: Option<&str>,) → Result<()>
pub fn mem_list(conn: &Connection, project_key: &str) → Result<Vec<MemItemRow>>
pub fn mem_get(conn: &Connection, project_key: &str, slug: &str) → Result<Option<MemItemRow>>
pub fn mem_delete(conn: &Connection, project_key: &str, slug: &str) → Result<bool>
pub fn mem_export(conn: &Connection, project_key: &str, memory_dir: &std::path::Path) → Result<usize>
```

### src/db/tasks.rs
```
pub struct DbTask
pub struct PersonalTaskInput
pub struct PersonalTaskRow
pub fn load_tasks_db(conn: &Connection) → Result<Vec<DbTask>>
pub fn insert_task(conn: &Connection, text: &str, agent: Option<&str>, project: Option<&str>,) → Result<i64>
pub fn toggle_task(conn: &Connection, id: i64, completed: bool) → Result<()>
pub fn cp_list_personal_tasks(conn: &Connection) → Result<Vec<PersonalTaskRow>>
pub fn cp_sync_personal_tasks(conn: &Connection, inputs: &[PersonalTaskInput], source_path: &str,) → Result<()>
pub fn cp_rebuild_personal_markdown(conn: &Connection, dev_ops: &std::path::Path) → Result<()>
```

### src/db/provider.rs
```
pub struct ProviderCapabilities
pub enum ProviderFailureKind
impl ProviderCapabilities
  pub fn seed_known() → Vec<Self>
impl ProviderFailureKind
  pub fn as_str(&self) → &str
  pub fn from_stored(s: &str) → Self
  pub fn classify(error: &str) → Self
pub fn cp_upsert_provider_capabilities(conn: &Connection, caps: &ProviderCapabilities) → Result<()>
pub fn cp_get_provider_capabilities(conn: &Connection, provider: &str,) → Result<Option<ProviderCapab...
pub fn cp_list_provider_capabilities(conn: &Connection) → Result<Vec<ProviderCapabili...
pub fn cp_seed_provider_capabilities(conn: &Connection) → Result<()>
pub fn cp_record_run_failure(conn: &Connection, agent_run_id: &str, kind: &ProviderFailureKind, detail: &str,) → Result<()>
pub fn cp_check_provider_supports(conn: &Connection, provider: &str, needs_tool_calling: bool, needs_patch_diff: bool, needs_long_running: bool,) → Result<bool>
pub fn cp_route_to_capable_provider(conn: &Connection, needs_tool_calling: bool, needs_patch_diff: bool, needs_long_running: bool,) → Result<Option<String>>
```

### src/db/projects.rs
```
pub struct DbProject
pub fn load_all_projects(conn: &Connection) → Result<Vec<DbProject>>
pub fn upsert_project(conn: &Connection, name: &str, category: &str, path: &str, github: Option<&str>, status: &str, stars: Option<i64>, last_commit: Option<&str>, version: Option<&str>, nickname: Option<&str>,) → Result<i64>
pub fn update_project_status(conn: &Connection, path: &str, status: &str) → Result<()>
pub fn project_id_for_path(conn: &Connection, path: &str) → Option<i64>
```

### src/db/scheduler.rs
```
pub struct ScheduledJob
pub fn cp_scheduled_job_create(conn: &Connection, title: &str, agent: &str, task_description: &str, interval_secs: i64,) → Result<String>
pub fn cp_scheduled_jobs_list(conn: &Connection) → Result<Vec<ScheduledJob>>
pub fn cp_scheduled_jobs_claim_due(conn: &Connection) → Result<Vec<ScheduledJob>>
pub fn cp_scheduled_job_mark_fired(conn: &Connection, id: &str, interval_secs: i64) → Result<()>
pub fn cp_scheduled_job_revert_firing(conn: &Connection, id: &str) → Result<()>
pub fn cp_scheduled_jobs_reset_firing(conn: &Connection) → Result<()>
pub fn cp_scheduled_job_delete(conn: &Connection, id: &str) → Result<()>
pub fn cp_scheduled_job_set_status(conn: &Connection, id: &str, status: &str) → Result<()>
pub fn cp_scheduled_job_trigger_now(conn: &Connection, id: &str) → Result<()>
```

### src/db/run_contract.rs
```
pub struct RunContract
pub struct RunContractBuilder
impl RunContractBuilder
  pub fn new(workspace_root: impl Into<String>) → Self
  pub fn task_id(mut self, id: impl Into<String>) → Self
  pub fn allowed_paths(mut self, paths: Vec<String>) → Self
  pub fn blocked_paths(mut self, paths: Vec<String>) → Self
  pub fn allowed_tools(mut self, tools: Vec<String>) → Self
  pub fn token_budget(mut self, tokens: i64) → Self
  pub fn time_budget_secs(mut self, secs: i64) → Self
  pub fn insert(self, conn: &Connection) → Result<String>
pub fn cp_insert_run_contract(conn: &Connection, task_id: Option<&str>, workspace_root: &str, allowed_paths_json: &str, blocked_paths_json: &str, allowed_tools_json: &str, token_budget: Option<i64>, time_budget_secs: Option<i64>,) → Result<String>
pub fn cp_get_run_contract(conn: &Connection, id: &str) → Result<Option<RunContract>>
pub fn cp_get_run_contract_for_agent_run(conn: &Connection, agent_run_id: &str,) → Result<Option<RunContract>>
```

### src/db/wf_swarm.rs
```
pub fn create_swarm_workflow(conn: &Connection, project_path: &str, description: &str, agent_name: &str,) → Result<crate::control_plane...
pub fn mark_swarm_workflow_running(conn: &Connection, task_id: &str, agent_run_id: &str,) → Result<()>
pub fn ensure_swarm_review_artifacts(conn: &Connection, task_id: &str, agent_run_id: &str, project_id: Option<i64>, project_path: &str, branch_name: &str, worktree_path: &str,) → Result<(String, String)>
pub fn mark_swarm_workflow_merged(conn: &Connection, task_id: &str, agent_run_id: &str, artifact_id: Option<&str>, approval_id: Option<&str>,) → Result<()>
pub fn mark_swarm_workflow_rejected(conn: &Connection, task_id: &str, agent_run_id: &str, artifact_id: Option<&str>, approval_id: Option<&str>,) → Result<()>
```

### src/db/wf_file_change.rs
```
pub struct FileChangeApprovalData
pub fn project_id_for_file_path(conn: &Connection, path: &str) → Option<i64>
pub fn create_file_change_workflow(conn: &Connection, path: &str, original_content: &str, new_content: &str, agent_name: &str,) → Result<crate::control_plane...
pub fn mark_file_change_workflow_applied(conn: &Connection, ids: &crate::control_plane::FileChangeWorkflowIds, resolved_by: &str,) → Result<()>
pub fn mark_file_change_workflow_rejected(conn: &Connection, ids: &crate::control_plane::FileChangeWorkflowIds, resolved_by: &str, reason: &str,) → Result<()>
pub fn mark_file_change_workflow_apply_failed(conn: &Connection, ids: &crate::control_plane::FileChangeWorkflowIds, resolved_by: &str, reason: &str,) → Result<()>
pub fn cp_load_pending_file_change_approvals(conn: &Connection,) → Result<Vec<FileChangeApprov...
```

### src/db/wf_sessions.rs
```
pub struct SessionRow
pub fn cp_session_start(conn: &Connection, agent_identity: &str, project_id: Option<i64>,) → Result<(String, String)>
pub fn cp_session_end(conn: &Connection, task_id: &str, run_id: &str, success: bool,) → Result<()>
pub fn cp_sessions_list(conn: &Connection, limit: usize) → Result<Vec<SessionRow>>
```

### src/db/tests/mod.rs
```
pub fn in_memory() → Connection
```

### src/filebrowser/agents.rs
```
pub struct AgentRuleGroup
impl AgentRuleGroup
  pub fn exists(&self) → bool
pub fn discover_all_agent_rules(dev_ops: &Path) → Vec<AgentRuleGroup>
```

### src/db/wf_task_graph.rs
```
pub struct ControlGraphNodeRow
pub fn create_task_graph_node_workflow(conn: &Connection, graph_id: &str, node_id: &str, description: &str, shell_cmd: &str, agent_name: &str, ready: bool,) → Result<crate::control_plane...
pub fn create_task_graph_edges(conn: &Connection, graph_id: &str, node_task_ids: &std::collections::HashMap<String, String>, nodes: &[crate::task_graph::NodeSpec],) → Result<()>
pub fn load_control_task_statuses(conn: &Connection, task_ids: &[String],) → std::collections::HashMap<S...
pub fn load_graph_control_task_statuses(conn: &Connection, graph_id: &str,) → std::collections::HashMap<S...
pub fn load_graph_node_dependencies(conn: &Connection, graph_id: &str,) → std::collections::HashMap<S...
pub fn load_control_graph_nodes(conn: &Connection, graph_id: &str) → Vec<ControlGraphNodeRow>
pub fn mark_control_task_ready(conn: &Connection, task_id: &str) → Result<()>
pub fn mark_control_task_running(conn: &Connection, task_id: &str, agent_run_id: &str, job_id: &str,) → Result<()>
pub fn mark_control_task_completed(conn: &Connection, task_id: &str, agent_run_id: &str, result: &str,) → Result<()>
pub fn mark_control_task_failed(conn: &Connection, task_id: &str, agent_run_id: &str, error: &str,) → Result<()>
```

### src/filebrowser/files.rs
```
pub fn get_master_rule_files(master_md: &Path) → Vec<FileEntry>
pub fn get_agent_config_files() → Vec<FileEntry>
pub fn get_policy_files() → Vec<FileEntry>
pub fn get_mempalace_files(dev_ops: &Path) → Vec<FileEntry>
pub fn discover_memory_files(base: &Path, limit: usize) → Vec<FileEntry>
pub fn load_file_content(path: &PathBuf) → String
pub fn save_file_content(path: &Path, content: &str) → std::io::Result<()>
pub fn find_file_by_name(query: &str, master_md: &Path) → Option<FileEntry>
```

### src/filebrowser/git.rs
```
pub struct RecentProject
pub struct GitCommitResult
pub fn load_recent_projects(base: &Path) → Vec<RecentProject>
pub fn git_get_remote_url(dir: &Path) → Option<String>
pub fn git_is_dirty(dir: &Path) → Option<bool>
pub fn get_git_log(dir: &Path) → Vec<String>
pub fn git_commit(dir: &Path, msg: &str) → GitCommitResult
pub fn git_push(dir: &Path) → Result<(), String>
```

### src/filebrowser/mod.rs
```
pub struct FileEntry
impl FileEntry
  pub fn new(name: impl Into<String>, path: impl Into<PathBuf>) → Self
  pub fn readonly(mut self) → Self
  pub fn exists(&self) → bool
```

### src/server/http/mod.rs
```
pub fn resolve_bind_addr(port: u16) → SocketAddr
pub fn detect_tailscale_ip() → Option<std::net::IpAddr>
pub async fn start_http_server(port: u16, daemon_state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>,) → anyhow::Result<()>
```

### src/setup_wizard/exec.rs
```
pub fn exec_workspace(dev_ops: &Path, github_user: &str) → Vec<WizardAction>
pub fn exec_master(master_path: &Path, github_user: &str) → Vec<WizardAction>
pub fn exec_claude(dev_ops: &Path, master_path: &Path) → Vec<WizardAction>
pub fn exec_codex(master_path: &Path) → Vec<WizardAction>
pub fn exec_opencode() → Vec<WizardAction>
pub fn exec_skills(dev_ops: &Path) → Vec<WizardAction>
pub fn exec_agent_wrapper(choice: usize) → Vec<WizardAction>
pub fn exec_initialize(dev_ops: &Path, master_path: &Path, skills_path: &Path, vault_path: Option<&Path>, agent_wrapper_enabled: bool,) → Vec<WizardAction>
```

### src/setup_wizard/types.rs
```
pub struct AgentStatus
pub struct WizardAction
pub enum WizardStep
impl WizardStep
  pub fn next(&self) → Self
  pub fn index(&self) → usize
  pub fn total() → usize
  pub fn title(&self) → &'static str
impl WizardAction
pub fn detect_agents() → AgentStatus
```

### src/task_graph/store.rs
```
pub struct GraphStore
impl GraphStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn create(&self, goal: &str, agent: &str, nodes: Vec<NodeSpec>) → Result<String>
  pub fn get(&self, graph_id: &str) → Option<TaskGraph>
```

### src/ui/components.rs
```
pub fn render_boot(frame: &mut Frame, app: &App)
pub fn render_bouncing_alert(frame: &mut Frame, app: &App)
pub fn render_launcher(frame: &mut Frame, area: Rect, app: &App)
pub fn render_command_palette(frame: &mut Frame, app: &App)
pub fn render_file_changed_badge(frame: &mut Frame, _app: &App)
pub fn render_launcher_modal(frame: &mut Frame, app: &App)
pub fn center_rect(width: u16, height: u16, parent: Rect) → Rect
pub fn render_handover_modal(frame: &mut Frame, app: &App)
```

### src/system_scan/mod.rs
```
pub struct SystemAiTool
pub struct UsageSnapshot
pub struct AiAuditReport
pub enum ToolStatus
pub enum UsageConfidence
pub enum UsageSource
impl UsageSnapshot
pub fn scan_system() → AiAuditReport
```

### src/task_graph/types.rs
```
pub struct NodeSpec
pub struct GraphNode
pub struct TaskGraph
pub enum NodeStatus
impl NodeStatus
```

### src/ui/panels/extensions.rs
```
pub fn render_extensions(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/health.rs
```
pub fn render_diagnostics(frame: &mut Frame, area: Rect, app: &App)
pub fn render_health_view(frame: &mut Frame, app: &App)
pub fn render_system_audit(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/panels/agents.rs
```
pub fn render_agents(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/mod.rs
```
pub fn spinner_char(tick: u64) → char
pub fn render(frame: &mut Frame, app: &App)
pub fn update_code_block_state(line: &str, ext: &str, in_block: &mut bool)
pub fn highlight_line(line: &'a str, in_block: &mut bool, ext: &str) → Vec<Span<'a>>
```

### src/ui/panels/recent.rs
```
pub fn render_recent(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/setup/mod.rs
```
pub fn render_setup(frame: &mut Frame, app: &App)
```

### src/ui/setup/steps/action.rs
```
pub fn render_agent(frame: &mut Frame, area: Rect, app: &App, name: &str, skipped: bool, installed: bool, version: &str, url: &str, will_create: &[&str],)
pub fn render_skills(frame: &mut Frame, area: Rect, app: &App)
pub fn render_agent_wrapper(frame: &mut Frame, area: Rect, app: &App)
pub fn render_initialize(frame: &mut Frame, area: Rect, app: &App)
pub fn render_done(frame: &mut Frame, area: Rect, app: &App)
```

### src/ui/setup/steps/info.rs
```
pub fn render_welcome(frame: &mut Frame, area: Rect, app: &App)
pub fn render_workspace(frame: &mut Frame, area: Rect, app: &App)
pub fn render_master(frame: &mut Frame, area: Rect, app: &App)
```

## tools

### tools/raios-tray/memory.md
```
h1 Project Memory: raios-tray
h2 Context
h2 Active Objectives
h2 Technical Decisions
h2 Important Links & Paths
h2 Current Focus
h2 Change Log & Agent Trail
```

### tools/raios-tray/raios-tray.py
```
@dataclass Agent  :151-153
@dataclass TrayState  :157-168
class PathInput  :770-805
  def __init__(initial_value: str, mode: str, parent: QWidget | None)
  def value() → str
  def pick_path() → None
class SettingsDialog  :808-967
  def __init__(parent: QWidget | None)
  def build_config() → dict
  def validate_config(config: dict) → str | None
  def persist() → bool
  def save_only() → None
  def save_and_restart() → None
  def open_config_dir() → None
class MemoryBrowserDialog  :970-1113
  def __init__(items: list[dict], parent: QWidget | None)
class QuickAddTaskDialog  :1116-1169
  def __init__(projects: list[dict], parent: QWidget | None)
class TaskListDialog  :1172-1292
  def __init__(tasks: list[dict], projects: list[dict], parent: QWidget | None)
class ProjectEditDialog  :1295-1317
  def __init__(parent: QWidget | None, name: str, path: str)
  def result() → tuple[str, str]
class ProjectManagerDialog  :1320-1579
  def filter_rows(text: str) → None
```

## vscode-extension

### vscode-extension/src/commands/CommandBridge.ts
```
export class CommandBridge  :8-107
constructor  :9-13
register  :15-63
```
