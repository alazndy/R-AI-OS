<!-- Generated by SigMap gen-context.js v6.15.0 -->
<!-- Updated: 2026-07-10T12:56:02.592Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
tools/raios-tray/raios-tray.py ← __future__, PySide6, gi, platform, shlex
vscode-extension/src/bridge/JumpToCode.ts ← ipc/DaemonClient
vscode-extension/src/commands/CommandBridge.ts ← ipc/DaemonClient, utils/raiosBinary, providers/DiagnosticProvider
vscode-extension/src/extension.ts ← ipc/DaemonClient, providers/StatusBarProvider, commands/CommandBridge, providers/DiffInboxProvider, providers/DiagnosticProvider
vscode-extension/src/ipc/DaemonClient.ts ← utils/raiosBinary
vscode-extension/src/ipc/TokenBridge.ts ← utils/raiosBinary
vscode-extension/src/ipc/DaemonManager.ts ← utils/raiosBinary
```

## crates

### crates/raios-runtime/src/daemon/cmd/swarm.rs
```
pub async fn handle_create_swarm_task(v: &serde_json::Value, swarm_store: &Arc<SwarmStore>, writer: &mut W,)
pub async fn handle_get_swarm_task(v: &serde_json::Value, swarm_store: &Arc<SwarmStore>, writer: &mut W,)
pub async fn handle_list_swarm_tasks(swarm_store: &Arc<SwarmStore>, writer: &mut W,)
pub async fn handle_approve_swarm_task(v: &serde_json::Value, swarm_store: &Arc<SwarmStore>, writer: &mut W,)
pub async fn handle_reject_swarm_task(v: &serde_json::Value, swarm_store: &Arc<SwarmStore>, writer: &mut W,)
```

### crates/raios-runtime/src/daemon/handlers.rs
```
pub struct ClientHandle
pub async fn handle_client_connection(h: ClientHandle)
```

### crates/raios-runtime/src/daemon/health.rs
```
pub async fn start_health_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration,)
```

### crates/raios-runtime/src/daemon/lifecycle.rs
```
pub async fn start_lifecycle_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration, standby_days: u64, archive_days: u64,)
```

### crates/raios-runtime/src/daemon/scheduler.rs
```
pub async fn start_scheduler_worker(_state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, check_interval: Duration,)
```

### crates/raios-runtime/src/daemon/sentinel.rs
```
pub async fn start_sentinel_worker(state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>, interval: Duration,)
```

### crates/raios-runtime/src/daemon/server.rs
```
pub struct Server
impl Server
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub async fn run_with_tx(&self, tx: broadcast::Sender<String>) → anyhow::Result<()>
  pub async fn run(&self) → anyhow::Result<()>
```

### crates/raios-runtime/src/daemon/proxy.rs
```
pub struct AgentProcess
pub struct ExecutionProxy
impl ExecutionProxy
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub fn with_event_tx(mut self, tx: tokio::sync::broadcast::Sender<String>) → Self
  pub async fn spawn_agent(&self, agent_name: &str, project_path: &str, timeout_secs: u64,) → Result<String>
```

### crates/raios-runtime/src/daemon/validation.rs
```
pub async fn start_validation_worker(state: Arc<RwLock<DaemonState>>, mut tx_rx: broadcast::Receiver<String>, tx_broadcast: broadcast::Sender<String>,)
```

### crates/raios-runtime/src/factory.rs
```
pub struct Job
pub struct Factory
pub enum JobStatus
impl JobStatus
impl Job
  pub fn new(description: &str, agent: &str, project: Option<&str>, webhook_url: Option<&str>,) → Self
impl Factory
  pub fn new(tx: broadcast::Sender<String>) → Self
  pub fn submit(&self, job: Job, task: TaskFn) → Uuid
```

### crates/raios-runtime/src/discovery.rs
```
pub struct AgentInfo
pub struct SkillInfo
impl AgentInfo
  pub fn exists(&self) → bool
pub fn discover_agents() → Vec<AgentInfo>
pub fn discover_skills(skills_path: &Path) → Vec<SkillInfo>
pub fn open_in_editor(path: &Path) → anyhow::Result<()>
```

### crates/raios-runtime/src/daemon/state.rs
```
pub struct SentinelFileStatus
pub struct FileChangeApproval
pub struct ValidationError
pub struct PendingDiff
pub struct DaemonState
impl DaemonState
  pub fn new() → Arc<RwLock<Self>>
  pub fn refresh_pending_from_db(&mut self)
  pub fn sync_payload(&self) → serde_json::Value
```

### crates/raios-runtime/src/filebrowser/agents.rs
```
pub struct AgentRuleGroup
impl AgentRuleGroup
  pub fn exists(&self) → bool
pub fn discover_all_agent_rules(dev_ops: &Path) → Vec<AgentRuleGroup>
```

### crates/raios-runtime/src/filebrowser/git.rs
```
pub struct RecentProject
pub struct GitCommitResult
pub fn load_recent_projects(base: &Path) → Vec<RecentProject>
pub fn git_get_remote_url(dir: &Path) → Option<String>
pub fn git_is_dirty(dir: &Path) → Option<bool>
pub fn get_git_log(dir: &Path) → Vec<String>
pub fn git_commit(dir: &Path, msg: &str) → GitCommitResult
pub fn git_push(dir: &Path) → Result<(), String>
```

### crates/raios-runtime/src/git_utils.rs
```
pub fn diff_stat(project_path: &Path) → Option<String>
```

### crates/raios-runtime/src/filebrowser/files.rs
```
pub fn get_master_rule_files(master_md: &Path) → Vec<FileEntry>
pub fn get_agent_config_files() → Vec<FileEntry>
pub fn get_policy_files() → Vec<FileEntry>
pub fn get_mempalace_files(dev_ops: &Path) → Vec<FileEntry>
pub fn discover_memory_files(base: &Path, limit: usize) → Vec<FileEntry>
pub fn load_file_content(path: &PathBuf) → String
pub fn save_file_content(path: &Path, content: &str) → std::io::Result<()>
pub fn find_file_by_name(query: &str, master_md: &Path) → Option<FileEntry>
```

### crates/raios-runtime/src/filebrowser/mod.rs
```
pub struct FileEntry
impl FileEntry
  pub fn new(name: impl Into<String>, path: impl Into<PathBuf>) → Self
  pub fn readonly(mut self) → Self
  pub fn exists(&self) → bool
```

### crates/raios-runtime/src/health.rs
```
pub struct ProjectHealth
pub fn check_project_fast(proj: &EntityProject) → ProjectHealth
pub fn check_project(proj: &EntityProject) → ProjectHealth
pub fn check_build_test_deps(h: &mut ProjectHealth)
pub fn check_project_with_security(proj: &EntityProject) → ProjectHealth
pub fn check_graphify(path: &Path) → (bool, Option<PathBuf>)
pub fn find_graphify_script(dev_ops: &Path) → Option<PathBuf>
pub fn validate_file(path: &Path, proj: &EntityProject) → Vec<ValidationError>
```

### crates/raios-runtime/src/intelligence/edge.rs
```
pub struct EdgeRouter
impl EdgeRouter
  pub fn new(descriptions: Vec<(String, String) → Self
  pub fn route(&self, query: &str) → Option<&str>
```

### crates/raios-runtime/src/intelligence/evolution.rs
```
pub struct CandidateStore
impl CandidateStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn insert(&self, rule: &str, source: &str, confidence: f32)
  pub fn list_pending(&self, limit: usize) → Vec<String>
  pub fn promote(&self, rule: &str)
  pub fn sweep_expired(&self) → usize
pub fn process_job_event(event: &serde_json::Value, description: &str, command: &str, store: &CandidateStore,)
pub fn suggest_from_trace(trace: &raios_core::db::ToolTraceRow) → Option<String>
pub fn import_trace_candidates(conn: &Connection, store: &CandidateStore, project: Option<&str>, limit: usize,) → rusqlite::Result<usize>
pub async fn start_evolution_worker(mut rx: broadcast::Receiver<String>)
```

### crates/raios-runtime/src/intelligence/instinct.rs
```
pub struct InstinctData
pub struct InstinctEngine
impl InstinctEngine
  pub fn init() → Self
  pub fn add_rule(&mut self, rule: String)
  pub fn save(&self) → anyhow::Result<()>
  pub fn get_instinct_prompt(&self) → String
pub fn suggest_from_health(health: &crate::health::ProjectHealth) → Vec<String>
pub fn suggest_from_outcome(description: &str, command: &str, result: &str) → Vec<String>
pub fn suggest_from_failure(description: &str, _command: &str, error: &str) → Vec<String>
pub fn append_to_memory_md(project_path: &std::path::Path, rule: &str) → anyhow::Result<()>
pub fn load_project_rules(project_path: &std::path::Path) → Vec<String>
```

### crates/raios-runtime/src/kernel.rs
```
pub struct Kernel
impl Kernel
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub async fn run(&self) → Result<()>
impl McpTcpSession
```

### crates/raios-runtime/src/intelligence/router.rs
```
pub struct AgentRouter
impl AgentRouter
impl AgentRouter
  pub fn new() → Self
  pub fn init() → anyhow::Result<Self>
  pub fn route(&self, task: &str) → anyhow::Result<Option<String>>
pub fn route_capability(query: &str) → Option<String>
```

### crates/raios-runtime/src/new_project.rs
```
pub struct NewProjectConfig
pub struct NewProjectResult
pub fn create(cfg: &NewProjectConfig) → NewProjectResult
```

### crates/raios-runtime/src/refactor_scan.rs
```
pub struct RefactorIssue
pub struct RefactorReport
pub struct RefactorThresholds
pub struct PartialThresholds
pub struct RefactorConfig
pub enum RefactorSeverity
impl RefactorSeverity
  pub fn label(&self) → &'static str
impl RefactorReport
  pub fn empty() → Self
impl RefactorThresholds
impl RefactorConfig
  pub fn for_ext(&self, ext: &str) → RefactorThresholds
pub fn scan_project(root: &Path) → RefactorReport
pub fn scan_project_with(root: &Path, config: &RefactorConfig) → RefactorReport
```

### crates/raios-runtime/src/proxy_store.rs
```
pub struct Capability
pub struct CapabilityStore
pub struct CapabilityProxy
pub enum Backend
impl CapabilityStore
  pub fn new() → Self
  pub fn register(&mut self, cap: Capability)
  pub fn get(&self, name: &str) → Option<&Capability>
  pub fn list(&self) → Vec<&Capability>
impl CapabilityProxy
  pub fn new(store: CapabilityStore) → Self
  pub fn execute(&self, capability: &str, input: &str) → Result<String>
  pub fn route(&self, query: &str, input: &str) → Result<String>
  pub fn store(&self) → &CapabilityStore
```

### crates/raios-runtime/src/radar.rs
```
pub struct Whisper
pub struct RadarChannel
pub enum WhisperKind
pub enum Severity
impl WhisperKind
  pub fn label(&self) → &'static str
impl Whisper
  pub fn compile_error(project: &str, file: PathBuf, line: u32, message: &str) → Self
  pub fn security_vuln(project: &str, file: Option<PathBuf>, message: &str, cve: Option<&str>,) → Self
  pub fn arch_violation(project: &str, file: PathBuf, message: &str, hint: Option<&str>) → Self
  pub fn lock_conflict(project: &str, resource: &str, owner: &str) → Self
  pub fn codebase_rule(project: &str, rule: &str, detail: &str) → Self
impl RadarChannel
  pub fn new(tx: broadcast::Sender<String>) → Self
  pub fn emit(&self, whisper: Whisper)
  pub fn emit_many(&self, whispers: impl IntoIterator<Item = Whisper>)
```

### crates/raios-runtime/src/search/hybrid.rs
```
pub struct HybridResult
pub enum ResultSource
impl ResultSource
  pub fn label(&self) → &'static str
pub fn fuse(bm25_results: Vec<BM25Result>, vector_results: Vec<VectorResult>, top_n: usize,) → Vec<HybridResult>
```

### crates/raios-runtime/src/sentinel/compiler.rs
```
pub fn run_cargo_check(project_path: &Path) → Result<Vec<ValidationError>>
```

### crates/raios-runtime/src/sentinel/mod.rs
```
pub enum SentinelState
```

### crates/raios-runtime/src/sentinel/tester.rs
```
pub fn run_cargo_test(project_path: &Path) → Result<Vec<ValidationError>>
pub fn has_tests(project_path: &Path) → bool
```

### crates/raios-runtime/src/sentinel/monitor.rs
```
pub struct FileMonitor
impl FileMonitor
  pub fn new(path: impl AsRef<Path>) → Self
  pub fn watch(&self) → Result<Receiver<Result<Even...
```

### crates/raios-runtime/src/server/http/a2a.rs
```
impl A2aResponse
```

### crates/raios-runtime/src/server/http/mod.rs
```
pub fn resolve_bind_addr(port: u16) → SocketAddr
pub fn detect_tailscale_ip() → Option<std::net::IpAddr>
pub async fn start_http_server(port: u16, daemon_state: Arc<RwLock<DaemonState>>, tx: broadcast::Sender<String>,) → anyhow::Result<()>
```

### crates/raios-runtime/src/session_canvas.rs
```
pub struct CanvasNode
pub fn fold_events(events: &[SessionEvent]) → Vec<CanvasNode>
pub fn to_mermaid(session_id: &str, nodes: &[CanvasNode]) → String
```

### crates/raios-runtime/src/session.rs
```
pub struct Session
pub struct SessionEvent
pub struct SessionStore
impl SessionStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn start(&self, agent: &str, project: Option<&str>) → String
  pub fn end(&self, id: &str, summary: Option<&str>)
  pub fn record_event(&self, session_id: &str, event_type: &str, data: &str)
  pub fn get(&self, id: &str) → Option<Session>
  pub fn events(&self, session_id: &str) → Vec<SessionEvent>
  pub fn current_open(&self) → Option<Session>
```

### crates/raios-runtime/src/session_memory/distillation.rs
```
pub fn rebuild_persona(conn: &rusqlite::Connection, project_key: &str) → Option<()>
```

### crates/raios-runtime/src/session_memory/heuristics.rs
```
pub fn decision_lines_from_transcript(transcript: &str) → Vec<String>
```

### crates/raios-runtime/src/session_memory/transcript_io.rs
```
pub fn find_latest_conversation(project_path: &str, min_mtime: Option<SystemTime>,) → Option<PathBuf>
pub fn extract_transcript(jsonl_path: &Path) → String
pub fn collect_transcript(agent: &str, project_path: &str, session_started: SystemTime) → String
```

### crates/raios-runtime/src/session_memory/mod.rs
```
pub fn generate_memory_entry(transcript: &str) → Option<String>
pub fn append_to_memory_md(project_path: &str, entry: &str) → std::io::Result<()>
pub fn post_session_memory_prompt(project_path: &str, session_started: SystemTime)
pub fn cmd_memory_gen(project: Option<&str>, json: bool)
pub fn auto_sync_agent_memory(agent: &str, project_path: &str, session_started: SystemTime, verbose: bool,)
pub fn auto_sync_claude_memory(project_path: &str, session_started: SystemTime)
```

### crates/raios-runtime/src/session_review.rs
```
pub struct PostRunReview
impl PostRunReview
  pub fn to_json(&self) → String
pub fn build_review(conn: &Connection, agent: &str, project_dir: &Path, session_started: SystemTime, session_start_utc: &str, session_end_utc: &str,) → PostRunReview
```

### crates/raios-runtime/src/swarm/merge.rs
```
pub struct DiffSummary
pub fn diff_summary(project_path: &Path, branch: &str) → Result<DiffSummary>
pub fn merge_branch(project_path: &Path, branch: &str, message: &str) → Result<()>
pub fn delete_branch(project_path: &Path, branch: &str) → Result<()>
```

### crates/raios-runtime/src/sync.rs
```
pub fn sync_universe(dev_ops: &Path, master: &Path) → Result<String>
```

### crates/raios-runtime/src/swarm/store.rs
```
pub struct SwarmStore
impl SwarmStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn create(&self, project_name: &str, project_path: &Path, description: &str, agent: &str,) → Result<SwarmTask>
  pub fn get(&self, id: &str) → Option<SwarmTask>
  pub fn list_active(&self) → Vec<SwarmTask>
```

### crates/raios-runtime/src/swarm/worktree.rs
```
pub fn create_worktree(project_path: &Path, task_id: Uuid, task_description: &str,) → Result<(PathBuf, String)>
pub fn remove_worktree(project_path: &Path, worktree_path: &Path) → Result<()>
pub fn list_worktrees(project_path: &Path) → Result<Vec<String>>
```

### crates/raios-runtime/src/swarm/mod.rs
```
pub struct SwarmTask
pub enum SwarmStatus
```

### crates/raios-runtime/src/system_scan/mod.rs
```
pub struct SystemAiTool
pub struct UsageSnapshot
pub struct AiAuditReport
pub enum ToolStatus
pub enum UsageConfidence
pub enum UsageSource
impl UsageSnapshot
pub fn scan_system() → AiAuditReport
```

### crates/raios-runtime/src/trace_recall.rs
```
pub fn record_post_run_review_trace(conn: &Connection, agent: &str, project_dir: &str, success: bool, review: &crate::session_review::PostRunReview, related_task_id: Option<&str>,) → rusqlite::Result<Option<Str...
pub fn relevant_trace_block(conn: &Connection, project_path: Option<&str>, query: &str, limit: usize,) → Option<String>
```

### crates/raios-runtime/src/workers.rs
```
pub enum RuntimeEvent
pub fn spawn_embedded_workers(tx: Sender<RuntimeEvent>, dev_ops: PathBuf)
```

### crates/raios-runtime/src/tasks.rs
```
pub struct Task
impl Task
  pub fn display(&self) → &str
  pub fn is_persisted(&self) → bool
  pub fn from_personal_row(r: raios_core::db::PersonalTaskRow) → Self
  pub fn agent_label(&self) → Option<&str>
pub fn parse_task_line(line: &str) → Option<Task>
pub fn load_tasks(dev_ops: &Path) → Result<Vec<Task>>
pub fn save_tasks(dev_ops: &Path, tasks: &[Task]) → Result<()>
pub fn dispatch_to_agent(task: &Task, agent: &str, project_path: Option<&PathBuf>, sentinel_errors: Option<Vec<String>>,) → String
```

### crates/raios-surface-cli/Cargo.toml
```
table [package]
table [dependencies]
table [features]
table [[bin]]
table [dev-dependencies]
key name
key version
key edition
key serde_json
key toml
key anyhow
key crossterm
key ratatui
key dirs
key sha2
key walkdir
key notify
key regex
key notify-rust
key which
key path
key tempfile
```

### crates/raios-surface-cli/src/cli/action_types.rs
```
pub enum HubAction
pub enum HubApiKeyAction
pub enum PolicyCmd
pub enum MemAction
pub enum TraceAction
pub enum AgentWrapperCmd
pub enum CronAction
pub enum HandoffTarget
pub enum HandoffStatus
pub enum QuarantineAction
pub enum SecretAction
pub enum SwarmAction
pub enum EvolveAction
pub enum GitCommands
impl HandoffTarget
  pub fn as_str(&self) → &'static str
impl HandoffStatus
  pub fn as_str(&self) → &'static str
```

### crates/raios-surface-cli/src/cli/cron.rs
```
pub fn cmd_cron(action: CronAction, json: bool)
```

### crates/raios-surface-cli/src/cli/args.rs
```
pub struct Cli
pub enum InstinctCmd
pub enum Commands
```

### crates/raios-surface-cli/src/cli/agent_wrapper.rs
```
pub enum AgentWrapperAction
pub fn cmd_agent_wrapper(action: AgentWrapperAction, json: bool)
```

### crates/raios-surface-cli/src/cli/ext/mod.rs
```
pub fn cmd_ext_list(dev_ops_path: &Path, json: bool)
pub fn cmd_ext(name: &str, ext_args: &[String], dev_ops_path: &Path, json: bool)
```

### crates/raios-surface-cli/src/cli/hub.rs
```
pub fn cmd_start(json: bool)
pub fn cmd_stop(json: bool)
pub fn cmd_status(json: bool)
pub fn cmd_install(enable: bool, json: bool)
pub fn cmd_logs(lines: usize)
pub fn cmd_api_key_show(reveal: bool)
pub fn cmd_api_key_generate(force: bool)
```

### crates/raios-surface-cli/src/cli/mod.rs
```
pub fn run(cli: Cli)
```

### crates/raios-surface-cli/src/cli/reflect.rs
```
pub fn cmd_reflect(dev_ops_path: &Path, json: bool)
```

### crates/raios-surface-cli/src/cli/security/owasp.rs
```
pub fn cmd_security(target: Option<String>, full: bool, watch: bool, dev_ops: &Path, json: bool,)
pub fn cmd_security_watch(path: &Path, json: bool) → anyhow::Result<()>
```

### crates/raios-surface-cli/src/cli/security/infra.rs
```
pub fn cmd_quarantine(action: raios_surface_cli::cli::QuarantineAction, json: bool)
pub fn cmd_secret(action: raios_surface_cli::cli::SecretAction, json: bool)
```

### crates/raios-surface-cli/src/cli/security/audit.rs
```
pub fn cmd_license(project: Option<String>, dev_ops: &Path, json_out: bool)
pub fn cmd_verify_chain(last: usize, json: bool)
pub fn cmd_rate_status(json: bool)
pub fn cmd_pin_reset(json: bool)
pub fn cmd_pin_status(json: bool)
```

### crates/raios-surface-cli/src/cli/task_update.rs
```
pub fn run_refactor_flag(json: bool)
```

### crates/raios-surface-mcp/Cargo.toml
```
table [package]
table [dependencies]
table [dev-dependencies]
key name
key version
key edition
key serde_json
key anyhow
key sha2
key tempfile
```

### crates/raios-surface-mcp/src/mcp/mod.rs
```
impl RpcResponse
impl McpServer
pub fn run_stdio() → Result<()>
```

### crates/raios-surface-mcp/src/mcp/tools_dev.rs
```
impl McpServer
```

### crates/raios-surface-mcp/src/mcp/tools.rs
```
impl McpServer
```

### crates/raios-surface-mcp/src/mcp/resources.rs
```
impl McpServer
```

### crates/raios-surface-mcp/src/mcp/tools_swarm.rs
```
impl McpServer
```

### crates/raios-surface-mcp/src/mcp/tools_git.rs
```
impl McpServer
```

### crates/raios-surface-tui/Cargo.toml
```
table [package]
table [dependencies]
table [dev-dependencies]
key name
key version
key edition
key ratatui
key crossterm
key serde_json
key toml
key anyhow
key dirs
key notify
key tempfile
```

### crates/raios-surface-tui/src/app/editor.rs
```
pub struct Editor
impl Editor
  pub fn from_content(content: &str, view_height: usize) → Self
  pub fn content(&self) → String
  pub fn handle_key(&mut self, key: KeyEvent)
pub fn simple_diff(old: &str, new: &str) → Vec<String>
pub fn char_to_byte(s: &str, char_pos: usize) → usize
```

### crates/raios-surface-tui/src/app/events/actions.rs
```
impl App
```

### crates/raios-surface-tui/src/app/events/bg_messages.rs
```
impl App
  pub fn handle_bg_msg(&mut self, msg: BgMsg)
```

### crates/raios-surface-tui/src/app/events/keyboard/dashboard.rs
```
impl App
```

### crates/raios-surface-tui/src/app/events/commands.rs
```
impl App
```

### crates/raios-surface-tui/src/app/events/helpers.rs
```
pub fn launch_agent(agent: &str, project_path: &Path) → String
pub fn append_memo(text: &str, dev_ops: &Path) → String
```

### crates/raios-surface-tui/src/app/events/ext_actions.rs
```
impl App
  pub fn load_extensions(&mut self)
  pub fn run_ext_cmd(&mut self)
  pub fn save_ext_config_field(&mut self)
```

### crates/raios-surface-tui/src/app/events/keyboard/editor.rs
```
impl App
```

### crates/raios-surface-tui/src/app/events/keyboard/mod.rs
```
impl App
  pub fn handle_key(&mut self, key: KeyEvent) → Result<()>
```

### crates/raios-surface-tui/src/app/events/keyboard/health.rs
```
impl App
```

### crates/raios-surface-tui/src/app/events/keyboard/project.rs
```
impl App
```

### crates/raios-surface-tui/src/app/events/keyboard/setup.rs
```
impl App
```

### crates/raios-surface-tui/src/app/ipc.rs
```
pub fn connect_daemon(tx: Sender<BgMsg>) → Option<Sender<String>>
pub fn connect_daemon_addr(tx: Sender<BgMsg>, remote_host: Option<String>) → Option<Sender<String>>
```

### crates/raios-surface-tui/src/app/mod.rs
```
pub struct PaletteItem
pub struct App
impl App
impl App
  pub fn new() → Self
  pub fn new_remote(host: String) → Self
pub fn filtered_palette(query: &str) → Vec<&'static PaletteItem>
```

### crates/raios-surface-tui/src/app/services.rs
```
pub struct InboxPanelData
pub struct SchedulerPanelData
pub struct PoliciesPanelData
pub struct ProjectListItem
pub struct ProjectsPanelData
pub struct ProjectDetailData
pub fn load_inbox_panel_data() → Result<InboxPanelData, String>
pub fn load_scheduler_panel_data() → Result<SchedulerPanelData, ...
pub fn load_policies_panel_data() → PoliciesPanelData
pub fn sort_project_indices(projects: &[EntityProject], health: &[ProjectHealth], sort: &SortMode,) → Vec<usize>
pub fn build_projects_panel_data(app: &App) → ProjectsPanelData
pub fn load_project_detail_data(project_path: &Path) → ProjectDetailData
pub fn load_graph_report_lines(project_path: &Path) → Result<Vec<String>, String>
pub fn load_git_diff_lines(project_path: &Path) → Vec<String>
pub fn daemon_search_command(query: &str) → String
pub fn daemon_get_logs_command(limit: u64) → String
pub fn daemon_submit_raios_command(args: &str) → String
pub fn create_vault_note(vault_projects_path: &Path, project: &EntityProject,) → std::io::Result<bool>
pub fn probe_service_active(service: &str) → bool
pub fn scan_extensions(dev_ops_path: &Path) → Vec<ExtensionInfo>
pub fn run_extension_command_bg(tx: &std::sync::mpsc::Sender<BgMsg>, ext_path: &Path, toml_path: &Path, ext_name: &str, cmd_name: &str,)
pub fn read_env_key(env_path: &Path, key: &str) → Option<String>
pub fn write_env_key(env_path: &Path, key: &str, value: &str) → std::io::Result<()>
```

### crates/raios-surface-tui/src/app/state.rs
```
pub struct ExtCmdInfo
pub struct ExtConfigField
pub struct ExtServiceStatus
pub struct ExtensionInfo
pub struct ExtState
pub struct Activity
pub struct LogEntry
pub struct SetupField
pub struct PortfolioStats
pub struct RuleCategory
pub struct SearchState
pub struct MempalaceState
pub struct HealthState
pub struct TaskState
pub struct SystemState
pub struct WizardState
pub struct SetupState
pub struct InventoryState
pub struct UIState
pub struct TimelineState
pub struct ProjectState
pub struct EditorState
pub enum ExtFocus
pub enum AppState
pub enum BgMsg
```

### crates/raios-surface-tui/src/setup_wizard/exec.rs
```
pub fn exec_workspace(dev_ops: &Path, github_user: &str) → Vec<WizardAction>
pub fn exec_master(master_path: &Path, github_user: &str) → Vec<WizardAction>
pub fn exec_claude(dev_ops: &Path, master_path: &Path) → Vec<WizardAction>
pub fn exec_codex(master_path: &Path) → Vec<WizardAction>
pub fn exec_opencode() → Vec<WizardAction>
pub fn exec_skills(dev_ops: &Path) → Vec<WizardAction>
pub fn exec_agent_wrapper(choice: usize) → Vec<WizardAction>
pub fn exec_initialize(dev_ops: &Path, master_path: &Path, skills_path: &Path, vault_path: Option<&Path>, agent_wrapper_enabled: bool,) → Vec<WizardAction>
```

### crates/raios-surface-tui/src/setup_wizard/types.rs
```
pub struct AgentStatus
pub struct WizardAction
pub enum WizardStep
impl WizardStep
  pub fn next(&self) → Self
  pub fn index(&self) → usize
  pub fn total() → usize
  pub fn title(&self) → &'static str
impl WizardAction
pub fn detect_agents() → AgentStatus
```

### crates/raios-surface-tui/src/ui/components.rs
```
pub fn render_boot(frame: &mut Frame, app: &App)
pub fn render_bouncing_alert(frame: &mut Frame, app: &App)
pub fn render_launcher(frame: &mut Frame, area: Rect, app: &App)
pub fn render_command_palette(frame: &mut Frame, app: &App)
pub fn render_file_changed_badge(frame: &mut Frame, _app: &App)
pub fn render_launcher_modal(frame: &mut Frame, app: &App)
pub fn center_rect(width: u16, height: u16, parent: Rect) → Rect
pub fn render_handover_modal(frame: &mut Frame, app: &App)
```

### crates/raios-surface-tui/src/ui/health.rs
```
pub fn render_diagnostics(frame: &mut Frame, area: Rect, app: &App)
pub fn render_health_view(frame: &mut Frame, app: &App)
pub fn render_system_audit(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/mod.rs
```
pub fn spinner_char(tick: u64) → char
pub fn render(frame: &mut Frame, app: &App)
pub fn update_code_block_state(line: &str, ext: &str, in_block: &mut bool)
pub fn highlight_line(line: &'a str, in_block: &mut bool, ext: &str) → Vec<Span<'a>>
```

### crates/raios-surface-tui/src/ui/filebrowser.rs
```
pub fn render_file_panel(frame: &mut Frame, area: Rect, app: &App, files: &[FileEntry])
pub fn render_file_view(frame: &mut Frame, app: &App)
pub fn render_file_edit(frame: &mut Frame, app: &App)
```

### crates/raios-surface-tui/src/ui/mempalace.rs
```
pub fn render_mempalace_info(frame: &mut Frame, area: Rect, app: &App)
pub fn render_mempalace_view(frame: &mut Frame, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/agents.rs
```
pub fn render_agents(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/content.rs
```
pub fn render_content(frame: &mut Frame, area: Rect, app: &App)
pub fn render_content_body(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/dashboard_main.rs
```
pub fn render_dashboard(frame: &mut Frame, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/extensions.rs
```
pub fn render_extensions(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/git_diff.rs
```
pub fn render_git_diff_view(frame: &mut Frame, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/header.rs
```
pub fn render_header(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/help.rs
```
pub fn render_help_view(frame: &mut Frame, app: &App)
pub fn render_help(frame: &mut Frame, area: Rect, _app: &App)
```

### crates/raios-surface-tui/src/ui/panels/inbox.rs
```
pub fn render_inbox(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/policies.rs
```
pub fn render_policies(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/logs.rs
```
pub fn render_logs(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/menu.rs
```
pub fn render_menu(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/stats.rs
```
pub fn render_quick_stats(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/scheduler.rs
```
pub fn render_scheduler(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/tasks.rs
```
pub fn render_tasks_panel(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/recent.rs
```
pub fn render_recent(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/rules.rs
```
pub fn render_rules(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/projects.rs
```
pub fn render_projects(frame: &mut Frame, area: Rect, app: &App)
pub fn render_project_detail(frame: &mut Frame, app: &App)
pub fn render_graph_report(frame: &mut Frame, app: &App)
```

### crates/raios-surface-tui/src/ui/search.rs
```
pub fn render_search(frame: &mut Frame, app: &App)
pub fn render_search_panel(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/panels/timeline.rs
```
pub fn render_timeline(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/sentinel.rs
```
pub fn render_sentinel_hub(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/setup/mod.rs
```
pub fn render_setup(frame: &mut Frame, app: &App)
```

### crates/raios-surface-tui/src/ui/setup/steps/info.rs
```
pub fn render_welcome(frame: &mut Frame, area: Rect, app: &App)
pub fn render_workspace(frame: &mut Frame, area: Rect, app: &App)
pub fn render_master(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-surface-tui/src/ui/setup/steps/action.rs
```
pub fn render_agent(frame: &mut Frame, area: Rect, app: &App, name: &str, skipped: bool, installed: bool, version: &str, url: &str, will_create: &[&str],)
pub fn render_skills(frame: &mut Frame, area: Rect, app: &App)
pub fn render_agent_wrapper(frame: &mut Frame, area: Rect, app: &App)
pub fn render_initialize(frame: &mut Frame, area: Rect, app: &App)
pub fn render_done(frame: &mut Frame, area: Rect, app: &App)
```

### crates/raios-runtime/src/cortex/store.rs
```
pub struct ChunkMeta
pub struct VectorResult
pub struct VectorEngine
impl EmbPoint
impl VectorEngine
  pub fn load() → Self
  pub fn load_from(db_path: &Path) → Self
  pub fn save(&mut self)
  pub fn is_indexed(&self, file_path: &str, mtime_secs: u64) → bool
  pub fn upsert_file(&mut self, file_path: &str, mtime_secs: u64, pairs: Vec<(Embedding, ChunkMeta)
pub fn default_db_path() → PathBuf
```

### crates/raios-runtime/src/search/indexer.rs
```
pub struct SearchResult
pub struct ProjectIndex
impl ProjectIndex
  pub fn build(root: &Path) → Result<Self>
  pub fn search(&self, query: &str) → Vec<SearchResult>
  pub fn load_or_build(root: &Path, db_path: &Path, force: bool) → Result<Self>
```

### crates/raios-surface-mcp/src/mcp/tools_workspace.rs
```
impl McpServer
```

## tools

### tools/raios-tray/handoff.md
```
h1 raios-tray Codex Handoff Prompt
h2 Kimlik
h2 Proje
h2 Son Mimari Karar
h2 Mevcut Durum
h2 Şu An Beklenen UI
h2 Yakın Zamandaki Önemli Commitler
h2 Kritik Tespit
h2 Servis
h2 Manuel Çalıştırma
h2 Kullanıcıdan Gelen Son Geri Bildirim
h2 Bir Sonraki Agent İçin Hedef
h2 Not
code-fence bash
code-fence plain
```

### tools/raios-tray/memory.md
```
h1 Project Memory: raios-tray
h2 Context
h2 Active Objectives
h2 Technical Decisions
h2 Important Links & Paths
h2 Current Focus
h2 Change Log & Agent Trail
```

### tools/raios-tray/README.md
```
h1 raios-tray
h2 Supported Platforms
h2 Stack
h2 Files
h2 Install
h2 Platform Notes
h3 Linux
h3 macOS
h3 Windows
h2 Runtime Expectations
code-fence bash
code-fence plain
```

### tools/raios-tray/raios-tray.py
```
@dataclass Agent  :151-153
@dataclass TrayState  :157-168
class PathInput  :770-805
  def __init__(initial_value: str, mode: str, parent: QWidget | None)
  def value() → str
  def pick_path() → None
class SettingsDialog  :808-967
  def __init__(parent: QWidget | None)
  def build_config() → dict
  def validate_config(config: dict) → str | None
  def persist() → bool
  def save_only() → None
  def save_and_restart() → None
  def open_config_dir() → None
class MemoryBrowserDialog  :970-1113
  def __init__(items: list[dict], parent: QWidget | None)
class QuickAddTaskDialog  :1116-1169
  def __init__(projects: list[dict], parent: QWidget | None)
class TaskListDialog  :1172-1292
  def __init__(tasks: list[dict], projects: list[dict], parent: QWidget | None)
class ProjectEditDialog  :1295-1317
  def __init__(parent: QWidget | None, name: str, path: str)
  def result() → tuple[str, str]
class ProjectManagerDialog  :1320-1579
  def filter_rows(text: str) → None
```

## vscode-extension

### vscode-extension/README.md
```
h1 R-AI-OS VS Code Extension
h2 Features
h2 Requirements
h2 Commands
h2 Configuration
h2 Connection
h2 Development
code-fence bash
code-fence plain
```

### vscode-extension/install.sh
```
# Build and install the R-AI-OS VS Code extension, replacing any
```

### vscode-extension/media/sidebar.css
```
var --bg-color
var --panel-bg
var --panel-border
var --accent-gradient
var --text-main
var --text-muted
var --success-color
var --error-color
var --warning-color
var --gray-color
```

### vscode-extension/pnpm-lock.yaml
```
keys: [lockfileVersion, settings, importers, packages, snapshots]
```

### vscode-extension/src/bridge/JumpToCode.ts
```
export class JumpToCode  :4-29
constructor  :5-5
activate  :7-28
```

### vscode-extension/src/commands/CommandBridge.ts
```
export class CommandBridge  :8-107
constructor  :9-13
register  :15-63
```

### vscode-extension/src/extension.ts
```
export function activate  :21-95
export function deactivate  :97-100
```

### vscode-extension/src/ipc/DaemonClient.ts
```
export class DaemonClient  :8-128
constructor  :16-19
onMessage  :25-27
connect  :29-81
send  :83-87
sendRaw  :89-92
disconnect  :94-99
```

### vscode-extension/src/ipc/TokenBridge.ts
```
export class TokenBridge  :7-157
constructor  :10-10
readToken  :16-41
async getToken  :46-64
async request  :69-138
async handleMessage  :143-157
```

### vscode-extension/src/ipc/DaemonManager.ts
```
export class DaemonManager  :10-91
isTokenFresh  :19-27
async ensureRunning  :33-38
async spawn  :41-64
dispose  :87-90
```

### vscode-extension/src/providers/RefactorTreeProvider.ts
```
export interface RefactorFileData  :4-9
file: string  :5-5
severity: "HIGH" | "MEDIUM"  :6-6
lines: number  :7-7
reasons: string[]  :8-8
class RefactorNode  :13-22
constructor  :14-21
```

### vscode-extension/src/utils/raiosBinary.ts
```
export function resolveRaiosBinary  :11-51
export function resolveAiosdBinary  :57-88
export function raiosConfigDir  :94-100
export function tokenFilePath  :105-107
```
