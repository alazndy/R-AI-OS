

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.0 -->
<!-- Updated: 2026-06-03T20:26:56.639Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
vscode-extension\src\commands\CommandBridge.ts ← ipc/DaemonClient, utils/raiosBinary, providers/DiagnosticProvider
vscode-extension\src\extension.ts ← ipc/DaemonClient, providers/StatusBarProvider, commands/CommandBridge, providers/DiffInboxProvider, providers/DiagnosticProvider
```

## src

### src\radar.rs
```
pub struct Whisper
pub struct RadarChannel
pub enum WhisperKind
pub enum Severity
impl WhisperKind
  pub fn label(&self) → &'static str
impl Whisper
  pub fn compile_error(project: &str, file: PathBuf, line: u32, message: &str) → Self
  pub fn security_vuln(project: &str, file: Option<PathBuf>, message: &str, cve: Option<&str>,) → Self
  pub fn arch_violation(project: &str, file: PathBuf, message: &str, hint: Option<&str>) → Self
  pub fn lock_conflict(project: &str, resource: &str, owner: &str) → Self
  pub fn codebase_rule(project: &str, rule: &str, detail: &str) → Self
impl RadarChannel
  pub fn new(tx: broadcast::Sender<String>) → Self
  pub fn emit(&self, whisper: Whisper)
  pub fn emit_many(&self, whispers: impl IntoIterator<Item = Whisper>)
```

### src\session.rs
```
pub struct Session
pub struct SessionEvent
pub struct SessionStore
impl SessionStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn start(&self, agent: &str, project: Option<&str>) → String
  pub fn end(&self, id: &str, summary: Option<&str>)
  pub fn record_event(&self, session_id: &str, event_type: &str, data: &str)
  pub fn get(&self, id: &str) → Option<Session>
  pub fn events(&self, session_id: &str) → Vec<SessionEvent>
  pub fn current_open(&self) → Option<Session>
```

### src\swarm\worktree.rs
```
pub fn create_worktree(project_path: &Path, task_id: Uuid, task_description: &str,) → Result<(PathBuf, String)>
pub fn remove_worktree(project_path: &Path, worktree_path: &Path) → Result<()>
pub fn list_worktrees(project_path: &Path) → Result<Vec<String>>
```

### src\tasks.rs
```
pub struct Task
impl Task
  pub fn display(&self) → &str
  pub fn agent_label(&self) → Option<&str>
pub fn parse_task_line(line: &str) → Option<Task>
pub fn load_tasks(dev_ops: &Path) → Result<Vec<Task>>
pub fn save_tasks(dev_ops: &Path, tasks: &[Task]) → Result<()>
pub fn dispatch_to_agent(task: &Task, agent: &str, project_path: Option<&PathBuf>, sentinel_errors: Option<Vec<String>>,) → String
```

### src\ui\projects.rs
```
pub fn render_projects(frame: &mut Frame, area: Rect, app: &App)
pub fn render_project_detail(frame: &mut Frame, app: &App)
pub fn render_graph_report(frame: &mut Frame, app: &App)
```

### src\instinct.rs
```
pub struct InstinctData
pub struct InstinctEngine
impl InstinctEngine
  pub fn init() → Self
  pub fn add_rule(&mut self, rule: String)
  pub fn save(&self) → anyhow::Result<()>
  pub fn get_instinct_prompt(&self) → String
pub fn suggest_from_health(health: &crate::health::ProjectHealth) → Vec<String>
pub fn suggest_from_outcome(description: &str, command: &str, result: &str) → Vec<String>
pub fn suggest_from_failure(description: &str, _command: &str, error: &str) → Vec<String>
pub fn append_to_memory_md(project_path: &std::path::Path, rule: &str) → anyhow::Result<()>
pub fn load_project_rules(project_path: &std::path::Path) → Vec<String>
```

### src\task_graph.rs
```
pub struct NodeSpec
pub struct GraphNode
pub struct TaskGraph
pub struct GraphStore
pub enum NodeStatus
impl NodeStatus
impl GraphStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn create(&self, goal: &str, agent: &str, nodes: Vec<NodeSpec>) → Result<String>
  pub fn get(&self, graph_id: &str) → Option<TaskGraph>
  pub fn ready_nodes(&self, graph_id: &str) → Vec<GraphNode>
```

### src\swarm\store.rs
```
pub struct SwarmStore
impl SwarmStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn create(&self, project_name: &str, project_path: &Path, description: &str, agent: &str,) → Result<SwarmTask>
  pub fn get(&self, id: &str) → Option<SwarmTask>
  pub fn list_active(&self) → Vec<SwarmTask>
```

### src\swarm\mod.rs
```
pub struct SwarmTask
pub enum SwarmStatus
```

### src\cli.rs.bak
```
fn load_cfg() -> Config {
let detected = Config::auto_detect();
pub fn run(cli: Cli) {
let cfg = load_cfg();
let cmd = cli.command.expect("Subcommand missing");
fn cmd_instinct(cmd: InstinctCmd, dev_ops: &std::path::Path, json: bool) {
fn cmd_instinct_add(rule: &str, path: Option<std::path::PathBuf>, json: bool) {
let project_path = path.unwrap_or_else(|| std::env::current_dir().unwrap_or_default());
let memory_ok = match append_to_memory_md(&project_path, rule) {
fn cmd_instinct_list(path: Option<std::path::PathBuf>, json: bool) {
let engine = InstinctEngine::init();
let global = &engine.data.learned_rules;
let project_path = path.unwrap_or_else(|| std::env::current_dir().unwrap_or_default());
let project = load_project_rules(&project_path);
let out = serde_json::json!({"global": global, "project": project});
```

### src\mcp_server.rs
```
impl RpcResponse
impl McpServer
pub fn run_stdio() → Result<()>
```

### src\core\disk.rs
```
pub struct CacheDir
pub struct DiskReport
pub struct CleanResult
impl CacheDir
  pub fn mb(&self) → f64
impl DiskReport
  pub fn total_mb(&self) → f64
  pub fn source_mb(&self) → f64
  pub fn cache_mb(&self) → f64
impl CleanResult
  pub fn freed_mb(&self) → f64
pub fn analyze(dir: &Path) → DiskReport
pub fn analyze_all(dev_ops: &Path) → Vec<DiskReport>
pub fn clean(dir: &Path, dry_run: bool) → CleanResult
pub fn dir_size(path: &Path) → u64
pub fn human_size(bytes: u64) → String
```

### src\filebrowser.rs
```
pub struct FileEntry
pub struct AgentRuleGroup
pub struct RecentProject
pub struct GitCommitResult
impl FileEntry
  pub fn new(name: impl Into<String>, path: impl Into<PathBuf>) → Self
  pub fn readonly(mut self) → Self
  pub fn exists(&self) → bool
impl AgentRuleGroup
  pub fn exists(&self) → bool
pub fn discover_all_agent_rules(dev_ops: &Path) → Vec<AgentRuleGroup>
pub fn get_master_rule_files(master_md: &Path) → Vec<FileEntry>
pub fn get_agent_config_files() → Vec<FileEntry>
pub fn get_policy_files() → Vec<FileEntry>
pub fn get_mempalace_files(dev_ops: &Path) → Vec<FileEntry>
pub fn discover_memory_files(base: &Path, limit: usize) → Vec<FileEntry>
pub fn load_recent_projects(base: &Path) → Vec<RecentProject>
pub fn load_file_content(path: &PathBuf) → String
pub fn save_file_content(path: &Path, content: &str) → std::io::Result<()>
pub fn find_file_by_name(query: &str, master_md: &Path) → Option<FileEntry>
pub fn git_get_remote_url(dir: &Path) → Option<String>
pub fn git_is_dirty(dir: &Path) → Option<bool>
pub fn get_git_log(dir: &Path) → Vec<String>
pub fn git_commit(dir: &Path, msg: &str) → GitCommitResult
pub fn git_push(dir: &Path) → Result<(), String>
```

### src\mempalace.rs
```
pub struct MemProject
pub struct MemRoom
pub fn build(dev_ops: &Path) → Vec<MemRoom>
```

### src\discovery.rs
```
pub struct AgentInfo
pub struct SkillInfo
impl AgentInfo
  pub fn exists(&self) → bool
pub fn discover_agents() → Vec<AgentInfo>
pub fn discover_skills(skills_path: &Path) → Vec<SkillInfo>
pub fn open_in_editor(path: &Path) → anyhow::Result<()>
```

### src\factory.rs
```
pub struct Job
pub struct Factory
pub enum JobStatus
impl JobStatus
impl Job
  pub fn new(description: &str, agent: &str, project: Option<&str>, webhook_url: Option<&str>,) → Self
impl Factory
  pub fn new(tx: broadcast::Sender<String>) → Self
  pub fn submit(&self, job: Job, task: TaskFn) → Uuid
  pub fn get(&self, id: &Uuid) → Option<Job>
  pub fn list_inbox(&self, limit: usize) → Vec<Job>
```

### src\system_scan.rs
```
pub struct SystemAiTool
pub struct AiAuditReport
pub enum ToolStatus
pub fn scan_system() → AiAuditReport
```

### src\workers.rs
```
pub fn spawn_embedded_workers(tx: Sender<BgMsg>, dev_ops: PathBuf)
```

### src\app\ipc.rs
```
pub fn connect_daemon(tx: Sender<BgMsg>) → Option<Sender<String>>
```

### src\app\editor.rs
```
pub struct Editor
impl Editor
  pub fn from_content(content: &str, view_height: usize) → Self
  pub fn content(&self) → String
  pub fn handle_key(&mut self, key: KeyEvent)
pub fn simple_diff(old: &str, new: &str) → Vec<String>
pub fn char_to_byte(s: &str, char_pos: usize) → usize
```

### src\app\events\actions.rs
```
impl App
```

### src\proxy_store.rs
```
pub struct Capability
pub struct CapabilityStore
pub struct CapabilityProxy
pub enum Backend
impl CapabilityStore
  pub fn new() → Self
  pub fn register(&mut self, cap: Capability)
  pub fn get(&self, name: &str) → Option<&Capability>
  pub fn list(&self) → Vec<&Capability>
impl CapabilityProxy
  pub fn new(store: CapabilityStore) → Self
  pub fn execute(&self, capability: &str, input: &str) → Result<String>
  pub fn route(&self, query: &str, input: &str) → Result<String>
  pub fn store(&self) → &CapabilityStore
```

### src\config.rs
```
pub struct Config
pub struct DetectResult
impl Config
  pub fn config_file() → PathBuf
  pub fn load() → Option<Self>
  pub fn save(&self) → Result<()>
  pub fn auto_detect() → DetectResult
```

### src\ui\filebrowser.rs
```
pub fn render_file_panel(frame: &mut Frame, area: Rect, app: &App, files: &[FileEntry])
pub fn render_file_view(frame: &mut Frame, app: &App)
pub fn render_file_edit(frame: &mut Frame, app: &App)
```

### src\ui\panels\stats.rs
```
pub fn render_quick_stats(frame: &mut Frame, area: Rect, app: &App)
```

### src\db.rs
```
pub struct DbProject
pub struct PortfolioStats
pub struct DbTask
pub fn open_db() → Result<Connection>
pub fn import_from_json(dev_ops: &Path, conn: &Connection) → usize
pub fn load_all_projects(conn: &Connection) → Result<Vec<DbProject>>
pub fn upsert_project(conn: &Connection, name: &str, category: &str, path: &str, github: Option<&str>, status: &str, stars: Option<i64>, last_commit: Option<&str>, version: Option<&str>, nickname: Option<&str>,) → Result<i64>
pub fn project_id_for_path(conn: &Connection, path: &str) → Option<i64>
pub fn upsert_health(conn: &Connection, project_id: i64, compliance_grade: &str, compliance_score: Option<u8>, security_grade: Option<&str>, security_score: Option<u8>, security_issues: usize, security_critical: usize, git_dirty: bool, has_memory: bool, has_sigmap: bool, remote_url: Option<&str>, refactor_grade: &str, refactor_score: u8, refactor_high: usize,) → Result<()>
pub fn query_stats(conn: &Connection) → Result<PortfolioStats>
pub fn load_tasks_db(conn: &Connection) → Result<Vec<DbTask>>
pub fn insert_task(conn: &Connection, text: &str, agent: Option<&str>, project: Option<&str>,) → Result<i64>
pub fn toggle_task(conn: &Connection, id: i64, completed: bool) → Result<()>
```

### src\ui\setup.rs
```
pub fn render_setup(frame: &mut Frame, app: &App)
```

### src\ui\components.rs
```
pub fn render_boot(frame: &mut Frame, app: &App)
pub fn render_bouncing_alert(frame: &mut Frame, app: &App)
pub fn render_launcher(frame: &mut Frame, area: Rect, app: &App)
pub fn render_command_palette(frame: &mut Frame, app: &App)
pub fn render_file_changed_badge(frame: &mut Frame, _app: &App)
pub fn render_launcher_modal(frame: &mut Frame, app: &App)
pub fn center_rect(width: u16, height: u16, parent: Rect) → Rect
pub fn render_handover_modal(frame: &mut Frame, app: &App)
```

### src\daemon\server.rs
```
pub struct Server
impl Server
  pub fn new(state: Arc<RwLock<DaemonState>>) → Self
  pub async fn run_with_tx(&self, tx: broadcast::Sender<String>) → anyhow::Result<()>
  pub async fn run(&self) → anyhow::Result<()>
```

### src\search\hybrid.rs
```
pub struct HybridResult
pub enum ResultSource
impl ResultSource
  pub fn label(&self) → &'static str
pub fn fuse(bm25_results: Vec<BM25Result>, vector_results: Vec<VectorResult>, top_n: usize,) → Vec<HybridResult>
```

### src\search\indexer.rs
```
pub struct SearchResult
pub struct ProjectIndex
impl ProjectIndex
  pub fn build(root: &Path) → Result<Self>
  pub fn search(&self, query: &str) → Vec<SearchResult>
  pub fn load_or_build(root: &Path, db_path: &Path) → Result<Self>
```

### src\intelligence\edge.rs
```
pub struct EdgeRouter
impl EdgeRouter
  pub fn new(descriptions: Vec<(String, String) → Self
  pub fn route(&self, query: &str) → Option<&str>
```

### src\intelligence\evolution.rs
```
pub struct CandidateStore
impl CandidateStore
  pub fn new(db_path: impl Into<PathBuf>) → Self
  pub fn default_path() → PathBuf
  pub fn insert(&self, rule: &str, source: &str, confidence: f32)
  pub fn list_pending(&self, limit: usize) → Vec<String>
  pub fn promote(&self, rule: &str)
  pub fn sweep_expired(&self) → usize
pub fn process_job_event(event: &serde_json::Value, description: &str, command: &str, store: &CandidateStore,)
pub async fn start_evolution_worker(mut rx: broadcast::Receiver<String>)
```

### src\mcp\mod.rs
```
impl RpcResponse
impl McpServer
pub fn run_stdio() → Result<()>
```

### src\mcp\resources.rs
```
impl McpServer
```

### src\mcp\tools.rs
```
impl McpServer
```

### src\mcp\tools_dev.rs
```
impl McpServer
```

### src\mcp\tools_git.rs
```
impl McpServer
```

### src\mcp\tools_swarm.rs
```
impl McpServer
```

### src\mcp\tools_workspace.rs
```
impl McpServer
```

### src\app\events\keyboard_bak\mod.rs
```
impl App
  pub fn handle_key(&mut self, key: KeyEvent) → Result<()>
```

### src\app\events\keyboard_bak\health.rs
```
impl App
```

### src\app\events\keyboard_bak\project.rs
```
impl App
```

### src\app\events\keyboard_bak\graph.rs
```
impl App
```

### src\app\events\keyboard_bak\mempalace.rs
```
impl App
```

### src\app\events\keyboard_bak\editor.rs
```
impl App
```

### src\app\events\keyboard_bak\dashboard.rs
```
impl App
```

### src\security\scanner.rs
```
pub fn scan_project(path: &Path) → SecurityReport
```

### src\core\ci.rs
```
pub struct CiRun
pub struct CiJob
pub struct CiReport
pub fn parse_owner_repo(remote_url: &str) → Option<String>
pub fn get_ci_status(project_path: &Path) → Result<CiReport>
```

### src\app\events\bg_messages.rs
```
impl App
  pub fn handle_bg_msg(&mut self, msg: BgMsg)
```

### src\core\git.rs
```
pub struct GitStatus
pub struct GitLogEntry
pub struct GitDiffSummary
pub struct GitBranch
pub struct GitOpResult
impl GitOpResult
pub fn status(dir: &Path) → GitStatus
pub fn log(dir: &Path, count: usize) → Vec<GitLogEntry>
pub fn diff(dir: &Path, staged: bool) → GitDiffSummary
pub fn commit(dir: &Path, msg: &str, add_all: bool) → GitOpResult
pub fn push(dir: &Path) → GitOpResult
pub fn pull(dir: &Path) → GitOpResult
pub fn branches(dir: &Path) → Vec<GitBranch>
pub fn checkout(dir: &Path, branch: &str) → GitOpResult
pub fn create_branch(dir: &Path, name: &str) → GitOpResult
pub fn current_branch(dir: &Path) → Option<String>
pub fn remote_url(dir: &Path) → Option<String>
pub fn is_dirty(dir: &Path) → bool
```

### src\health.rs
```
pub struct ProjectHealth
pub fn check_project(proj: &EntityProject) → ProjectHealth
pub fn check_build_test_deps(h: &mut ProjectHealth)
pub fn check_project_with_security(proj: &EntityProject) → ProjectHealth
pub fn check_graphify(path: &Path) → (bool, Option<PathBuf>)
pub fn find_graphify_script(dev_ops: &Path) → Option<PathBuf>
pub fn validate_file(path: &Path, proj: &EntityProject) → Vec<ValidationError>
```

### src\intelligence\instinct.rs
```
pub struct InstinctData
pub struct InstinctEngine
impl InstinctEngine
  pub fn init() → Self
  pub fn add_rule(&mut self, rule: String)
  pub fn save(&self) → anyhow::Result<()>
  pub fn get_instinct_prompt(&self) → String
pub fn suggest_from_health(health: &crate::health::ProjectHealth) → Vec<String>
pub fn suggest_from_outcome(description: &str, command: &str, result: &str) → Vec<String>
pub fn suggest_from_failure(description: &str, _command: &str, error: &str) → Vec<String>
pub fn append_to_memory_md(project_path: &std::path::Path, rule: &str) → anyhow::Result<()>
pub fn load_project_rules(project_path: &std::path::Path) → Vec<String>
```

### src\ui\health.rs
```
pub fn render_diagnostics(frame: &mut Frame, area: Rect, app: &App)
pub fn render_health_view(frame: &mut Frame, app: &App)
pub fn render_system_audit(frame: &mut Frame, area: Rect, app: &App)
```

### src\app\state.rs
```
pub struct Activity
pub struct LogEntry
pub struct SetupField
pub struct PortfolioStats
pub struct RuleCategory
pub struct SearchState
pub struct MempalaceState
pub struct HealthState
pub struct TaskState
pub struct SystemState
pub struct WizardState
pub struct SetupState
pub struct InventoryState
pub struct UIState
pub struct TimelineState
pub struct ProjectState
pub struct EditorState
pub enum AppState
pub enum BgMsg
pub enum SortMode
impl SetupField
  pub fn new(label: &'static str, hint: &'static str) → Self
  pub fn with_detected(mut self, path: Option<PathBuf>) → Self
impl SortMode
  pub fn next(&self) → Self
```

### src\intelligence\router.rs
```
pub struct AgentRouter
impl AgentRouter
impl AgentRouter
  pub fn new() → Self
  pub fn init() → anyhow::Result<Self>
  pub fn route(&self, task: &str) → anyhow::Result<Option<String>>
pub fn route_capability(query: &str) → Option<String>
```

### src\core\audit.rs
```
pub struct AuditResult
pub fn parse_lighthouse_json(url: &str, json: &str, duration_ms: u128) → AuditResult
pub fn run_lighthouse(url: &str) → AuditResult
```

### src\security\mod.rs
```
pub struct SecurityIssue
pub struct SecurityReport
pub enum Severity
pub enum ProjectType
impl Severity
  pub fn label(&self) → &'static str
  pub fn deduction(&self) → i32
impl SecurityReport
  pub fn grade_from_score(score: u8) → &'static str
  pub fn critical_count(&self) → usize
  pub fn high_count(&self) → usize
pub fn score_color(score: u8) → &'static str
pub fn severity_emoji(s: &Severity) → &'static str
```

### src\security\license.rs
```
pub struct LicenseDep
pub struct LicenseReport
pub fn scan_licenses(path: &Path) → LicenseReport
pub fn is_copyleft(license: &str) → bool
pub fn is_unknown(license: &str) → bool
```

### src\security\patterns.rs
```
pub fn scan_file(path: &Path) → Vec<SecurityIssue>
```

### src\kernel\security\lease.rs
```
pub struct SecretLease
```

### src\kernel\observability\breadcrumbs.rs
```
pub struct Breadcrumb
impl Breadcrumb
  pub fn new(message: &str, level: &str) → Self
```

### src\kernel\security\redaction.rs
```
pub struct Redactor
impl Redactor
  pub fn new() → Self
  pub fn redact(&self, input: &str) → String
```

### src\kernel\security\policy.rs
```
pub struct PolicyManager
```

### src\kernel\security\audit.rs
```
pub struct AuditRecord
pub struct AuditLedger
impl AuditLedger
  pub fn new(path: &str) → Result<Self>
  pub fn log(&mut self, content: &str) → Result<()>
  pub fn verify_chain(&self) → Result<bool>
```

### src\kernel\observability\sentry_layer.rs
```
pub fn init() → Result<Option<sentry::Clien...
pub fn add_breadcrumb(message: &str, category: &str, level: sentry::Level)
```

### src\app\mod.rs
```
pub struct PaletteItem
pub struct App
impl App
impl App
  pub fn new() → Self
  pub fn tick(&mut self)
  pub fn current_menu_files(&self) → Vec<FileEntry>
pub fn filtered_palette(query: &str) → Vec<&'static PaletteItem>
```

### src\app\events\keyboard.rs
```
impl App
  pub fn handle_key(&mut self, key: KeyEvent) → Result<()>
```

### src\cli\mod.rs
```
pub struct Cli
pub enum InstinctCmd
pub enum Commands
pub enum SwarmAction
pub enum EvolveAction
pub enum GitCommands
pub fn run(cli: Cli)
```

### src\core\build.rs
```
pub struct BuildDiagnostic
pub struct BuildResult
pub struct TestResult
pub enum ProjectType
impl ProjectType
  pub fn label(&self) → &'static str
pub fn detect_type(dir: &Path) → ProjectType
pub fn build(dir: &Path) → BuildResult
pub fn test(dir: &Path) → TestResult
pub fn build_flutter(dir: &Path) → BuildResult
pub fn build_flutter_release(dir: &Path) → BuildResult
pub fn build_flutter_check(dir: &Path) → BuildResult
pub fn test_flutter(dir: &Path) → TestResult
pub fn build_ios(dir: &Path) → BuildResult
pub fn build_ios_release(dir: &Path) → BuildResult
pub fn build_ios_check(dir: &Path) → BuildResult
pub fn test_ios(dir: &Path) → TestResult
pub fn build_android(dir: &Path) → BuildResult
pub fn build_android_release(dir: &Path) → BuildResult
pub fn build_android_check(dir: &Path) → BuildResult
pub fn test_android_unit(dir: &Path) → TestResult
pub fn test_android_instrumented(dir: &Path) → TestResult
```

### src\core\deps.rs
```
pub struct OutdatedDep
pub struct CveIssue
pub struct DepsReport
impl DepsReport
pub fn check(dir: &Path) → DepsReport
```

### src\core\version.rs
```
pub struct VersionInfo
pub struct BumpResult
pub enum BumpType
impl BumpType
  pub fn parse(s: &str) → Option<Self>
  pub fn label(&self) → &'static str
pub fn info(dir: &Path) → Option<VersionInfo>
pub fn bump(dir: &Path, bump_type: &BumpType, update_changelog: bool, tag: bool) → BumpResult
pub fn changelog(dir: &Path) → String
```

### src\refactor_scan.rs
```
pub struct RefactorIssue
pub struct RefactorReport
pub struct RefactorThresholds
pub struct PartialThresholds
pub struct RefactorConfig
pub enum RefactorSeverity
impl RefactorSeverity
  pub fn label(&self) → &'static str
impl RefactorReport
  pub fn empty() → Self
impl RefactorThresholds
impl RefactorConfig
  pub fn for_ext(&self, ext: &str) → RefactorThresholds
pub fn scan_project(root: &Path) → RefactorReport
pub fn scan_project_with(root: &Path, config: &RefactorConfig) → RefactorReport
```

## vscode-extension

### vscode-extension\src\commands\CommandBridge.ts
```
export class CommandBridge
  constructor(private readonly client, private readonly outputChannel, private readonly diagnosticProvider)
  register(context) → void
```

### vscode-extension\src\extension.ts
```
export function activate(context) → void
export function deactivate() → void
```

### vscode-extension\src\ipc\DaemonClient.ts
```
export class DaemonClient
  constructor(private readonly port, private readonly host)
  onMessage(handler) → void
  connect() → void
  send(method, params, unknown> = {}) → void
  sendRaw(payload, unknown>) → void
  disconnect() → void
```

### vscode-extension\src\providers\RefactorTreeProvider.ts
```
export interface RefactorFileData
  file: string
  severity: "HIGH" | "MEDIUM"
  lines: number
  reasons: string[]
class RefactorNode
  constructor(public readonly kind, label, collapsible, public readonly fileData?)
```
