# R-AI-OS Agent Context

Follow the nearest applicable workspace or user-level `AGENTS.md` and agent constitution as the authoritative instruction source; do not duplicate those rules here.

Use `SIGMAP.md` for the current architecture map, `memory.md` for durable project decisions and operational history, and `README.md` for supported product behavior and installation guidance.
